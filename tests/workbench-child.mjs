import { mkdtemp, rm } from 'node:fs/promises'; import os from 'node:os'; import path from 'node:path';
import { migrateDatabase } from '../src/main/db/migrations.ts'; import { upsertSource } from '../src/main/sources.ts'; import { saveCurrentPlan } from '../src/main/planning.ts'; import { getToday } from '../src/main/workbench.ts'; import { createProjectFromPlanItem, getStudio } from '../src/main/content.ts';
const directory = await mkdtemp(path.join(os.tmpdir(), 'wmb-today-'));
try { const db = migrateDatabase(path.join(directory, 'wmb.db')); const source = upsertSource(db, { originalUrl: 'https://example.com', title: 'Source', summary: 'Summary source', categories: ['官方发布'], priority: 2 }); const item = { title: 'Plan', priority: 1, whyNow: 'now', timeliness: 'high', targetAudience: 'all', angle: 'angle', pointOfView: 'view', platforms: ['x'], formats: ['text'], titleGuidance: 't', openingGuidance: 'o', structureGuidance: 's', effortEstimate: '1h', sourceIds: [source.id], availableMaterials: ['release'], missingMaterials: ['test'] }; saveCurrentPlan(db, { planDate: '2026-07-27', timezone: 'Asia/Shanghai', summary: 'Summary', items: [item] }); const today = getToday(db, '2026-07-27'); const planItem = today.plan?.items[0]; if (today.sources[0]?.summary !== 'Summary source' || today.sources[0]?.categories[0] !== '官方发布' || planItem?.pointOfView !== 'view' || planItem.platforms[0] !== 'x' || planItem.availableMaterials[0] !== 'release' || planItem.missingMaterials[0] !== 'test') throw new Error('Rich Today readback mismatch'); const first = createProjectFromPlanItem(db, planItem.id); const replay = createProjectFromPlanItem(db, planItem.id); const studio = getStudio(db); if (!first.created || replay.created || first.id !== replay.id || studio[0]?.revisions[0]?.number !== 1) throw new Error('Plan-to-project mismatch'); db.close(); } finally { await rm(directory, { recursive: true, force: true, maxRetries: 3, retryDelay: 100 }); }
