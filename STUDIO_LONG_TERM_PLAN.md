# WeMediaBuddy 长期创作工作台计划

- 状态：已批准进入任务台账
- 日期：2026-07-29
- 目标：让 Studio 在 1000+ 内容项目下仍能快速找到、继续和交付稿件，并让 Pi 正确区分“新文章”和“已有文章的新版本”。
- 对应能力：CAP-004、CAP-005、CAP-014

## 1. 已确认的问题

当前 Studio 适合少量稿件，不适合作为长期工作台：

1. `getStudio()` 读取全部项目，并为每个项目分别读取全部核心版本和平台版本，形成 `1 + 2N` 查询。
2. 全部历史正文会一次性进入 IPC、Renderer 和 MCP；界面还会每 5 秒重复这次全量读取。
3. 左侧直接渲染所有项目，没有搜索、工作状态、归档和分页。
4. 项目与版本的业务边界没有落实。底层 MCP 已有 `content.create`，但 Pi 扩展没有创建项目工具，只能向已有项目追加版本。
5. `content.get` 返回全库，不能按项目精读；核心版本保存也没有项目 revision 冲突控制。
6. Studio 只显示最新版本正文和版本数量，不能查看历史版本；平台标签也没有形成完整切换体验。

本次已出现真实反例：Skill 新文章被保存成 MCP 文章的版本 3。数据没有丢失，但项目归属错误。

## 2. 产品规则

### 2.1 项目与版本

- 一篇独立文章对应一个内容项目。
- 大纲、初稿、修改稿和定稿是同一项目的核心版本。
- 用户说“写一篇”“另写一篇”或从新榜单/新机会开始创作时，默认创建新项目。
- 只有用户明确要求继续、修改或重写指定文章时，才给该项目追加版本。
- Pi 保存后必须按项目 ID 回读项目标题、最新版本号和正文，再向用户报告成功。
- 不使用标题相似度或模型判断自动猜测项目归属。

### 2.2 长期工作区

- Studio 默认只显示最近活动且未归档的项目。
- 项目具有简单工作状态：`idea`、`drafting`、`review`、`ready`、`completed`。
- 归档使用独立 `archived_at`，不建设状态机。
- “已发布”从真实发布记录派生，不作为可手工修改的项目状态。
- 用户可以搜索标题和最新正文，按状态、归档与更新时间筛选。
- 列表只返回摘要；选中项目后才读取完整版本、平台版本、来源和素材。

## 3. 目标界面

Studio 继续使用现有应用壳和右侧 Pi，不建立第二套工作区：

1. 项目栏：搜索、最近/各状态/归档快捷筛选、最多 50 条结果和“加载更多”。
2. 编辑区：项目标题、工作状态、正文编辑与保存。
3. 项目检查区：在编辑区内切换资料、版本历史、平台版本和素材；不能因 Pi 展开而消失。
4. 版本历史：查看任意历史版本、回到最新版、基于旧版本另存新版本；历史版本保持不可变。
5. Pi 上下文：始终携带准确的项目 ID；新主题没有项目 ID 时必须先创建项目。

1100px 和 1920px 宽度下都必须保持正文可编辑。项目栏固定在约 280px，编辑区自适应，Pi 继续支持折叠和调宽。

## 4. 实施阶段

### 阶段 A：修正 Pi 的项目边界

- 将现有 `content.create` 暴露为 `wmb_create_content_project`。
- 支持一次业务操作创建项目并保存首个核心版本，避免创建成功但正文失败。
- 将 `wmb_get_content` 改为按项目 ID 精读，另提供项目搜索/列表工具。
- 写入规则只保留一条：新主题新建项目；明确继续指定稿件才追加版本。
- 保存后必须精确回读，不允许仅凭模型记忆报告“已保存”。

验收：

- 从 Skill 榜单要求写新文章时产生新项目和 v1。
- 原 MCP 项目的版本数量保持不变。
- 回读的项目 ID、标题、版本号和正文一致。

### 阶段 B：数据与查询边界

- 迁移 `content_projects.status` 与 `archived_at`，旧项目默认 `drafting`。
- 为实际列表查询增加最小索引。
- 新增 `content.list({ query, status, archived, limit, offset })`，只返回项目摘要、最新更新时间、版本数和平台摘要。
- 新增 `content.get({ project_id })`，只返回单个项目完整详情。
- 搜索先使用 SQLite `LIKE` 匹配标题和最新正文；1000 篇阶段不引入 FTS。
- 核心版本写入携带项目 revision，在同一事务中检查、写版本并递增 revision。

验收：

- 构造 1001 个项目、每项 3 个 1500 字版本。
- 列表每页不超过 50 条且 payload 不含历史正文。
- 查询数量不随当前页项目数线性增加。
- 搜索能命中第 1001 个项目；筛选和翻页不重不漏。
- 两个客户端持同一 revision 保存时，一次成功，另一次返回 `REVISION_CONFLICT` 且不产生多余版本。

### 阶段 C：长期工作台界面

- 项目列表改用服务端搜索、状态/归档筛选和分页。
- 停止每 5 秒读取全库正文；轮询只更新当前列表摘要。
- 只有选中项目的 `updated_at` 变化时才重读其详情。
- 刷新、筛选和加载更多后保持当前选中项目稳定。
- 空结果、读取失败和冲突重载提供直接可执行反馈。

验收：

- 1001 项下首屏最多渲染 50 项。
- 搜索、状态、归档、清空筛选和加载更多均可实际操作。
- 外部 Agent 写入后，当前列表在一次轮询内更新；选中项目正文按需刷新。
- 1100px 与 1920px 下无页面横向溢出，正文、项目栏和 Pi 均可用。

### 阶段 D：版本与项目资料

- 实现历史版本查看、返回最新版和基于旧版另存。
- 实现资料来源、平台版本、素材的真实切换和回读。
- 版本不可原地覆盖；平台版本继续绑定明确的核心版本。
- 为误归版本提供显式“复制为新项目”操作，不自动批量猜测和搬迁历史数据。

验收：

- 任意旧版本可查看但不能被直接修改。
- 从旧版本另存后只新增一个版本。
- 项目来源、平台版本和素材与数据库读回一致。
- 现有误归版本 3 可由用户显式复制成独立 Skill 项目，正文保持一致。

### 阶段 E：迁移与千篇验收

- 在真实现有数据副本上执行迁移。
- 比对迁移前后的项目数、版本数、平台版本数和正文哈希。
- UI 与 Pi/MCP 使用同一业务命令和读模型。
- 阶段完成后只做一次 Windows 打包与真实桌面验收。

验收：

- 数据数量与正文哈希不变。
- 重启后状态、归档、当前选择和版本详情可读。
- 1001 项桌面场景、Pi 新建项目、外部 Agent 续写和冲突回读全部通过。

## 5. 明确不做

当前计划不建设：

- 文件夹和多级目录；
- 自由标签体系；
- 知识图谱；
- 看板和拖拽工作流；
- FTS/外部全文搜索服务；
- 虚拟列表依赖；
- 批量管理、收藏和协作权限；
- 自动主题相似度归类；
- 通用状态机或工作流引擎。

只有 1001 项真实验收证明分页与 SQLite `LIKE` 不够时，才升级搜索或列表技术。

## 6. 失败判定

出现以下任一情况即不算完成：

- 只在前端对全量项目做搜索或筛选；
- 项目列表接口仍返回完整正文或全部历史版本；
- Pi 通过标题相似度猜测新旧项目；
- 创建项目成功、正文保存失败却报告完成；
- 项目状态只存在界面，不经过统一业务命令；
- 定时轮询继续读取全库正文；
- 将“已发布”保存成与真实发布记录可能冲突的手工状态；
- 仅用耗时数字证明规模能力，而没有查询数量、payload 和数据一致性证据。
