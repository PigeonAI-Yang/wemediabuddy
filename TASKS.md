# WeMediaBuddy Task Ledger

This is the only progress ledger.

Statuses:

- `todo`: ready after dependencies are done;
- `doing`: actively being changed; at most one per Owner;
- `blocked`: cannot meet acceptance without user input or external state;
- `done`: deliverables and verification evidence exist.

Progress is task evidence, not a percentage.

Done receipt contract (WMB-4810 and later):

A row whose Task number is ≥ 4810 (compare the numeric part) must, when marked `done`, satisfy all four receipts in its Evidence cell:

- at least one existing repository-relative evidence path (for example `.ai/wmb-4810-xxx.json`, `tests/foo.test.mjs`);
- `Pi operator Skill impact: (updated|no change) — <non-empty note>`;
- `Independent review: <name> — <non-empty conclusion>` or `Independent review: not required — (docs-only|test-only|evidence-only|copy-only)`;
- Evidence cell total at most 700 characters; narrative detail goes in `.ai/wmb-XXXX-evidence.md` (XXXX = numeric task part).

Ledger archive: `TASKS.archive.md` stores `done` rows no longer referenced by any non-`done` row's Depends on cell; `scripts/check-ledger.mjs` validates both files as one union (the same `WMB-*` id must not appear in both), and when active `TASKS.md` exceeds 120 lines run `node scripts/tasks-archive.mjs` to move eligible `done` rows there (append-only, idempotent, byte-preserving).

CAP eval: for every `CAP-xxx` in SPEC, if all tasks referencing it are `done` and the largest such task number is ≥ 4810, `.ai/evals/EVAL-CAP-xxx.md` must exist (compare by the CAP number as lowercase 3 digits; file name uppercase, e.g. `EVAL-CAP-025.md`).

Current state:

- Active task: WMB-4910
- Next eligible task: none
- Blocked tasks: none
- Completed harness tasks: `WMB-0001`, `WMB-0002`, `WMB-0003`
- Incident reconciliation: the later report citing the 01:51 run, 62 stored sources and a temporarily null plan is the same `WMB-2404` task `4bce2956-a51e-40e1-84f7-d9d173760d55`, not a new regression. Pi inspected it before plan `8e656f92` committed at 02:06:16 and incorrectly inferred that the daily session had not run; final validation ended at 02:08:57.

| Task | Milestone | Capability | Status | Depends on | Deliverable | Verification / evidence | Owner |
| --- | --- | --- | --- | --- | --- | --- | --- |
| WMB-4910 | M-4910 | CAP-002, CAP-003, CAP-014, CAP-021 | done | WMB-4904 | Land pure `assembleEditorialBrief` (identity/history/inventory/increment) with seed-DB fixtures | 2026-08-06: `src/main/editorial-brief.ts` + `renderEditorialBrief`; four blocks (身份/历史/存量/增量), bounded SQL (30d published, 3 final reviews, 5 findings), watermark or 24h fallback increment. `tests/editorial-brief.test.mjs` 4/4; typecheck pass. Pi operator Skill impact: no change — pure context-assembly module, no Pi-facing surface yet (prompt rewire is WMB-4911). Independent review: not required — test-only | main |
| WMB-4911 | M-4910 | CAP-003, CAP-014, CAP-021 | done | WMB-4910 | Rewire daily judgment prompt to the brief + four-question mandate; judgment carries zero browser dependency | 2026-08-06: `dailyPrompt` now injects `renderEditorialBrief(assembleEditorialBrief(db))` + 四问必答 + `wmb_get_knowledge_context` 同主题查重指令；简报 `businessDate` 锚定 `task.businessDate`（评审 P3 修复），水印读 `checkpoint.judgeWatermark` 空值安全。判断路径仅依赖 Pi 配置，零浏览器依赖（prerequisite 证实）。operator Skill「资料与今日方案」新增简报/四问/查重三条。`tests/agent-runner.test.mjs` 3/3 + `tests/editorial-brief.test.mjs` 5/5；全套 283/283；typecheck pass。Pi operator Skill impact: updated — 编辑简报/四问必答/同主题查重已写入 Skill。Independent review: ReviewWmb4911 — PASS-WITH-NOTES；P3 businessDate 锚定已修复并加测试 | main |
| WMB-4912 | M-4910 | CAP-020, CAP-021 | done | WMB-4911 | Isolate per-channel scan failure (official-web never browser-bound; X absence annotated never blocking); unify rolling/manual scan entries | 2026-08-06: preflight 只挡 CHANNELS_NOT_CONFIGURED；needs_user/failed 聚合改为标注并照常判断；finishDailyIntelligenceFromReceipts 以 partial+注解收尾；validate 保留方案存在/归属/真实 sourceIds 三关并补 command_receipts 归属回退（修复预存 plans.save 归属校验只读 mcp_request_results 导致真任务恒 partial 的缺陷）。`DailyScanScheduler`（官网 2h/X 8h，env 覆盖，防重入，isCurrent 后盾，激活失败/stopRuntime/before-quit 全停）+ 协调器 `scanOnly`（跳过 Pi 前置与判断）。`tests/daily-intelligence-channels.test.mjs` 14/14、`tests/daily-scan-scheduler.test.mjs` 3/3；全套 288/288；typecheck pass；wmb-dev 实机启动稳定（清掉过期 wmb-package-final 实例后）。Pi operator Skill impact: no change — 渠道语义面向系统，Pi 工作流指引未变。Independent review: CreepyMacaw — PASS-WITH-NOTES；唯一实质 NOTE（plans.save 归属校验）已修复并加测试 | main |
| WMB-4913 | M-4910 | CAP-021 | done | WMB-4912 | Incremental judgment via checkpoint watermark, single-instance judge with queued follow-up, auto-trigger after each scan (trigger 甲) | 2026-08-06: 合成成功后写 `checkpoint.judgeWatermark`（失败不写，下轮重评）；`readLatestJudgeWatermark` 跨任务继承最近水印（评审 N1 修复，滚动流程真正只评新资料）；协调器 `judgeOnly` 直进判断、`scanOnly` 无新入库即收尾（无僵尸 running）、savedCount>0 保持 channel_scanned；调度器 `onNewSources` 全局单例 judge + judgeQueued 折叠。`tests/daily-scan-scheduler.test.mjs` 5/5、channels 15/15、agent-runner 3/3；全套 291/291；typecheck pass。Pi operator Skill impact: no change — 触发与水印为系统编排，Pi 工作流指引未变。Independent review: ChubbyHedgehog — PASS-WITH-NOTES；N1 跨任务水印已修复并加继承测试，N2-N4 为预存恢复模式与不可达边界 | main |
| WMB-4914 | M-4910 | CAP-002, CAP-003, CAP-021 | done | WMB-4913 | Pool semantics: cross-date unterminated pool view in workbench; ferment expired/dismissed states; publish-time same-topic demotion | 2026-08-06: `getOpportunityPool`（跨日期 current plan 并集，排除已采纳/dismissed/expired/done/时效过期；长青不受新近度限制；24h 同主题发布降权置尾；6h isNew）；`classifyTimeliness`+窗口（爆点 24h/热点 72h/长青常驻）；`dismissCarryForPlanItem`（状态机+无池项直接落指纹行，重播种永不复活）；`today:dismiss-plan-item` 走 dispatcher `opportunities.dismiss`+preload/global 面。`tests/opportunity-pool.test.mjs` 5/5（含 expired 分支，评审 NOTE 补）；全套 295/295；typecheck pass。Pi operator Skill impact: no change — 池语义为系统读取，Pi 工作流指引未变。Independent review: DeliberateBat — PASS-WITH-NOTES；非阻断 notes：global.d.ts pool 类型由 WMB-4915 UI 接入时补；归档重开不对称与既有 ferment 语义一致；expired 分支测试已补 | main |
| WMB-4915 | M-4910 | CAP-021, Cross-capability UX | todo | WMB-4914 | Today pool projection: pool list, timeliness/new markers, evidence-chain links, channel-absent banner, dismiss action | Pending. Pi operator Skill impact: pending. Independent review: pending | main |
| WMB-4916 | M-4910 | CAP-002, CAP-014 | todo | WMB-4911 | Deep-dive ingestion constraint (canonicalUrl required before citation) + preset native-search capability flag in Pi settings | Pending. Pi operator Skill impact: pending. Independent review: pending | main |
| WMB-4917 | M-4910 | CAP-021, CAP-025 | todo | WMB-4915, WMB-4916 | End-to-end acceptance: absent-channel run, watermark continuity, review-visible-in-brief within minutes, pool four-question quality readback | Pending. Pi operator Skill impact: pending. Independent review: pending | main |
| WMB-4900 | M-4900 | CAP-021, Cross-capability UX | done | WMB-4811 | Freeze Today daily-intelligence mainline design and hang 4901-4903 implementation chain | Design `docs/spark/2026-08-06-today-daily-intelligence-mainline-design.md` + K3 §13; PLAN M-4900; ledger 4900-4903. Owner freeze 2026-08-06 (6 items). Pi operator Skill impact: no change — docs/ledger only. Independent review: not required — docs-only | main |
| WMB-4901 | M-4900 | CAP-021, Cross-capability UX | done | WMB-4900 | Land pure `TodayRunView` projection and render Today command/empty/right-rail from that single source | 2026-08-06: `src/renderer/today-run-view.ts` + `today-command-bar.tsx`/`today-blockers.tsx`; Today consumes single view. `tests/today-run-view.test.mjs` 11/11; typecheck pass; smoke ok. `.ai/wmb-4901-evidence.md`. Pi operator Skill impact: no change — renderer projection only. Independent review: not required — copy-only UX projection with unit fixtures | main |
| WMB-4902 | M-4900 | CAP-021, Cross-capability UX | done | WMB-4901 | Remove fake missing-plan pending action; apply CTA/blocker matrix; minimal settings deep-link for real blockers | 2026-08-06: no fake plan pending card; partial CTA「继续生成方案」; needs_user「继续今日情报」+ blocker→settings section via sessionStorage. `workbench` pendingActions []. Evidence `.ai/wmb-4901-evidence.md`. Pi operator Skill impact: no change — UI navigation/copy. Independent review: not required — copy-only | main |
| WMB-4903 | M-4900 | CAP-021, Cross-capability UX | done | WMB-4902 | Prove start/continue/partial/needs_user mainline with non-contradictory copy and no start flash/requestId regression | 2026-08-06: fixtures cover idle/scan/judge/partial/needs_user/failed/done±empty/localStarting; typecheck; smoke `http://127.0.0.1:27391/`. `.ai/wmb-4901-evidence.md`. Pi operator Skill impact: no change — UX regression only. Independent review: not required — test-only | main |
| WMB-4904 | M-4900 | CAP-021, Cross-capability UX | done | WMB-4903 | Make Today distinguish current-day plan/sources from latest historical plan/sources and cross-day fermenting data so command, opportunity, feed and drawer copy cannot contradict | 2026-08-06: current-day stats exclude historical data; recent plan/source surfaces carry dates; competing formatter removed; internal codes hidden. Typecheck pass; projection 13/13; smoke and live Electron readback pass. `.ai/wmb-4904-evidence.md`. Separate backend write-routing failure remains recorded there. Pi operator Skill impact: no change — renderer projection/copy only. Independent review: not required — copy-only | main |
| WMB-4905 | M-4900 | CAP-014, CAP-021, Cross-capability UX | done | WMB-4904 | Treat the user's explicit collect, create, or review action as the Pi collaboration boundary: automatically bind the scoped task grant and worker lease, remove the separate Today authorization control, and retain precise confirmation only for external side effects | 2026-08-06: runner `onTaskReady` auto issues/reuses intent-minimal task grants before channel/Pi work, bound to exact task + worker lease; blocked/stale/no-work paths do not grant. Removed Today grant UI/preload/global API/IPC; backend enforcement + MCP get/list retained; external side-effect confirmations unchanged. Pi operator Skill updated, 3 copies hash-identical. Typecheck pass; focused 38/38; package pass; packaged Electron readback shows no grant control. `.ai/wmb-4905-evidence.md`. Pi operator Skill impact: updated — explicit action auto-binds task grant; no separate authorization. Independent review: ReviewWmb4905 — PASS after P1 blocked-path fix | main |
| WMB-4806 | M-4800 | CAP-006, CAP-017, CAP-020, CAP-021, CAP-025 | done | WMB-4805 | Implement precise execution grants and UI-only issue/revoke gates without adding roles or generic permissions | 2026-08-05: migration v42 and `.ai/wmb-4806-precise-execution-grants.json` add root-local `PreciseExecutionGrantV1`; Owner renderer IPC alone issues/revokes while MCP exposes get/list only. Exact workspace/runtime/task grant/command/inputHash/identity/actor/browser binding/account/transition/readback/expiry scope is validated and consumed once inside the same dispatcher transaction as website/X confirmation; rollback restores the grant and zero-writes every missing, stale, cross-root, broadened, identity, expiry, revoke and replay rejection. `tests/execution-grants.test.mjs`, channel proposal/MCP suites and typecheck passed (14 focused tests; 6 grant tests after frozen-field hardening). Current renderer identity smoke passed on locked port 27391 after user-approved temporary release, and the prior wemotionbuddy service was restored/read back. Pi operator Skill impact: updated - proposal-only Owner UI exact-grant/readback/no-reuse playbooks and obsolete direct channel tools removed. Independent review: GPT slow completion - ACCEPTABLE, no blocker; atomic consumption, rejection order, Owner-only surfaces and representative migrations satisfy WMB-4806. | Codex |
| WMB-4807 | M-4800 | CAP-002, CAP-003, CAP-004, CAP-005, CAP-011, CAP-012, CAP-025 | done | WMB-4806 | Route all remaining current-scope UI, MCP and scheduler business writes through the dispatcher and remove migrated direct SQLite/file write entrypoints | 2026-08-05: `.ai/wmb-4807-unified-write-routing.json` records unified dispatcher terminals and current-scope route families; `npm test` passed 251/251, `node --test tests/write-guard.test.mjs` passed 1/1, the 13-file focused regression passed 37/37, and prior `npm run typecheck` passed; only browser/publication side effects in `src/main/ipc-publishing-results.ts` and pre-runtime publication recovery in `src/main/data-root-selection.ts` are explicitly deferred to WMB-4808; Pi operator Skill impact: updated — the runtime-bound request/task/grant/receipt and exact no-direct-SQLite/data-root-file-write contract was added with no new tool surface; Independent review: `Wmb4807McpCore` — PASS, read-only MCP/UI/scheduler source review found no in-scope blocker, representative canonical business routes reach `dispatchBusinessCommand`/`dispatchCommand`, TaskGrant IDs align, runtime write guard blocks bypass, and browser/publication side effects remain the explicit WMB-4808 follow-up. | Codex |
| WMB-4808 | M-4800 | CAP-006, CAP-007, CAP-008, CAP-009, CAP-010, CAP-017, CAP-025 | done | WMB-4807 | Move browser side effects to precise grants, committed operation states, immutable publication snapshots and truthful reconciliation; retire implicit conversational/direct-tool authorization | 2026-08-06: `.ai/wmb-4808-browser-publication.json` records exact runtime/profile/binding/account/input grant checks, committed X/publication browser-operation states, immutable publication snapshots, truthful restart reconciliation and manual final publication. Final reviewer findings were fixed: editor preparation rejects profile/binding/account mismatch before browser ownership or adapter invocation, and publication/account/due metrics browser effects run under the lease returned by `setBrowser`. Focused publishing/metrics regression passed 11/11; typecheck passed; shared suite passed 252/252; operator Skill tests passed 5/5. Pi operator Skill impact: updated - exact browser authorization, immutable snapshot, truthful recovery and manual-publication playbooks synchronized. Independent review: ReviewWmb4808FixesFallback - PASS; exact identity, lease, replay, grant, operation/reconciliation and manual-final-publication invariants confirmed. | Codex |
| WMB-4809 | M-4800 | CAP-001, CAP-005, CAP-006, CAP-013, CAP-014, CAP-018, CAP-019, CAP-025 | done | WMB-4808 | Complete legacy read-only compatibility and current-package EVAL-029 across real AI/UK roots, then remove remaining legacy write entrypoints | 2026-08-06: `.ai/wmb-4809-package-readback.json` proves current Windows package AI→UK root switch, Owner profile relaunch, old MCP rejection, task-grant MCP read/write/replay/conflict, byte-preserved legacy conversation and unchanged inactive root. `npm test` 257/257; `tests/pi-operator-install.test.mjs` 4/4 after same-target install serialization; typecheck and package build pass. Pi operator Skill impact: updated - added Pi-safe task-grant aliases and exact authority guidance. Independent review: Fix4809Review - all high-severity package/fixture/alias/migration gaps repaired and package acceptance passed. | Codex |
| WMB-4810 | M-4800 | CAP-011, CAP-025 | done | WMB-4808 | Keep startup metric-job scheduling idempotent across restarts so app boot reaches window creation | 2026-08-06: fixed every-second-boot windowless crash — deterministic `publication:*:metrics-schedule` requestIds replayed under fresh runtimeEpoch in inputHash → REQUEST_REPLAY_CONFLICT before `createWindow`. Repro-first fix: `hasScheduledPublicationMetricJobs` + dual skip (windows complete OR receipt recorded) in `src/main/metric-commands.ts` boot loop. `tests/metrics-jobs.test.mjs` restart+drift test; focused regression 21/21; typecheck pass; live boot CDP 1 page target, no rejection, smoke ok; `.ai/wmb-4810-evidence.md`. Pi operator Skill impact: no change — internal startup scheduling, no Pi-facing surface. Independent review: reviewer — 通过 (0.85)；P3 已加固。 | Kimi |
| WMB-4811 | M-4800 | Cross-capability UX | done | WMB-4810 | Implement the approved renderer design pass: truthful action summaries, recency-grouped Library cards, semantic Studio formatting groups, and user-facing terminology cleanup | 2026-08-06: `.ai/wmb-4811-evidence.md` records the real Studio summary chain, Today/Results action sentences, Library recency groups, Studio toolbar semantics and business-language cleanup. Typecheck and focused regression passed; full suite 258/258; Windows x64 package built and its EXE rendered `WeMediaBuddy` from app.asar with a populated root and no overflow. Pi operator Skill impact: no change — presentation and read-only summary changes do not alter Pi tools, workflow, authority or recovery. Independent review: ReviewWmb4811 — evidence and current diff support every claim; no blockers. | main |
| WMB-4812 | M-4800 | CAP-025 | done | WMB-4809 | Close the remaining current-package EVAL-029 coverage explicitly left by WMB-4809: real Pi model continuation under worker lease, packaged precise-grant negative matrix, and packaged immutable publication-snapshot/browser reconciliation | 2026-08-06: `.ai/wmb-4809-package-readback.json` + `.ai/wmb-4812-evidence.md`. Fixed list-ops pure-read + one-shot recover and headless Owner-dialog accept. Packaged ok: Pi modelExecuted=true/save_version, precise-grant matrix, snapshot immutable + authorize BROWSER_NEEDS_USER, profile relaunch 1→2. Typecheck + focused X List tests pass. Pi operator Skill impact: no change — runtime/acceptance only. Independent review: ReviewWmb4812 — 通过，无 mismatch。 | Codex |
| WMB-4600 | M-4600 | CAP-014 | done | WMB-4509 | Reuse pinned `pi-vision-tool` so text-only DeepSeek V4 Flash can delegate explicit local-image inspection to MiMo and continue from the returned text without changing the main model | Baseline proved only `deepseek-v4-flash input=text` and no vision tool. Pinned audited `pi-vision-tool@1.3.7`; packaged Pi now loads `describe_image`, keeps DeepSeek as the text-only main model, registers `mimo-v2.5 input=text,image` on the same preset and exposes `/vision`. Focused tests passed 9/9, typecheck, `git diff --check` and Windows packaging passed; packaged resources contain extension 1.3.7 and the updated operator Skill, app.asar SHA-256 `BB9598EAA58500F09671897BDE27F1FF9ADB541C03F11DF4DFCA4788F8B4E215`. Real packaged startup readback proved title `WeMediaBuddy`, main model `deepseek-v4-flash`, `/vision` and the dual text/multimodal registry; a current authenticated provider call made `mimo-v2.5` correctly read `VI.png` as `WeMediaBuddy` on black. `/models` misleadingly listed `mimo-v2-omni`, but its real image request returned HTTP 400 unsupported, so the route was corrected to the proven `mimo-v2.5`. EVAL-028 passed 2026-08-06 after the harness-only filter fix (events broadcast as `tool`/`tool-result`, not `tool_execution_start`/`tool_execution_end`): `.ai/wmb-4600-eval-028.json` records exit 0 with title `WeMediaBuddy`, `textDescribeImageCalls=0` (`deepseek-v4-flash` control reply `文本正常`), `visionDescribeImageEvents=2` (renderer-visible `describe_image` via `wmb-api/mimo-v2.5`), `visionProviderModel=true`, and final assistant text reporting `WeMediaBuddy` 黑色 `#000000`. Pi operator Skill impact: updated with explicit `describe_image` use, compression and truthful failure/no-fallback boundaries. | Codex |
| WMB-4509 | M-4500 | CAP-013 | done | WMB-4508 | Use the approved Work motion for an unfinished Pi tool call and the approved closed-eye Sleep motion after its result arrives | Tool rows now map unfinished calls to the complete approved Work state (body, gaze, left/right task cards and progress bar) for the entire call, then map returned results to the closed-eye Sleep state and hide the work cues. Canonical Logo and motion-library assets are unchanged; reduced-motion keeps a static closed eye. Focused Pi flow passed 11/11, typecheck, `git diff --check` and Windows packaging passed. Real packaged `bash · sleep 3` readback proved `running/is-working`, `wmb-creature-work`, `work-eye`, both task-card animations and progress animation; the same row then became `completed/is-sleep`, lid `translateY(118px)`, with work cues hidden. Launched PID 286544; app.asar SHA-256 `B7276DC15AD8F556E26E48C24842E86C941BBC5509176F374410FD15444316D4`. Pi operator Skill impact: none — renderer-only status presentation. | Codex |
| WMB-1305A | M-1300 | CAP-005, CAP-014, CAP-016 | done | WMB-1305 | Replace the standalone context-package interface with direct page selection | 2026-07-29 Owner changed the contract twice: no package UI and no invisible package/snapshot rows. The minimal reproduction counted real DB `knowledge_context_packages=1` and `knowledge_context_uses=3`, then showed the old turn wrote both. The replacement resolves the current canvas read-only immediately before send and embeds that exact manifest in the existing Pi session turn; renderer no longer calls package create/use for direct context. Hidden real Electron on normal user data showed no package UI, default `当前页 2 项`, checkbox multi-select `已选 2 项`, and blank-board pointer down/up restored `当前页 2 项`. A selected-one real Pi turn returned exactly title `Chubby Skills…` and node `0bae3d3d-216c-45cd-bcb3-3f350e7ef61e`; the new session JSONL turn contains only that direct manifest and no tool call, while DB stayed exactly `packages=1, uses=3`. 1100×700 and 1920×900 had zero document/body overflow; screenshots `.ai/wmb-1305a-direct-context-1100.png` and `wmb-1305a-direct-context-1920.png`. Typecheck and lightweight harness passed. Historical package rows/APIs remain read-only-compatible but new UI creates none. | Codex |
