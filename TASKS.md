# WeMediaBuddy Task Ledger

This is the only progress ledger.

Statuses:

- `todo`: ready after dependencies are done;
- `doing`: actively being changed; at most one task;
- `blocked`: cannot meet acceptance without user input or external state;
- `done`: deliverables and verification evidence exist.

Progress is task evidence, not a percentage.

Current state:

- Active task: none (`WMB-2403` is at its verified root-local X login gate)
- Next eligible task: none
- Blocked tasks: `WMB-2403` (waiting for the user to sign in to X in the exact UK root-local Edge window opened by WMB)
- Completed harness tasks: `WMB-0001`, `WMB-0002`, `WMB-0003`
- Incident reconciliation: the later report citing the 01:51 run, 62 stored sources and a temporarily null plan is the same `WMB-2404` task `4bce2956-a51e-40e1-84f7-d9d173760d55`, not a new regression. Pi inspected it before plan `8e656f92` committed at 02:06:16 and incorrectly inferred that the daily session had not run; final validation ended at 02:08:57.

| Task | Milestone | Capability | Status | Depends on | Deliverable | Verification / evidence | Owner |
| --- | --- | --- | --- | --- | --- | --- | --- |
| WMB-2400 | M-2400 | CAP-014 | done | WMB-0003 | Create the canonical installation-wide `wemedia-buddy-operator` Skill with current software-operation playbooks and deterministic validation of every named `wmb_*` tool | 2026-08-03: Baseline from WMB-0003 confirmed no canonical shared operator Skill existed and the active UK root contained only its lane Skill. The required `skill-creator` initializer created `skills/wemedia-buddy-operator`; the final 96-line `SKILL.md` defines current-root identity, WMB-only writes, stable request IDs, UI-only confirmation/final publication, exact readback, state/error preservation and workflow routing for channels, X Lists, sources/plans, content/briefs, workspaces, Xiaohongshu read-only research and reviews. Its compact inventory names all 48 currently registered `wmb_*`/`xhs_*` Pi tools. `tests/pi-operator-skill.test.mjs` deterministically extracts the real tool registry and requires exact equality, no stale external-tool term, no placeholders and all hard boundaries. Skill Creator `quick_validate.py` passed; focused operator/extension coverage passed 2/2; `git diff --check` passed. The lightweight Harness passed its required policy indexes but now stops during placeholder scanning because it traverses ignored runtime data under `data/ukcontentdata/browser-profile` and finds third-party extension `TODO` text; WMB-2400 did not change that Harness behavior or runtime data. No data-root copy, Pi load or packaged behavior is claimed before WMB-2401/2402. Pi operator Skill impact: updated — created the canonical operating playbooks and exact live-registry validator. | Codex |
| WMB-2403 | M-2400 | CAP-014, CAP-017, CAP-018, CAP-020 | blocked | WMB-2400 | Falsifiably reproduce and repair the current-root browser/login workflow that blocks X List save/read after configuring the dedicated Edge profile; remove stale user-facing `pyaireader` guidance and update the operator Skill to the verified flow | 2026-08-03 partial evidence: live packaged MCP on UK workspace `4f29f851-c489-4cd2-bad7-12e95f956cdd` reproduced `BROWSER_NEEDS_USER: 浏览器未在时限内提供 CDP` while the correctly selected root-local profile was already running under WMB. Process/readback proved the configured profile had no persisted CDP URL, the managed browser used dynamic port 54770, and each later `startBrowser(config)` ignored the same-profile runtime because the map was keyed only by CDP URL; Pi's claim that X List reads require a separate manual background-start step was false. The regression now reuses a reachable managed runtime by resolved profile path. Current Windows package then read account `@KimbomArtist` and 8 real Lists from the existing logged-in AI profile, proving the runtime fix, while the UK root truthfully returned `无法从专用 X 浏览器读取当前账号` rather than timing out. Legacy UI labels now say `旧版共享 X 登录态（仅 AI 工作空间）`; UK and operator Skills contain no retired provider guidance. Pi conversation preparation atomically refreshes only the current root lane Skill: repository/package/UK-installed `uk-life-content-radar` SHA-256 is `ECE80AC9…98C0C1`, with no AI lane in UK. Focused browser/X/conversation/profile/operator checks passed 11/11; package build passed, app.asar SHA-256 `D1057946…A16DA`; typecheck retains only the three historical diagnostics. The exact UK Edge is visible at PID 6692 with profile `data\ukcontentdata\browser-profile\Default`; successful real index readback after human login remains the only acceptance item. Pi operator Skill impact: updated — corrected automatic browser reuse/login guidance; the UK lane now routes X only through current-root WMB tools. | Codex |
| WMB-2404 | M-2400 | CAP-014, CAP-021 | done | WMB-2400 | Falsifiably reproduce the reported 13/13 daily-intelligence apparent stall; determine whether persisted task progress, renderer phase mapping or the daily Pi judgment/save lifecycle is wrong, then keep progress and status truthful until the plan is available | 2026-08-03: The real UK-root task `4bce2956-a51e-40e1-84f7-d9d173760d55` supplied the falsifiable timeline: 13/13 channels finished at 02:02:49, Pi reported judgment at 02:05:13, plan `8e656f92` committed at 02:06:16, final validation ended at 02:08:57. This disproved Pi's claim that judgment never started. Root cause was twofold: Today presented channel counters as whole-task progress and lacked labels for `channel_scanned`/lane-Pi phases, while the UK/game lane runner had no 15-second persisted heartbeat although the AI runner did, so normal Pi reasoning exceeded the renderer's 60-second stalled threshold. The lane runner now records `judging_opportunities`, an immediate phase event and a 15-second heartbeat until settlement. Today labels the channel and judgment phases, changes the bar to indeterminate after channel completion, labels 13/13 as channel scan rather than total completion, and preserves the stalled warning for genuinely stale heartbeats. The operator Skill forbids launching a duplicate judgment/save while the same daily task is in its post-channel phases. The focused test was red before the change and then the daily/task/workspace/UI/operator suite passed 20/20; package build passed, and the built renderer contains the new phase labels and indeterminate state. app.asar SHA-256 begins `972620335FCC5872`. Typecheck reports only the unchanged three historical diagnostics. `git diff --check` passed. Pi operator Skill impact: updated — documented the post-channel automatic closure and duplicate-run prohibition. | Codex |
| WMB-2401 | M-2400 | CAP-014 | done | WMB-2404 | Package, install and revision-refresh the shared operator Skill for every Pi data root while preserving one root-specific lane Skill and a minimal immutable system prompt | 2026-08-03: Baseline proved the package contained `skills/` but data roots installed only whichever lane Skill had previously run, while dock startup duplicated detailed workflow instructions in its system prompt. New 59-line `pi-operator-skill.ts` hashes the complete canonical tree, stages and atomically replaces only `pi-agent/skills/wemedia-buddy-operator`, records deterministic `.wmb-install.json`, skips an identical tree, and refreshes corrupted/stale copies. `ensurePiConversationLayout` covers fresh/on-use roots; packaged startup installs every registered root in parallel. All five Pi launch sites now reuse one immutable authority prompt with no named operating sequence; lane startup appends only workspace/lane identity. A falsifiable two-root test first failed with the missing installer, then passed installation, stale-file removal, corrupted-copy refresh, identical revision and sibling lane preservation. Focused installer/conversation/extension/runner/profile/operator coverage passed 8/8; Windows packaging passed. Launching that package installed exact revision `770bbe0a…bd325` into existing AI, game and UK roots; repository, packaged resource and all three installed trees read back the same revision and `SKILL.md` SHA-256 `6CEA6B2D…A8FD2`. Existing AI `wemedia-intelligence-engine` and UK `uk-life-content-radar` hashes still match their packaged lane sources; game had no prior lane copy and WMB did not invent one. No actual Pi load/follow claim is made before WMB-2402. Typecheck retains only the same three historical diagnostics; every changed source remains at or below 500 lines; `git diff --check` passed. Pi operator Skill impact: no change — canonical playbooks are unchanged; this task installs their exact current hash and removes duplicated workflow text from system prompts. | Codex |
| WMB-2402 | M-2400 | CAP-014, CAP-017, CAP-018, CAP-020 | todo | WMB-2401, WMB-2403 | Run focused Windows-package acceptance proving fresh/existing roots load and follow the shared operator Skill for channel proposal, real X List save/read and content-project workflows without direct writes or confirmation bypass | Partial acceptance is preserved: current package UK Pi prepared an exact no-write channel proposal and created/read back content project `6be71387-2bc7-4271-a7f8-f3b2e3316186`; fresh root Pi loaded the root-installed operator Skill and made read-only WMB calls with all business counts still zero; existing AI/UK roots and lane copies match the packaged operator revision. Final X/Pi/provider and no-bypass audit resumes after WMB-2403. | Codex |
| WMB-2300 | M-2300 | CAP-020, CAP-021 | done | WMB-2205 | Restore Today as an information-first creation surface: remove every channel selection/readiness/configuration surface, automatically scan every enabled Discover channel, keep one canonical start action, and report preflight only after explicit start | 2026-08-03: Owner rejected the first package readback because a persistent no-channel/Discover guidance still made Today carry backend configuration responsibility and three `开始今日情报` buttons duplicated the same action. The final contract and renderer remove every persistent channel-selection/readiness/configuration surface, narrow the Today preload payload to `{ businessDate }`, and retain authoritative all-enabled-channel preflight only after an explicit start. Today now has one canonical top-right start action; the empty opportunity state and pending-action card are status-only. Focused daily/UI coverage passed 14/14, including both default frozen modules and an exact one-start-handler source assertion. `npm run build` passed and reused the unchanged Pi runtime. Current Windows-package DOM readback at 1600×960 returned exactly one `开始今日情报` button, zero persistent channel guidance, zero channel panels, zero checkboxes and no horizontal overflow; screenshot `J:\Users\yangda01\Temp\wmb-2300-today-final.png` was visually inspected. EXE SHA-256 `D841BCB4…B8E8F152`; app.asar `13F6846D…022D38CE`. Typecheck retains only the same three historical diagnostics in `intelligence-wire.ts` and `x-list-execution.ts`; `git diff --check` passed. The lightweight gate still stops on the same 12 historical over-500-line files; Today shrank to 1344 lines. | Codex |
| WMB-2100 | M-2100 | CAP-020, CAP-021 | done | WMB-2002 | Approve the workspace intelligence-channel product/technical contracts and freeze one dependency-ordered implementation chain from source configuration through real packaged acceptance | 2026-08-03: Owner approved the design and authorized implementation. PRD REQ-020–022/AC-016–018, SPEC CAP-020–021/EVAL-020–023, technical design, PLAN M-2100 and WMB-2100–2107 now agree that fixed website/X Lists modules use root-local source configuration and one shared daily scan path; existing AI source-index/List fixtures migrate to ordinary AI-root configuration while AI rankings remain profile-only. Two independent read-only audits traced contracts and the real source/X List/daily completion call path. `M2100_TRACEABILITY_OK` verified every new ID, row, dependency and the single-doing invariant; `git diff --check` passed. The lightweight gate reached and reproduced only the exact 13 pre-existing over-500-line source files; manually continuing the historical row parser also reproduced pre-existing malformed WMB-1402. Neither baseline issue was changed. The Pi stale temporary-working-directory BUG remains explicitly outside M-2100. | Codex |
| WMB-2101 | M-2100 | CAP-020 | done | WMB-2100 | Add root-local website-source configuration, generic per-source scan receipts, shared channel/readiness business reads and migrations while reusing `source_feeds`, `source_items` and existing X List bindings | 2026-08-03: migration 37 adds root-local `website_sources` referencing existing `source_feeds` plus generic `source_scan_receipts`; X List identity remains only in `x_list_bindings`. New `intelligence-channels.ts` provides ready-only website configuration creation with explicit existing-feed reuse, canonical uniqueness, revisioned enable/resolution state, workspace/module/source/feed-bound idempotent receipt read/write, and one website/X List readiness projection. Enabled X bindings without a valid root browser config report `needs_user`, not ready. `workspaces.get_current` now returns the same authoritative channel summary. Two temporary real data roots stored the same canonical URL and logical task receipt under different workspace IDs with different source/feed IDs and zero cross-read; wrong workspace, missing workspace and wrong website/X source-feed pair each produced zero receipt write. Sol required and verified those negative cases after Luna-max implementation. `node --test tests/migrations.test.mjs tests/intelligence-channels.test.mjs tests/workspace-profiles.test.mjs tests/workspace-proposals.test.mjs` passed 5/5; migration reopen reads 37 and both tables. `git diff --check` passed; all changed/new code files are under 500 lines. `npm run typecheck` reports only the unchanged three baseline diagnostics at `intelligence-wire.ts:76` and `x-list-execution.ts:145`. No package/UI/network check was required for this schema/business-read task. | Codex |
| WMB-2102 | M-2100 | CAP-020 | done | WMB-2101 | Deliver arbitrary public website name/URL resolution, canonical identity, real trial read, duplicate rejection, enable/disable/remove and zero-item truthful readback without heartbeat source items | 2026-08-03: Baseline `b125273` reproduced that `runOfficialWebWire` wrote a fake `[官宣巡检]` source when extraction returned zero and had no generic website resolver. The shared website channel now resolves direct URL/domain or real Bing candidates, canonicalizes identity, rejects literal local/private addresses and unsafe redirects, requires successful trial read and explicit choice when duplicate feeds are ambiguous, then supports confirm/enable/disable/remove while retaining historical feeds/items. Scans persist only extracted items plus truthful per-source receipts; zero-item success writes 0 items, and partial write failure returns exactly the committed IDs/count. `node --test tests/website-channel.test.mjs tests/intelligence-channels.test.mjs tests/source-foundation.test.mjs tests/intelligence-wire.test.mjs tests/migrations.test.mjs` passed 15/15; `git diff --check` passed. A live temporary-root run searched `GOV.UK visas immigration official`, returned 8 candidates, selected `https://www.gov.uk/`, trial-read `Welcome to GOV.UK` with 4 candidates, and read back 1 website source, 1 feed, 4 real items and 1 succeeded receipt; the root was removed. `npm run typecheck` still reports only the unchanged three baseline diagnostics at `intelligence-wire.ts:76` and `x-list-execution.ts:145`. | Codex |
| WMB-2103 | M-2100 | CAP-020 | done | WMB-2101 | Resolve X List name/URL/ID against the current root account, return every real same-name candidate, and reuse the existing binding path without guessing or cross-root state | 2026-08-03: Baseline `7f80420` confirmed `readXListIndex` and stable-ID `bindXList` existed but no business function converted a supplied List name/URL/ID into explicit candidates. New `x-list-channel.ts` reads the current workspace browser index, treats that readback account as identity, resolves NFKC/case-insensitive exact names or exact canonical URL/ID, returns every same-name candidate in deterministic order and performs zero writes. Confirmation verifies workspace/resolution identity, re-reads the current account/List and delegates to existing `bindXList`; same binding/feed is reused, while forged candidates, account changes, cross-root resolutions and browser-workspace mismatch all write zero rows. `node --test tests/x-list-channel.test.mjs tests/x-list-cache.test.mjs tests/x-list-timeline-cache.test.mjs tests/x-list-session.test.mjs tests/x-list-operations.test.mjs` passed 12/12; `git diff --check` passed; both new files are under 500 lines. `npm run typecheck` still reports only the unchanged three baseline diagnostics at `intelligence-wire.ts:76` and `x-list-execution.ts:145`. The real read-only `tests/x-list-live-readback.mjs` honestly failed with `ECONNREFUSED 127.0.0.1:9334`; no legacy/default X profile was started or claimed as current-root evidence. Real isolated-account readback remains an explicit WMB-2107 packaged acceptance item. | Codex |
| WMB-2104 | M-2100 | CAP-021 | done | WMB-2102, WMB-2103 | Move deterministic website/X List scans and preflight to one shared daily-intelligence entry; freeze selected source revisions, persist per-source receipts, aggregate four truthful task states, and allow zero-update empty-plan success | 2026-08-03: Baseline `6c4180e` reproduced `VALIDATION_ERROR: 成功要求至少一条真实资料落库。` with no receipts/items/plan and confirmed empty plans were rejected. New `daily-intelligence-channels.ts` is the shared AI/UK/game coordinator: it freezes workspace/profile/module/source revisions, scans websites concurrently and X Lists serially before lane judgment, persists one root-local receipt per selected source, rejects unfrozen receipts, resumes only `resume_pending`, prevents duplicate scans, and aggregates succeeded/partial/needs_user/failed without rolling back committed items. X collection now revalidates binding/revision/account after async read and before item/cache writes. Daily completion is receipt-backed, permits a real empty current plan, retains strict real-source validation for nonempty plans and preserves Studio/Results gates. Legacy AI fixed website/List wires and six collection routes no longer run; watching/fermenting summaries remain judgment-only inputs. Cancel remains cancelled. The real IPC now leaves task creation to the coordinator and deduplicates by data-root/date instead of precreating an unfrozen task. Parent focused coverage passed 41/41 across coordinator, task gates, websites, X, workspace routing, needs_user and platform boundaries. A production-function temporary-root readback froze source revision 1, scanned a readable zero-link website, saved an empty plan, and read back `succeeded/completed`, 0 source items, 1 succeeded receipt and 0 plan items. Full `npm test` passed 130/133; the only failures are unchanged baselines: content-list child Windows `EBUSY`, settings expecting migration 36 while current is 37, and source-logo expecting 29 while current is 38. `npm run typecheck` reports only the unchanged three diagnostics at `intelligence-wire.ts:76` and `x-list-execution.ts:160`; `git diff --check` passed. The lightweight gate stops only on the same 12 pre-existing over-500-line files; every changed/new source is now under 500 lines. Real isolated X/browser and packaged Windows acceptance remains WMB-2107. | Codex |
| WMB-2105 | M-2100 | CAP-020, CAP-021 | done | WMB-2104 | Add Discover intelligence-channel management plus Today module/source selection and preflight, using the shared business snapshot instead of renderer reconstruction | 2026-08-03: Discover now defaults to a fixed “情报渠道” surface and retains AI-only rankings plus full List management. Its narrow IPC delegates arbitrary website resolve/trial/confirm and current-account X List resolve/confirm to the existing business functions, returns the shared channel summary plus latest receipts, and reuses existing revisioned enable/disable/remove/scan paths; website removal and X “移出发现” retain historical data. Today reads that same root-authoritative summary, shows website/X configured/enabled/ready/blocked counts and source names, defaults both modules, permits per-run deselection only, sends `{ businessDate, modules }`, blocks every start entrance when Pi or runnable channels are missing, and no longer offers the obsolete per-source skip control. Source mutations refresh Settings/Today through the existing data-changed bus. Focused parent coverage passed 26/26 across website resolution/trial/confirm/removal, X same-name candidates/current-account confirmation, channel receipts, shared daily orchestration and UI preflight/payload. An isolated Electron/CDP cold readback returned title `WeMediaBuddy` and `#root`; Today showed both default module checks and disabled primary/pending-action starts under the Pi blocker, while Discover showed `情报渠道 / 榜单 / List 管理`, both add flows and current sources. `npm test` completed 132/135; only the unchanged baselines failed: content-list child Windows `EBUSY`, settings expecting migration 36 rather than 37, and source-logo expecting 29 rather than 38. `npm run typecheck` reports only the unchanged three diagnostics at `intelligence-wire.ts:76` and `x-list-execution.ts:160`; `git diff --check` passed. The lightweight gate still stops only on the same 12 historical over-500-line files; every new source and `index.ts` are below 500, while the minimally touched historical `today-library-view.tsx` remains one of those 12. Real current-account X and packaged root-switch acceptance remains WMB-2107. | Codex |
| WMB-2106 | M-2100 | CAP-020 | done | WMB-2105 | Let Pi/external Agents prepare exact batched source changes and let UI confirm once against workspace/profile/source identities and exact diff, with stale zero-write rejection | 2026-08-03: Baseline `fcc8bac` had only direct UI mutations and no Agent-prepared channel change contract. A session-only `IntelligenceChannelProposalStore` now gives UI, MCP and Pi the same root-authoritative reads plus website resolve/trial, current-account X resolve and request-id-idempotent batch prepare. Prepared changes bind workspace ID, profile revision, proposal revision/hash, action/module/stable identity, existing source revision and the complete ordered displayed diff; prepare writes zero data. Only `intelligence-channels:proposal-confirm` exists, behind the workspace mutation gate; MCP/Pi expose no confirm/activate tool. Confirmation re-trials websites, re-reads the current X account/List index, revalidates every database identity/revision and diff before applying one transaction; any tamper, cross-root/profile/source/account change, duplicate/conflicting item or apply-time failure returns `CONFIRMATION_STALE` and rolls the whole batch back. Successful remove/disable retains source items. Main review found and corrected a root-isolation gap: pending proposals are now listed only when both current workspace ID and profile revision match, so another root or an old profile proposal is neither shown nor confirmable. Parent focused coverage passed 30/30 across proposal/MCP/Pi, channel UI/business, website/X resolution, workspace proposals/runtime and Pi extension copying; the proposal suite alone passed 7/7 with real DB zero-write/commit/rollback/readback. The Agent full suite completed 138/141 with only the unchanged baselines: content-list Windows `EBUSY`, settings migration `37 !== 36`, and source-logo `38 !== 29`. `npm run typecheck` still has only the unchanged three diagnostics at `intelligence-wire.ts:76` and `x-list-execution.ts:160`; `git diff --check` passed. The lightweight gate stops only on the same 12 historical over-500-line files; all new/changed source files are at or below 500 (`website-channel.ts` exactly 500, `index.ts` 499). WMB-2107 retains real packaged AI/UK, current-account X and cold-switch acceptance. | Codex |
| WMB-2107 | M-2100 | CAP-020, CAP-021 | done | WMB-2106 | Run focused packaged AI/UK acceptance for arbitrary websites, same-name X Lists, cold-switch isolation, zero-update success, partial preservation, all-blocked preflight and exact authorization | 2026-08-03: Final current-source Windows package `J:\Users\yangda01\Temp\wmb-2107-package-final-current-652` (EXE SHA-256 `a54fe415…dbe6d21e`, app.asar `7a4fcf5b…ff6752da`) passed EVAL-020–023; the user's running `out` package was left untouched. Packaged UI resolved/trial-read/confirmed a redirected GOV.UK entry, rejected duplicate canonical and stale confirmation with identical before/after table counts, retained unreadable HTTP 404 truth, scanned 8 real items and wrote truthful receipts. The real dedicated X profile read `@KimbomArtist` and 7 accessible Lists; `AI前沿` name, canonical URL and ID all resolved to List `2082851520417255750`, UI confirmation reused the exact binding/feed, while duplicate binding, forged account and missing-login paths wrote zero. The current X management DOM no longer exposes stable List anchors; the existing GraphQL management response remained authoritative, and CLI readback was repaired to clear successful-operation timers and detach its CDP socket while Electron keeps pooled reuse. Cold packaged AI/UK roots retained distinct website/X source, feed, item and receipt IDs with zero cross-read; MCP/Pi exposed prepare but no confirm, and UI-only confirmation remained workspace/profile/source/diff bound. Production-function receipts proved zero-update empty-plan `succeeded`, one-success/one-failure `partial`, all-blocked pre-Pi `needs_user`, and no trustworthy receipt `failed`. Packaged Discover/Today at 1672×982, 1366×768 and 1100×760 passed readable hierarchy, hit target and no-overflow checks after the idle command card was allowed to wrap; the 1100 channel selection measured 416px and the primary action 114×40. Acceptance also reproduced and fixed two product bugs before rebuilding: channel confirmation was incorrectly exempt from workspace-switch drain, and website redirects overwrote the selected request identity. Parent focused coverage passed 24/24; full `npm test` completed 142/145 with only the unchanged content-list Windows `EBUSY`, settings migration 37/36 and source-logo 38/29 baselines. `npm run typecheck` retained only the unchanged three diagnostics at `intelligence-wire.ts:76` and `x-list-execution.ts:160`; `git diff --check` passed. `x-list-session.ts` remains its 652-line baseline with +10/-10, and the lightweight gate still stops on the same 12 historical over-limit files. Reproducible scripts, exact DB evidence and screenshots are in `.ai/wmb-2107-package-acceptance.*`, `.ai/wmb-2107-x-acceptance.mjs` and `.ai/wmb-2107-today-layout-*`. | Codex |
| WMB-2200 | M-2200 | CAP-022 | done | WMB-2107 | Approve the X List trend-opportunity contract, capture a current real structured-metrics baseline, and run a minimal falsifiable List-ID response-isolation experiment before implementation | 2026-08-03: REQ-023/AC-019/CAP-022/EVAL-024–025 and the six-task M-2200 chain now agree on explicit user start, exactly +15m/+60m/+180m active-root observations, append-only snapshots over canonical `source_items`, truthful per-field status/raw evidence, deterministic two/three-point trend math, existing topic/plan/content lineage and no service/API/social automation/second store. `.ai/wmb-2200-baseline.mjs` passed against the real dedicated X profile: `@KimbomArtist` List `AI前沿` (`2082851520417255750`) returned 20 posts and all 20 had at least one structured metric. A two-List real GraphQL experiment proved each `ListLatestTweetsTimeline` response exposes exact `variables.listId`; source inspection then reproduced the current no-op mismatch branch, so a response for control List `2082177169078251627` would not be rejected from a target capture. Root cause is frozen for WMB-2201; no product behavior was changed in this contract task. `git diff --check` passed. | Codex |
| WMB-2201 | M-2200 | CAP-011, CAP-017, CAP-022 | done | WMB-2200 | Add truthful per-field X metric parsing evidence and append-only root-local post snapshots after account/binding/revision revalidation, reusing the existing source identity and latest cache | 2026-08-03: Migration 38 adds root-local append-only `x_post_metric_snapshots` keyed by observation/source while canonical post identity remains in `source_items` and latest browse cache remains disposable. GraphQL and DOM paths now preserve all five fields as `value`/`unsupported`/`unavailable`/`parse_failed` with raw label, raw value and parsing source while retaining numeric `metrics` for the existing UI. Exact `variables.listId` matching fixes the WMB-2200 cross-List capture root cause. The sole collect path revalidates workspace/account/binding/revision after the browser read, requires metric evidence, and commits source upserts, snapshots, cache and observation atomically; replay is idempotent, later observations append, failure/stale paths roll back, and observation metadata no longer mutates the binding configuration revision. Two independent read-only reviews challenged parser and transaction boundaries before acceptance. Focused migration/metric/collection/daily/cache coverage passed 23/23, including zero, `亿`, malformed labels, wrong/missing List IDs, same-observation replay, later append, stale zero-write and forced snapshot rollback. A real logged-in isolated-temp-root read of `@KimbomArtist` List `AI前沿` wrote 20 canonical sources and 20 snapshots; raw `views.count`, normalized views, page fingerprint and workspace evidence read back, with no formal data-root mutation. Full `npm test` passed 146/148; only the unchanged content-list Windows `EBUSY` and source-logo `38 !== 29` baselines remain (the prior settings migration-count failure is now correctly updated). Typecheck retains only the unchanged three diagnostics in `intelligence-wire.ts` and `x-list-execution.ts`; `git diff --check` passed. The lightweight gate stops on the same 12 historical over-limit files, while `x-list-browser.ts` shrank substantially and all new files are below 500. | Codex |
| WMB-2202 | M-2200 | CAP-003, CAP-011, CAP-015, CAP-022 | done | WMB-2201 | Add deterministic snapshot/trend business reads, exact data-insufficient reasons and read-only IPC/MCP/Pi surfaces without a black-box score | 2026-08-03: `x-post-metrics.ts` now returns a bounded chronological snapshot history and a deterministic trend projection over the latest real samples. Two comparable snapshots at least ten minutes apart return exact `views_per_hour`; three return the difference between consecutive velocities plus both auditable intervals/snapshot IDs. Stable reasons cover missing source/samples, invalid or non-increasing capture time, short interval, unsupported/unavailable/parse-failed views and decreasing counts. The readback has no score or persisted derived value. Root-local read-only IPC/preload surfaces and MCP tools `x_lists.post_metric_snapshots_list` / `x_lists.post_trend_get` use the same business functions without opening X; Pi exposes only those MCP reads as `wmb_list_x_post_metric_snapshots` / `wmb_get_x_post_trend`. Focused business/MCP/Pi/IPC/workspace coverage passed 4/4; the exact fixture returned 320 views/hour and +80 velocity change, while every insufficient case returned its frozen reason and UK retained the same generic X read surface. The full run completed 146/149: two unchanged baselines remained (content-list Windows `EBUSY`, source-logo `38 !== 29`) plus the old exact MCP tool-list expectation; that expectation was updated for the two contractual read-only tools and its focused workspace test then passed, so the unchanged full suite was not repeated. Typecheck still reports only the three known diagnostics in `intelligence-wire.ts` and `x-list-execution.ts`; `git diff --check` passed. All WMB-2202 source files remain below 500 lines. | Codex |
| WMB-2203 | M-2200 | CAP-011, CAP-017, CAP-018, CAP-022 | done | WMB-2202 | Add explicit-start +15m/+60m/+180m observation jobs with idempotency, serial browser use, stop/restart/expiry and inactive-root zero-write behavior | 2026-08-03: Explicit UI/MCP/Pi start now performs one serial initial read for each selected enabled binding, freezes workspace/profile/account/binding identity, and creates exactly three root-local `x_list_observation` jobs at +15m/+60m/+180m in the existing jobs table. A single active-root scheduler runs under `WorkspaceRuntimeGate`, opens one List at a time, revalidates after browser read, disposes pooled X sessions on switch/quit, and never invokes Pi. Same workspace/request/List input replays without reading or adding jobs; changed input is rejected. Restart restores running jobs, keeps only the latest overdue window per List, expires older/24-hour windows, and stop invalidates running/pending jobs so delayed browser results fail the job-state check before the atomic source/snapshot/cache write. Focused business/metric/profile/Pi tests passed 6/6, including exact due times, one recovered window, 24-hour expiry and an in-flight stop with unchanged one source/one snapshot. Full `npm test` passed 149/151; only unchanged content-list Windows `EBUSY` and source-logo `38 !== 29` baselines remain. Typecheck reports only the three unchanged diagnostics in `intelligence-wire.ts` and historical member-state code in `x-list-execution.ts`; `git diff --check` passed. New files are below 500 lines, `index.ts`/`mcp.ts` are exactly 500, and the touched historical `x-list-session.ts` shrank from 654 to 653 lines; lightweight gate therefore reports the same 12 historical over-limit files. | Codex |
| WMB-2204 | M-2200 | CAP-002, CAP-003, CAP-004, CAP-015, CAP-021, CAP-022 | done | WMB-2203 | Connect trend evidence to existing event aggregation, Today/Discover and UI/MCP plan-to-content lineage while keeping X social actions and parallel stores absent | 2026-08-03: Trend evidence is derived at read time from existing append-only snapshots and real plan `sourceIds`; no parallel source/event/opportunity store or derived score was added. Existing `topics`/`topic_source_links` still merge multiple sources under one canonical event. Workbench/MCP now return per-opportunity status, source/snapshot IDs, exact velocity intervals and capture evidence; Today shows the auditable views/hour value, and the Pi synthesis prompt receives the same bounded facts with an explicit no-inference rule. Discover's existing channel page can start/stop one selected root-local X List observation, shows the fixed 15/60/180 windows and latest exact trend evidence. A daily X scan reuses its one committed initial read to schedule the same three jobs instead of opening X twice. The shared project creator now derives topic and all sources from `plan_item_id`, so UI and external MCP preserve identical plan/topic/source lineage even when MCP omits `source_ids`. Focused event/trend/daily/UI/MCP/profile coverage passed 7/7: two sources produced one event topic, 320 views/hour and +80 velocity change with all three snapshot IDs in Today/Pi, one daily collect produced exactly three jobs, and MCP content readback inherited the topic and source. Full `npm test` passed 150/152; only unchanged content-list Windows `EBUSY` and source-logo `38 !== 29` baselines remain. Typecheck retains only the same three historical diagnostics; `git diff --check` passed. No dependency/migration/social action was added; all changed source files are below 500 except the historical Today file, which remains exactly 1359 lines with zero net growth. | Codex |
| WMB-2205 | M-2200 | CAP-011, CAP-017, CAP-018, CAP-022 | done | WMB-2204 | Run focused packaged EVAL-024–025 with three real X metric reads, exact trend math, responsive opportunity evidence, account/root stale zero-write and cold-switch lifecycle | 2026-08-03: EVAL-024–025 passed against the current Windows package and the authorized dedicated X profile. Three real reads of `@KimbomArtist` List `AI前沿` at `00:54:55Z`, `01:05:25Z` and `01:15:50Z` produced eight common canonical posts with three append-only snapshots each. The retained sample kept one source ID, three distinct snapshot IDs, exact DOM raw labels/page evidence and visible views `3,078,765 → 3,078,765 → 3,078,765`; deterministic readback truthfully returned `0 views/hour` and velocity change `0` rather than inventing heat. The isolated real root was removed after receipt creation. Current package hashes are EXE `e0f13b4d…06a48d3` and app.asar `989e7511…1de2098`. Packaged fixture readback independently displayed exact `320 views/hour`, acceleration `80` and three snapshot IDs in Today/Discover. At 1100×700, 1366×768 and 1920×900 both views had zero horizontal overflow; Today create and Discover observation controls passed real center-point hit tests, and six screenshots were visually inspected. Packaged MCP content creation inherited the exact plan item, event topic and two sources. Cold AI→UK switching closed the old MCP, left AI DB/WAL/SHM byte-identical while UK was active, then recovered the overdue AI job as accurate `needs_user` with the original three snapshots unchanged. Focused account-change, root-change and in-flight-stop experiments all retained exactly one source/one snapshot and the root-stale path opened no browser. Final full `npm test` passed 151/153; only unchanged content-list Windows `EBUSY` and source-logo `38 !== 29` baselines remain. Typecheck retains only the same three historical diagnostics; `git diff --check` passed. Lightweight gate still stops on the same 12 historical over-limit files, with Today fixed at its 1359-line baseline. Reproducible receipts/scripts/screenshots are `.ai/wmb-2205-*`. | Codex |
| WMB-2000 | M-2000 | CAP-014 | done | WMB-1018 | Align the built-in Pi/model contract, remove Anthropic runtime protocol support, reject unsupported protocol values at the shared configuration boundary, and prove existing OpenAI-compatible configuration remains unchanged without fallback | 2026-08-02: contracts now state that WMB embeds and supervises Pi RPC but no model weights/inference; external Agents are vendor-neutral, and Pi exposes only OpenAI Responses/Chat Completions. Runtime/UI/preload types removed Anthropic; shared `requirePiApiType` rejects unsupported or missing values in read/save/list/resolve with no inference or fallback. Luna-max implemented the bounded code slice; Sol reviewed and accepted it. Focused `node --test tests/pi-config.test.mjs` passed 3/3, including four unsupported-protocol boundaries. Windows `npm run build` passed; packaged `app.asar` checked 4 code/text files with zero `anthropic-messages`/`Anthropic Messages` hits and two OpenAI-protocol hits. Formal data-root readback stayed revision 13: active OpenCode Go `deepseek-v4-flash`/`openai-completions`, inactive local `gpt-5.6-terra`/`openai-responses`, no Anthropic profile and no data mutation. `npm run typecheck` was run and reproduced only unchanged baseline failures in `intelligence-wire.ts:76` and `x-list-execution.ts:145`; neither file changed. Historical receipts and Anthropic news-source content were intentionally preserved. | Codex |
| WMB-1600 | M-1600 | CAP-017 | done | WMB-1500 | Approve X List product contract and add durable binding/operation schema | 2026-07-31: user expanded scope to complete X List management; PRD REQ-016/AC-012, SPEC CAP-017, technical design, plan and single task chain now agree. Migration 28 adds bindings, operations and per-member outcomes without touching existing sources. `node --test tests/migrations.test.mjs` 1/1 and lightweight harness both passed. | Codex |
| WMB-1601 | M-1600 | CAP-017 | done | WMB-1600 | Fixed visible X List executor for list/read/member/owner operations | 2026-07-31: Pyaireader `edge_cdp_profile` live smoke passed. The executor kept one of two identical restored X home tabs, then real-read `@KimbomArtist` at `https://x.com/i/lists` with a stable page fingerprint and 0 visible Lists; differing X tabs still require user choice. `node --test tests/x-list-session.test.mjs`, `npm run typecheck`, and `scripts/check.ps1` passed. No external List write was attempted; owned-List mutation remains a final real-platform check. | Codex |
| WMB-1602 | M-1600 | CAP-017 | done | WMB-1601 | Shared List bindings, operation commands, IPC and MCP/Pi prepare surface | 2026-07-31: persisted `prepared → awaiting_confirmation → running` operation chain freezes semantic account/List/member snapshots, invalidates stale confirmation, requires typed List name for delete, records pre-click intent, and preserves `unknown` after an uncertain click. Bind/unbind reuses an existing source feed without deleting data; timeline collection is capped at 50. MCP/Pi expose only list/read/prepare. `node --test tests/migrations.test.mjs tests/x-list-session.test.mjs tests/x-list-operations.test.mjs` 4/4, `npm run typecheck`, `scripts/check.ps1`, and Windows `npm run build` passed; packaged `app.asar` readback contains X List IPC/MCP/Pi preparation surfaces. | Codex |
| WMB-1603 | M-1600 | CAP-017 | done | WMB-1602 | Discover X Lists workspace and bound-list source collection | 2026-07-31: Discover X Lists UI live Electron readback on Pyaireader CDP showed @KimbomArtist with 7 lists, create/prepare controls present, no forbidden social actions, no overflow (.ai/wmb-1603-readback.mjs, .ai/wmb-1603-x-lists-1100.png). Real index root-cause fixed to /{handle}/lists + ListsManagementPageTimeline; MCP/Pi gained x_lists.read_* and wmb_read_x_list_*. Focused tests 4/4; typecheck exit 0. | Codex |
| WMB-1604 | M-1600 | CAP-017 | done | WMB-1603 | Real X List acceptance and restart/readback proof | 2026-07-31 EVAL-016 minimal live acceptance on owned list 赚钱信息差博主 (2082167416352579643): prepare/arm/confirm member batch add+remove @OpenAI both succeeded with platform readback; bind + collect wrote 5 traceable source_items. Receipt .ai/wmb-1604-acceptance.json. Member controls path updated to Edit List -> Manage members -> Suggested search. Focused X-list tests 4/4, typecheck exit 0, UI readback exit 0. No unsafe external-list social actions. | Codex |
| WMB-1700 | M-1700 | CAP-002, CAP-003, CAP-014, CAP-017 | done | WMB-1605, WMB-1115 | Official-release wire foundation: expand source-index (DeepSeek multi-channel + ByteDance/Seedance), harden Skill A-class checklist SOP, require enabled X List bindings (AI前沿) in daily intelligence queue per INTELLIGENCE_WIRE_PLAN.md | 2026-07-31: INTELLIGENCE_WIRE_PLAN.md; source-index 38 sources validate OK; DeepSeek multi-channel + Seed/Jimeng/Dreamina; Skill/SOP/x collector A-class checklist; .ai/ai-frontier-list-members-suggested.json (list 2082851520417255750) | Codex |
| WMB-1701 | M-1700 | CAP-002, CAP-003, CAP-014, CAP-017 | done | WMB-1700 | Daily intelligence runner W0/W1 deterministic wire: enabled X List timeline collect + primary release source checklist before Pi B/C routes; per-source failure isolation and checkpoint fields | 2026-07-31: src/main/intelligence-wire.ts + agent-runner W0→W1→W2; AI_FRONTIER_LIST_ID preferred; cache-only fallback; node --test tests/intelligence-wire.test.mjs 4/4; npm run typecheck exit 0 | Codex |
| WMB-1702 | M-1700 | CAP-002, CAP-003, CAP-017 | done | WMB-1701 | Acceptance nails + List member governance: ensure DSV4-Flash and Seedance 2.5 enter source_items; suggest/add required official handles into AI前沿 via existing X List operation flow | 2026-07-31: nails in DB dsv4-flash a7494702… + seedance d15f6d80…; W0 AI前沿 collected dreamina Seedance post 119026c7…; W1 22 official heartbeats; members diff + prepared operation af4302f7… (8 handles) awaiting user arm/confirm. Receipt .ai/wmb-1702-acceptance.json | Codex |
| WMB-1605 | M-1600 | CAP-017 | done | WMB-1604 | X List timeline browse cache (L1 preview) with hard caps, LRU eviction, failure-safe writes; L2 collected assets unchanged | Design: `docs/x-list-timeline-cache.md`. Migration 30 `x_list_timeline_cache`; helpers enforce per-account/global caps; read path L1→L2→L3; no auto multi-list refresh; settings clear L1 only. Verified: `tsc --noEmit`; `node --test tests/x-list-timeline-cache.test.mjs tests/migrations.test.mjs tests/settings.test.mjs` 5/5. | Codex |
| WMB-0001 | M-000 | CAP-001, CAP-002, CAP-003, CAP-004, CAP-005, CAP-006, CAP-007, CAP-008, CAP-009, CAP-010, CAP-011, CAP-012, CAP-013 | done | — | Harness entrypoint, workflow, verification, eval templates | `scripts/check.ps1` passes in commit containing harness | Codex |
| WMB-0002 | M-000 | CAP-001, CAP-002, CAP-003, CAP-004, CAP-005, CAP-006, CAP-007, CAP-008, CAP-009, CAP-010, CAP-011, CAP-012, CAP-013 | done | WMB-0001 | `SPEC.md`, `PLAN.md`, task traceability | Harness reference check passes | Codex |
| WMB-0003 | M-000 | Harness | done | WMB-0002 | Index one project rule for maintaining the installation-wide Pi operation Skill whenever WMB behavior, tools, workflows, confirmation boundaries, states or packaging change | 2026-08-03: Baseline confirmed the active UK Pi root contains only its lane Skill while generic WMB operating knowledge is scattered across the appended system prompt and tool descriptions. New `docs/pi-operation-skill-maintenance.md` defines the canonical future source, four-layer ownership, exact update/no-update triggers, task evidence wording, update sequence, tool-reference checks, fresh/existing-root package acceptance and rule-routing boundaries. `AGENTS.md`, `docs/ai-harness.md`, `docs/development-workflow.md` and `docs/verification.md` now index the same policy; `scripts/check.ps1` requires the file and all four references. The focused policy check returned `PI_OPERATOR_POLICY_INDEX=PASS`, `TASK_DOING_COUNT=1` and `M2400_CHAIN=PASS`; PowerShell parsing returned `CHECK_PS1_PARSE_OK`. The lightweight Harness passed its new required-file/index/placeholder stages, then stopped only on the unchanged 12 historical over-500-line files. WMB-2400–2402 serially track the still-missing canonical Skill, installer/refresh and packaged fresh/existing-root acceptance; none was implemented in this Harness task. Pi operator Skill impact: no change — this task governs the missing canonical asset but intentionally does not create or package it. `git diff --check` passed. | Codex |
| WMB-0101 | M-100 | CAP-001 | done | WMB-0002 | `package.json`, `package-lock.json`, Electron Forge/Vite scaffold, sandboxed preload and renderer | 2026-07-27: `powershell -ExecutionPolicy Bypass -File scripts/check.ps1` passed; TypeScript typecheck, Node scaffold test, and Electron Forge Windows x64 package passed | Codex |
| WMB-0102 | M-100 | CAP-001 | done | WMB-0101 | `src/main/data-root.ts`, data-root IPC/preload/UI selection flow | 2026-07-27: temporary-root regression creates required layout, validates/reopens it, and rejects an incomplete root; `scripts/check.ps1` passed including Windows x64 package | Codex |
| WMB-0103 | M-100 | CAP-001 | done | WMB-0102 | `src/main/db/migrations.ts`, `src/main/db/object.ts`, data-root migration hook | 2026-07-27: migration/reopen regression applies schema once and retains it after reopen; `scripts/check.ps1` passed including Windows x64 package | Codex |
| WMB-0104 | M-100 | CAP-013 | done | WMB-0102, WMB-0103 | `src/main/settings.ts`, fixed settings IPC/preload surface and Settings UI | 2026-07-27: temporary-root readback proves resolved paths, exact log bytes, migration count and current health; `scripts/check.ps1` passed including Windows x64 package | Codex |
| WMB-0105 | M-100 | CAP-013 | done | WMB-0103 | `src/main/result.ts`, `src/main/operations.ts`, migration 2 | 2026-07-27: command-envelope and operation-log black-box regression passed; `scripts/check.ps1` passed including Windows x64 package | Codex |
| WMB-0201 | M-200 | CAP-002 | done | WMB-0103 | migration 3 and `src/main/sources.ts` source-feed/item commands | 2026-07-27: black-box regression proves canonical URL and content-fingerprint dedupe update existing identities; `scripts/check.ps1` passed including Windows x64 package | Codex |
| WMB-0202 | M-200 | CAP-003 | done | WMB-0201 | migration 4 and `src/main/planning.ts` topic/current-plan commands | 2026-07-27: black-box regression proves cited stored source IDs and exactly one current plan per date; `scripts/check.ps1` passed including Windows x64 package | Codex |
| WMB-0203 | M-200 | CAP-002, CAP-003 | done | WMB-0202 | `src/main/workbench.ts`, fixed Today IPC/preload API and renderer view | 2026-07-27: Today readback regression proves stored source and current plan item are returned together; `scripts/check.ps1` passed including Windows x64 package | Codex |
| WMB-0301 | M-300 | CAP-004 | done | WMB-0103 | migration 5 and `src/main/content.ts` project/version commands | 2026-07-27: regression proves immutable core version numbering and stale platform-version write conflict; `scripts/check.ps1` passed including Windows x64 package | Codex |
| WMB-0302 | M-300 | CAP-004 | done | WMB-0301 | migration 6 and `src/main/assets.ts` atomic importer | 2026-07-27: black-box regression proves hash reuse, one persisted asset record and no remaining temporary file; `scripts/check.ps1` passed including Windows x64 package | Codex |
| WMB-0303 | M-300 | CAP-004 | done | WMB-0302 | Studio view, version history, platform tabs, conflict reload | 2026-07-27: packaged Windows app rendered a temporary-root project in Studio; CDP screenshot readback shows `Studio 验收项目`, core v1 and X revision r1 at `J:\Users\yangda01\Temp\wmb-studio-ui\studio-renderer-final.png`; `scripts/check.ps1` passed including Windows x64 package | Codex |
| WMB-0304 | M-300 | CAP-005 | done | WMB-0202, WMB-0301 | Loopback MCP server and required read tools | 2026-07-27: loopback Streamable HTTP endpoint returned MCP 200 initialize, tool listing and `context.get_workbench` readback; Settings exposes runtime URL; `scripts/check.ps1` passed (11 tests and Windows x64 package) | Codex |
| WMB-0305 | M-300 | CAP-005 | done | WMB-0304 | MCP write tools, request idempotency, revision conflicts | 2026-07-27: MCP source and plan writes replay one persisted result per request ID; project/core/platform write sequence returns `REVISION_CONFLICT` for stale revision; Today and Studio poll every five seconds; `scripts/check.ps1` passed (11 tests and Windows x64 package) | Codex |
| WMB-0401 | M-400 | CAP-006 | done | WMB-0104 | Installed Chrome discovery/selection, dedicated profile, CDP lifecycle | 2026-07-27: Chrome 150 launched with temporary data root `browser-profile`, dynamic loopback CDP endpoint returned `/json/version`, and PID/profile/CDP were read back; `scripts/check.ps1` passed | Codex |
| WMB-0402 | M-400 | CAP-006 | done | WMB-0401 | Account identity persistence and mismatch blocking | 2026-07-27: account regression stores the selected identity and returns `ACCOUNT_MISMATCH` for a different browser identity; focused test passed | Codex |
| WMB-0204 | M-200 | CAP-002, CAP-003, CAP-004 | done | WMB-0203, WMB-0303 | Today editorial workbench, complete plan/source read model, plan-to-project entry | 2026-07-27: rich Today regression and idempotent plan-to-project v1 creation pass; canonical URL false-dedupe regression fixed; packaged 1672×940 DOM/screenshot receipt at `J:\Users\yangda01\Temp\wmb-today-final.png` shows hero judgment plus four distinct cited sources, and UI click opens Studio with core v1; full `scripts/check.ps1` passes 13 tests, typecheck and Windows x64 package | Codex |
| WMB-0205 | M-200 | CAP-002, CAP-003 | done | WMB-0204 | Responsive Today shell with fixed chrome, adaptive content and source drawer | 2026-07-27: packaged DOM receipts show 1672px = 224/1030/418, 1366px = 208/817/342, 1100px = 76/1024 with 420px source drawer; all three report zero horizontal overflow; compact screenshot `J:\Users\yangda01\Temp\wmb-responsive-1100.png`; full `scripts/check.ps1` passes 13 tests, typecheck and Windows x64 package | Codex |
| WMB-0206 | M-200 | CAP-002, CAP-003 | done | WMB-0205 | UI-matched overlay scrollbars for application scroll regions | 2026-07-27: packaged DOM readback on a real overflowing source list reports idle thumb `rgba(0, 0, 0, 0)`, hover thumb `rgb(105, 115, 130)`, `411px` client height vs `552px` scroll height and `overflow:auto`; full `scripts/check.ps1` passes 13 tests, typecheck and Windows x64 package | Codex |
| WMB-0207 | M-200 | CAP-001, CAP-002 | done | WMB-0206 | Frameless Windows shell and Dark/Daylight Violet theme system | 2026-07-27: packaged app has custom draggable title bar and five no-drag controls; IPC maximize returns `true` and restore returns `false`; Dark Violet readback `theme=dark`, `#090c11`; Daylight Violet readback `theme=light`, persisted `light`, `#f5f3fb`; screenshots `J:\Users\yangda01\Temp\wmb-dark-violet.png` and `J:\Users\yangda01\Temp\wmb-daylight-violet.png`; full `scripts/check.ps1` passes 13 tests, typecheck and Windows x64 package | Codex |
| WMB-0208 | M-200 | CAP-002 | done | WMB-0207 | User-facing empty copy, source empty spacing and functional Library navigation | 2026-07-27: packaged formal-root reversal shows zero `外部 AI Agent` UI strings; source empty text has 16/18px padding and measured 17/19px border gaps instead of 1px; clicking Library changes heading and active nav to `资料库` and renders its empty state; screenshot `J:\Users\yangda01\Temp\wmb-library-fixed.png`; full `scripts/check.ps1` passes 13 tests, typecheck and Windows x64 package | Codex |
| WMB-0209 | M-200 | CAP-002 | done | WMB-0208 | Remove decorative empty-state glyph tiles across product pages | 2026-07-27: packaged formal-root DOM shows `.empty-state > div` count `0` on Today, Library, Publish and Results; screenshot `J:\Users\yangda01\Temp\wmb-empty-no-glyph.png`; full `scripts/check.ps1` passes 13 tests, typecheck and Windows x64 package | Codex |
| WMB-0210 | M-200 | CAP-002, CAP-003, CAP-004, CAP-007, CAP-011, CAP-012 | done | WMB-0209 | Consolidate the four approved screen references into the product UI design system | 2026-07-27: `DESIGN.md` defines the Private Editor Desk north star, Dark/Daylight Violet tokens, responsive geometry and Today/Library/Studio/Publish/Results patterns; `.impeccable/design.json` parses as schema v2; four stable references are stored under `docs/design/references`; full `scripts/check.ps1` passes 13 tests, typecheck and Windows x64 package | Codex |
| WMB-0211 | M-200 | CAP-002, CAP-003, CAP-004, CAP-007, CAP-011, CAP-012 | done | WMB-0210 | Rebuild every product page and shared shell against the approved UI design system | 2026-07-27: packaged app renders the approved private-editor shell across Today, Library, Studio, Publish, Results, Diagnostics and Settings; formal logo and Dark/Daylight Violet themes are shared; 1672/1366/1100 DOM receipts show nav widths 224/208/76 and zero body/workspace horizontal overflow on all seven pages; five business surfaces contain zero MCP/IPC/CDP/revision/external-Agent copy; screenshots `J:\Users\yangda01\Temp\wmb-redesign-{library,studio,publish,results}.png`; full `scripts/check.ps1` passes 13 tests, typecheck and Windows x64 package | Codex |
| WMB-0212 | M-200 | CAP-004, CAP-005 | done | WMB-0211 | Repair assets readback, Studio latest-version selection and remove the inactive global search control | 2026-07-27: falsifiable repro produced `no such column: original_name` and showed Studio order `2,1` while UI selected `1`; shared `listAssets` now returns migration-backed columns, Studio selects revision index 0, inactive search control removed; focused regressions and full `scripts/check.ps1` pass 13 tests, typecheck and Windows x64 package | Codex |
| WMB-0213 | M-200 | CAP-002, CAP-003, CAP-005 | done | WMB-0212 | Versioned `wemedia-intelligence-engine` main/child Skill package; full source MCP write/search/readback and explainable opportunity fields | 2026-07-28: 29-source index and scan-output validators pass; Skill validator passes; independent forward review defects were repaired; temporary real MCP flow wrote and searched complete source analysis then saved/read back materials and score reasons; `scripts/check.ps1` passes 19 tests, typecheck and Windows x64 package | Codex |
| WMB-0214 | M-200 | CAP-002, CAP-003, CAP-005 | done | WMB-0213 | Execute one current real-source scan, verify and cluster evidence, save 1–3 opportunities to the configured WMB data root, select one and save a first core draft | 2026-07-28: current OpenAI and GitHub sources were saved as three real source records; plan `f2ee4fcd-3de8-41b8-b6fa-ac25d225a652` read back three cited opportunities; selected project `b6a9466c-b99d-4244-931c-69005a619585` inherited source `55d5ab6c-aa37-406d-a873-0741a68b17c6` and read back core v1 with the complete 649-character draft. Studio's shared query was repaired to return and render the body; focused regression and full `scripts/check.ps1` passed 19 tests, typecheck and Windows x64 package; packaged app restarted on the configured formal root. | Codex |
| WMB-0215 | M-200 | CAP-002, CAP-003, CAP-005 | done | WMB-0213 | Reduce the intelligence Skill to five business modules, demand-driven source expansion and minimum useful checks | 2026-07-28: child Skills reduced 10→5; runtime Skill package reduced 479→295 lines; nine-dimension scoring and duplicate X registry removed; source and output smoke validators pass; `scripts/check.ps1` passes 19 tests, typecheck and Windows x64 package | Codex |
| WMB-0216 | M-200 | CAP-002, CAP-003 | done | WMB-0215 | Make aggressive-but-redeemable editorial framing the intelligence Skill's default content stance | 2026-07-28: main Skill and opportunity editor require a strong point of view, headline hook, opening hook and promise boundary; Skill and output smoke validators pass | Codex |
| WMB-0217 | M-200 | CAP-002 | done | WMB-0213, WMB-0211 | Store one local identity asset for every registered source and render registered source Logos instead of generated initials | 2026-07-28: all 29 registered sources reference 24 packaged local assets, including five distinct professional-account avatars; source-index validation and focused matching tests passed. Packaged formal-root UI readback showed OpenAI's local image loaded at 48×48 while two unregistered GitHub sources used the neutral fallback; screenshot `J:\Users\yangda01\Temp\wmb-source-logos.png`. Full `scripts/check.ps1` passed 21 tests, typecheck and Windows x64 package. | Codex |
| WMB-0403 | M-400 | CAP-007 | done | WMB-0302, WMB-0402 | Publication, attempt, confirmation, reconciliation schema and state machine | 2026-07-27: migration 9 creates publications, attempts, confirmations, reconciliation evidence and event timeline; state regression rejects invalid/terminal transitions, requires stable identity for published, closes attempts on uncertain readback and preserves ambiguous reconciliation; full `scripts/check.ps1` passes 14 tests, typecheck and Windows x64 package | Codex |
| WMB-0404 | M-400 | CAP-007 | done | WMB-0403 | Prepare/editor readback and confirmation snapshot | 2026-07-27: prepare persists exact editor title/body/evidence and ordered asset SHA-256 binding; confirmation revalidates platform version, account and assets, rejects editor mismatch and stale revision, then atomically consumes one confirmation into one attempt; focused EVAL-009 regression and full `scripts/check.ps1` pass 15 tests, typecheck and Windows x64 package | Codex |
| WMB-0405 | M-400 | CAP-007, CAP-013 | done | WMB-0404 | Publish UI, UI-only confirm, timeline, takeover, reconcile | 2026-07-27: renderer reads real publication queue/payload/assets/events, final confirmation exists only on the UI bridge, needs-user takeover opens the configured browser, unknown can be atomically reconciled as not published without creating another attempt; controlled EVAL-010 regression and full `scripts/check.ps1` pass 16 tests, typecheck and Windows x64 package | Codex |
| WMB-0501 | M-500 | CAP-008 | done | WMB-0405 | X account identify and pure-text publish/readback | 2026-07-28: packaged WMB used only pyaireader CDP `9334`, identified `@KimbomArtist`, exactly read back the editor body, consumed a fresh WMB UI confirmation, and persisted publication `05b19f1f-a481-448d-ab28-3ba4ca7225d1` as `published`; X and DB readback both prove status ID `2081798936373641318` at `https://x.com/KimbomArtist/status/2081798936373641318` with the exact prepared body; full `scripts/check.ps1` passed 16 tests, typecheck and Windows x64 package | Codex |
| WMB-0502 | M-500 | CAP-008 | done | WMB-0501 | X one-image publish/readback | 2026-07-28: asset `b73afc1a-d2c3-4cb8-9833-73df9273c16d` SHA-256 `720cfb43515b61cae84cf1a29cbf3cdfe698db5a76a34fc48e2214a9050441e5` was bound to the editor readback and fresh WMB UI confirmation; publication `79a3ff1d-8cad-4998-99a1-1aa9b0d60821` persisted `published` with X photo readback, status ID `2081803173061181535` and URL `https://x.com/KimbomArtist/status/2081803173061181535`; full gate passed before live confirmation | Codex |
| WMB-0503 | M-500 | CAP-008 | done | WMB-0501 | X one-video publish/readback and processing wait | 2026-07-28: generated/imported a 3-second 720×720 MP4 with SHA-256 `f599eea8e4b8334c045aade8787a34f83ae05948610ee1b2e0b5828510f1573f`, waited for X processing readiness, consumed a fresh WMB UI confirmation, then uniquely reconciled account/body/time and `videoPlayer` evidence after correcting the lazy-player selector; publication `501750ce-2271-422a-99b7-90f33a16d18c` is `published` at `https://x.com/KimbomArtist/status/2081810967545995311`, status ID `2081810967545995311`; full gate passed 16 tests, typecheck and Windows x64 package | Codex |
| WMB-0504 | M-500 | CAP-008, CAP-011 | done | WMB-0501 | X creator-page metric mapping | 2026-07-28: packaged adapter captured `https://x.com/KimbomArtist/status/2081798936373641318` at `2026-07-27T18:43:16.576Z`; normalized fields were views `4`, likes `0`, replies `0`, reposts `0`, all with status `value` and exact raw labels; bookmark `0` remained optional raw data; parser regression and full gate passed 17 tests, typecheck and Windows x64 package | Codex |
| WMB-0601 | M-600 | CAP-009 | done | WMB-0405 | Xiaohongshu image-note platform version and mandatory MCP/manual handoff | 2026-07-28: official `xpzouying/xiaohongshu-mcp` v2.1.1 is installed, registered, logged in as `羊毛团Ymtuan`, and enforced by the validated `wemedia-buddy` skill. The legacy Xiaohongshu Playwright adapter and every WMB browser/IPC/UI preparation path were deleted; full gate passed 19 tests, typecheck and Windows package. Under the 2026-07-28 manual-publication scope, a real note receipt is not required. | Codex |
| WMB-0602 | M-600 | CAP-009 | done | WMB-0601 | Xiaohongshu video platform-version handoff | 2026-07-28: focused regression persists and reads back the exact Xiaohongshu title, body, `video` format and ordered video asset binding; real publication is outside the completion gate. Full `scripts/check.ps1` passed 19 tests, typecheck and Windows x64 package. | Codex |
| WMB-0603 | M-600 | CAP-009, CAP-011 | done | WMB-0601 | Optional Xiaohongshu read-only discovery/metrics route | 2026-07-28 scope decision: per-platform creator-page metric automation is not a completion requirement; when needed, Xiaohongshu AI reads must use the installed read-only MCP tools. | Codex |
| WMB-0701 | M-700 | CAP-010 | done | WMB-0405 | WeChat account identify, article editor prepare/readback | 2026-07-28: packaged WMB identified `鸽子杨的雾灯会`, prepared MCP-created platform version `0de4b1f3-415f-457f-b2f5-086af634ce33`, exactly read back title/body from the real article editor, and persisted publication `6c68e52a-1da2-422b-ab1b-97743ed8a55a` at revision 3 awaiting confirmation; full gate passed 17 tests, typecheck and Windows x64 package | Codex |
| WMB-0702 | M-700 | CAP-010 | done | WMB-0701 | WeChat manual handoff and optional accessible article readback | 2026-07-28: exact article payload is prepared for manual publication; optional `https://mp.weixin.qq.com/s...` URL validation requires an exact title match before recording. URL validation regression and full gate passed 19 tests, typecheck and Windows package. A real article URL is not required under the manual-publication scope. | Codex |
| WMB-0703 | M-700 | CAP-010, CAP-011 | done | WMB-0702 | Optional WeChat URL-based metrics route | 2026-07-28 scope decision: backend metric automation is not a completion requirement; metrics may run only after the user supplies an article URL. | Codex |
| WMB-0801 | M-800 | CAP-011 | done | WMB-0504 | Metric jobs for recorded URLs, four capture windows, idempotency, overdue recovery | 2026-07-28: migration 12 adds `jobs`, `publication_metric_snapshots`, `account_metric_snapshots`. Published transitions schedule 1h/6h/24h/72h jobs with unique `publication_id+scheduled_for` and dedupe keys; overdue capture stores real `captured_at` and never overwrites an existing snapshot; startup recovers `running` metric jobs to `pending`. Focused `tests/metrics-jobs.test.mjs` passed 3/3. IPC: `metrics:schedule`, `metrics:list-jobs`, `metrics:list-snapshots`, `metrics:process-due`. `tsc --noEmit` passed. | Codex |
| WMB-0802 | M-800 | CAP-011 | done | WMB-0402, WMB-0801 | Account metric snapshots | 2026-07-28: `collectXAccountMetrics` reads X profile follower labels; `saveAccountMetricSnapshot` / `listAccountMetricSnapshots` persist and read back account-level fields. Live CDP capture on `@KimbomArtist` stored snapshot `ba09e74b-3fda-49a6-98c2-681169756e5f` with followers `819` (`819 关注者`) from `https://x.com/KimbomArtist`. Receipt `J:\Users\yangda01\Temp\wmb-account-metric-live.json`. Focused `tests/account-metrics.test.mjs` passed 3/3; `tsc --noEmit` passed. IPC: `metrics:collect-account-x`, `metrics:list-account-snapshots`. | Codex |
| WMB-0803 | M-800 | CAP-011 | done | WMB-0801 | Results snapshot/raw/status views | 2026-07-28: Results lists published content and shows scheduled/captured times, source URL, normalized table, raw table, field status, plus account follower snapshot. Live X status `501750ce-2271-422a-99b7-90f33a16d18c` snapshot views=4 likes/replies/reposts=0; account followers=819. Packaged UI receipt `J:\Users\yangda01\Temp\wmb-results-metrics\receipt.json`, screenshot `results.png`. Manual collect-x now persists snapshot. `tsc --noEmit` passed; Windows package rebuilt. | Codex |
| WMB-0804 | M-800 | CAP-012 | done | WMB-0202, WMB-0803 | Reviews, method findings, backlinks, Results UI | 2026-07-28: migration 13 adds `reviews` + `method_findings`. Final review requires real metric snapshot IDs and concrete Keep/Stop/Change; method findings retain review link; later plan items backlink both. Live final review `bb20dd21-fb0f-4773-83c1-123285b8a958` on publication `501750ce-…` snapshot `446633ab-…`; finding `86dd6a42-…`; plan `27530b74-…` item backlink. Packaged Results shows status `final`, Keep/Stop/Change values, finding title, and backlink “用更强结论重做视频开头”. Receipts `J:\Users\yangda01\Temp\wmb-reviews-live.json`, `J:\Users\yangda01\Temp\wmb-reviews-ui\receipt.json`. Focused `tests/reviews.test.mjs` 2/2; `tsc --noEmit`; Windows package rebuilt. MCP: `reviews.get` / `reviews.save`. | Codex |
| WMB-0901 | M-900 | CAP-001 | done | WMB-0104 | Whole data-root move/reopen | 2026-07-28: EVAL-013 regression created a migrated root with a real source row plus receipts under assets/browser-profile/logs/exports, closed SQLite, moved the complete root, reopened only the new path, and read every value back while the old path remained absent; focused test passed 2/2 and full `scripts/check.ps1` passed 18 tests, typecheck and Windows x64 package | Codex |
| WMB-0902 | M-900 | CAP-007 | done | WMB-0405 | Crash/restart publication safety | 2026-07-28: falsifiable restart regression showed the recovery function was absent; `recoverInterruptedPublications` now atomically converts every interrupted `publishing` record and latest attempt to `unknown`, persists `PUBLICATION_UNKNOWN` plus a timeline event, and is idempotent on later starts. `migrate(dataRoot)` invokes it before normal runtime use; focused regression and full `scripts/check.ps1` passed 18 tests, typecheck and Windows x64 package | Codex |
| WMB-0903 | M-900 | CAP-001, CAP-002, CAP-003, CAP-004, CAP-005, CAP-006, CAP-007, CAP-008, CAP-009, CAP-010, CAP-011, CAP-012, CAP-013 | done | WMB-0804, WMB-0901, WMB-0902 | Full harness checks and packaged Windows build | 2026-07-28: schema-v14 expectations pass in `tests/migrations.test.mjs` and `tests/settings.test.mjs`; typecheck passed; **35/35** tests passed; lightweight harness passed; Windows x64 package rebuilt successfully at `out/WeMediaBuddy-win32-x64/WeMediaBuddy.exe` with resources `.pi-runtime`, `extensions`, `playwright-core`, `skills`. No Electron Forge makers/installer configured in `forge.config.ts` — portable package is the release artifact. | Codex |
| WMB-0904 | M-900 | CAP-008, CAP-009, CAP-010, CAP-011, CAP-012 | done | WMB-0903 | Six-format payload acceptance and full feedback chain | 2026-07-28: EVAL-001–EVAL-013 all **pass** under manual-publication scope. Six payloads: X text `05b19f1f-…` / image `79a3ff1d-…` / video `501750ce-…`; XHS image `b53c2300-…` + video `909d20ef-…` exact title/body/asset handoff; WeChat article `6c68e52a-…` exact title/body. Feedback chain: final review `bb20dd21-…` Keep/Stop/Change + finding `86dd6a42-…` backlinked by plan `27530b74-…`. Stale confirmation, unknown recovery, restart/data-root covered by focused regressions (exit 0). Matrix `J:\Users\yangda01\Temp\wmb-0904\eval-matrix.json` + `EVAL-MATRIX.md`. Real XHS/WeChat final platform posts remain outside completion gate. | Codex |
| WMB-1001 | M-1000 | CAP-005, CAP-013 | done | WMB-0305 | Real packaged Pi RPC spike using WMB-configured OpenAI-compatible/CPA API, a thin WMB MCP proxy, fixed prompt, streaming events, abort and isolated-root source write/readback | 2026-07-28: Windows package embeds an independent Pi 0.82.1 production runtime (131 packages); packaged `WeMediaBuddy.exe` ran the official Pi RPC CLI through configured CPA `gpt-5.6-sol`, accepted the fixed prompt, completed `wmb_save_source`, read source `f96400c6-10b5-472e-adee-3a1727f89c11` back through WMB business code and accepted abort. The isolated Pi config used only WMB's API key environment and did not read another Agent OAuth. Full `scripts/check.ps1` passed 21 tests, typecheck and Windows package. | Codex |
| WMB-1002 | M-1000 | CAP-014 | done | WMB-1001 | Amend `PRD.md`, `SPEC.md`, `TECHNICAL_DESIGN.md` and `PLAN.md` for the approved built-in Pi executor scope, including an independently replaceable Pi runtime | 2026-07-28: PRD, SPEC CAP-014, technical design, delivery plan and `PI_INTEGRATION_PLAN.md` now agree on explicit Pi intents, one independently replaceable bundled runtime, WMB-only API credentials, MCP-only business writes, persistent right dock, external-Agent continuation and manual final publication. Full `scripts/check.ps1` passed 21 tests, typecheck and Windows package. | Codex |
| WMB-1003 | M-1000 | CAP-014 | done | WMB-1002 | First usable Pi conversation in packaged WMB: bundled runtime, one RPC supervisor, narrow chat IPC and enabled right-dock composer | 2026-07-28: packaged `WeMediaBuddy.exe` with bundled `.pi-runtime` used configured CPA `http://localhost:61946/v1` + `gpt-5.6-sol`; supervisor waits on `agent_settled`, disables default tools, and exposes narrow `pi:chat` IPC to the right-dock composer. Packaged CDP readback completed three consecutive real turns `香蕉` → `banana` → `banane`, plus one visible dock turn returning `我是 WeMediaBuddy 内置 Pi。`; screenshot `J:\Users\yangda01\Temp\wmb-pi-chat-ui\pi-dock-three-turns.png` and receipt `J:\Users\yangda01\Temp\wmb-pi-chat-ui\receipt.json`. Focused `tests/pi-runtime.test.mjs` and `tsc --noEmit` passed; Windows package rebuilt. 2026-07-30 fix (owner report: Pi exits with “Extension does not export a valid factory function”): root cause — Pi discovers every top-level `.ts` under `pi-agent/extensions` as an extension (loader.js `discoverExtensionsInDir`), so the three WMB library files were loaded as extensions and rejected; fix restructures the extension as a subdirectory package `.pi/extensions/wmb-mcp/{index.ts + 3 libs}` (only `*/index.ts` is loaded), `preparePiExtension` now copies the package and deletes stale flat files in the data root, top-level env throw in `wmb-mcp-client.ts` moved into `mcpUrl()`; live-root CDP verification `.ai/wmb-pi-fix-verify.mjs` exit 0 — layout flat=[] pkg=[index.ts+3 libs], real Pi chat 「只回复两个字:正常」→「正常」(`.ai/wmb-pi-fix-reply.png`); affected content tests updated to the new path, `tests/pi-runtime.test.mjs` + `tests/pi-config.test.mjs` + `tsc --noEmit` + lightweight harness all passed | Codex |
| WMB-1004 | M-1000 | CAP-014 | done | WMB-1003 | Stream Pi reply events into the dock, expose stop generation, and show actionable starting/running/failed/stopped states | 2026-07-28: `pi:event` streams `starting/running/delta/idle/stopped/failed`; dock shows live text and a one-click `停止` button; `pi:stop` aborts the active turn and immediately re-enables send. Packaged CDP receipt proved progressive deltas (`deltaCount=50`, `1` → `1..12`), stop kept partial assistant text `1\n2\n3` without echoing the user prompt, then resume reply `恢复` returned to `idle`. Screenshot `J:\Users\yangda01\Temp\wmb-pi-stream-stop\pi-stream-stop.png`, receipt `J:\Users\yangda01\Temp\wmb-pi-stream-stop\receipt.json`. Focused `tests/pi-runtime.test.mjs` and `tsc --noEmit` passed; Windows package rebuilt. | Codex |
| WMB-1005 | M-1000 | CAP-014 | done | WMB-1004 | Persist Pi conversation/session identity and make Windows start, crash, restart and shutdown deterministic | 2026-07-28: Pi conversations persist under data-root `pi-agent/conversations` with one session file per conversation; page changes keep the active conversation; a killed Pi child becomes visible `failed` (`Pi 进程已退出 …可重新发送`) with zero orphan `cli.js` processes; app restart restores the active session and transcript. Packaged receipts `J:\Users\yangda01\Temp\wmb-pi-persist\receipt.json`, screenshots `before-restart.png` / `after-restart.png`. Focused `tests/pi-runtime.test.mjs` + `tests/pi-conversation.test.mjs`, typecheck and Windows package passed. Before-quit aborts/stops Pi before exit. | Codex |
| WMB-1006 | M-1000 | CAP-002, CAP-003, CAP-004, CAP-007, CAP-011, CAP-012, CAP-014 | done | WMB-1005 | Inject only the current page and selected business-object references into Pi turns | 2026-07-28: dock sends only compact `[WMB_CONTEXT]` refs (`page/pageLabel/objectType/objectId/objectTitle`) plus the user message; UI shows the same ref line and persists only the visible user text. Packaged CDP five-page readback: Today `plan_item`, Library `source`, Studio `project`, Publish `publication`, Results page-only; replies identified page/object without restating full bodies. Receipt `J:\Users\yangda01\Temp\wmb-pi-context\receipt.json`, screenshot `context-pages.png`. `tsc --noEmit` passed; Windows package rebuilt. | Codex |
| WMB-1007 | M-1000 | CAP-005, CAP-014 | done | WMB-1006 | Thin Pi extension exposing the necessary existing WMB MCP read/write tools and approved research tools | 2026-07-28: packaged Pi launches with `--no-builtin-tools` plus bundled `.pi/extensions/wmb-mcp.ts` proxy tools (`wmb_get_workbench`, `wmb_search_sources`, `wmb_get_source`, `wmb_save_source`, `wmb_get_content`) over live loopback MCP only. Dock turn read the workbench and wrote source `9a463668-d709-44ad-9fd0-5c46413bc8e8` (`WMB-PI-MCP-1785234592428`) through MCP; SQLite readback matched title/url/summary and assistant confirmed the save without direct DB/file claims. Receipt `J:\Users\yangda01\Temp\wmb-pi-mcp\receipt.json`, screenshot `pi-mcp-write.png`. `tsc --noEmit` passed; Windows package includes `resources/extensions/wmb-mcp.ts`. | Codex |
| WMB-1008 | M-1000 | CAP-005, CAP-013, CAP-014 | done | WMB-1007 | `agent_tasks` migration, business commands, narrow fixed-intent IPC, duplicate-click reuse, interruption recovery and business-readback completion | 2026-07-28: migration 11 adds `agent_tasks`; business commands cover start/reuse/get/cancel/fail/complete with stable `taskId:step` request IDs; restart recovery converts running → interrupted; daily_intelligence/studio_draft success requires live business-object readback. Focused `tests/agent-tasks.test.mjs` passed 3/3 (reuse, completion gate, interruption/cancel/fail/success). Narrow IPC `agent:*` exposed on preload. `tsc --noEmit` passed. | Codex |
| WMB-1009 | M-1000 | CAP-002, CAP-003, CAP-005, CAP-014 | done | WMB-1008, WMB-0216 | Packaged Today “开始今日情报” flow using real Pi, the intelligence Skill and live WMB MCP | 2026-07-28: Today button starts fixed intent `daily_intelligence` through real Pi + bundled `wemedia-intelligence-engine` + live MCP (`wmb_save_source`/`wmb_save_plan`). Packaged UI run saved plan `e617603b-f8c0-4a29-9240-38e7e358de67` with 2 cited opportunities and 7 sources; task `389e8907-2058-4e5d-ab24-cc0e365734e0` succeeded after business readback; concurrent second start returned `reused:true` for the same task id (no duplicate run). Screenshot `J:\Users\yangda01\Temp\wmb-daily-intel\today-intelligence.png`, receipt `receipt.json`. Skills packaged under `resources/skills`. | Codex |
| WMB-1010 | M-1000 | CAP-004, CAP-005, CAP-014 | done | WMB-1009 | Packaged Studio “让 Pi 写初稿” flow saving a core version through WMB MCP | 2026-07-28: Studio button starts `studio_draft` through real Pi + MCP `wmb_save_core_version`. Packaged UI on project `8b0406a5-ebd4-4604-b6eb-3d4403cde3f9` saved core v2 `73af7e89-66a2-49c5-8222-e143a2258133`; task `d6e50312-83c2-4dc8-98df-b4c313ed579f` succeeded; Studio page showed the new body without external chat. Receipt `J:\Users\yangda01\Temp\wmb-studio-draft\receipt.json`, screenshot `studio-draft.png`. | Codex |
| WMB-1011 | M-1000 | CAP-005, CAP-011, CAP-012, CAP-014 | done | WMB-0804, WMB-1008 | Results-page Pi review flow using real publications, snapshots and existing review business commands | 2026-07-28: Results “让 Pi 复盘” starts fixed intent `results_review` through real Pi + MCP (`metrics.get`/`reviews.save`/`reviews.get`). Packaged UI on publication `79a3ff1d-…` (snapshot `b9e326d8-…`) saved final review `e653ed59-…` with concrete Keep/Stop/Change + finding `0755c861-…`; task `74f35d3f-…` succeeded after business readback. Receipt `J:\Users\yangda01\Temp\wmb-results-review\receipt.json`, screenshot `results-pi-review.png`. | Codex |
| WMB-1012 | M-1000 | CAP-013, CAP-014 | done | WMB-1005 | Independently update the bundled Pi runtime with version display, staged replacement and rollback | 2026-07-28: Settings shows active Pi version `0.82.1` (bundled). `pi-runtime-manager` stages into data-root `pi-runtime/staging`, probes CLI `--help`, activates to `active`, keeps failed probes off the live path, and can rollback `previous`. Isolated probe: bad source rejected (`NOT_FOUND`), good source activates override, second update creates previous, rollback succeeds. Receipts `J:\Users\yangda01\Temp\wmb-pi-runtime-update\receipt.json`, Settings UI `J:\Users\yangda01\Temp\wmb-pi-runtime-settings\receipt.json`. Dock/agent runners resolve active override CLI without reinstalling WMB. | Codex |
| WMB-1013 | M-1000 | CAP-014 | done | WMB-1005 | Minimum durable dock UX: Markdown, hidden-theme scrollbar, auto-scroll, copy reply and explicit new conversation | 2026-07-28: Pi dock renders assistant Markdown (`strong`/`li`/`code`), auto-scrolls on new messages, copies replies, and supports durable new/switch conversation history. Packaged widths 1100/1672: dock usable, body/shell overflow 0. Interrupted merge recovery removed a duplicated dock/settings implementation and fixed first-conversation recursion; typecheck, focused Pi tests, **35/35** total tests, lightweight harness and Windows package all pass. Receipt `J:\Users\yangda01\Temp\wmb-dock-ux\receipt.json`, screenshots `dock-1100.png` / `dock-1672.png`. | Codex |
| WMB-1014 | M-1000 | CAP-013, CAP-014 | done | WMB-1003 | Settings API preset manager with multiple encrypted configurations and one explicit active preset | 2026-07-28: existing single configuration reads back as `默认配置`; Settings can create, edit, activate and delete named OpenAI-compatible presets while never returning encrypted keys to the renderer. Switching or saving the active preset stops the existing Pi process so the next turn uses the selected endpoint. Focused preset test passed; typecheck and lightweight harness passed; Windows x64 package rebuilt and launched successfully. | Codex |
| WMB-1015 | M-1000 | CAP-013, CAP-014 | done | WMB-1014 | Fetch protocol-correct model IDs for API presets and select one in Settings | 2026-07-28: official OpenCode Go research corrected the endpoint to `https://opencode.ai/zen/go/v1`, model catalog to `/models`, and MiMo protocol/model to OpenAI Chat Completions + `mimo-v2.5`; no OpenCode CLI is involved. WMB supports OpenAI Responses, OpenAI Chat Completions and Anthropic Messages, plus an OpenCode Go quick preset. Live official `/models` readback returned 23 IDs. The user's existing preset was corrected in-place while preserving its encrypted key. Three focused tests, typecheck, lightweight harness and Windows package passed; packaged app restarted. | Codex |
| WMB-1016 | M-1000 | CAP-013, CAP-014 | done | WMB-1015 | Design Settings as a dedicated Codex Desktop-style workspace | 2026-07-28: standalone interactive HTML at `docs/design/settings-page-concept.html`; 1920×1080 Edge render confirms fixed 260px settings navigation, centered 860px adaptive form workspace, no Pi/sidebar competition, Dark/Daylight Violet themes and responsive 1100/760px behavior. Screenshot `J:\Users\yangda01\Temp\wmb-settings-concept.png`; lightweight harness passed. | Codex |
| WMB-1017 | M-1000 | CAP-013, CAP-014 | done | WMB-1016 | Implement the approved dedicated Settings workspace in the packaged app | 2026-07-28: Settings now opens as a dedicated workspace with its own fixed category navigation and centered adaptive form surface; normal business sidebar, Pi dock and status bar leave the layout while Settings is open, and “返回工作台” returns to Today. Existing API presets/model fetch, OpenCode Go shortcut, theme, data root, browser profile, MCP status, logs and Pi runtime controls were preserved without backend changes. Typecheck, Vite/Electron development launch, 1616×968 real window render and lightweight harness passed. Screenshot `J:\Users\yangda01\Temp\wmb-settings-implemented-print.png`. | Codex |
| WMB-1018 | M-1000 | CAP-014 | done | WMB-1017 | Move Pi model control into the dock status row with model and reasoning selection | 2026-07-28: the Pi composer status row now uses a neutral compact model button; hover/open adds violet emphasis. Clicking reads the active preset's live `/models` catalog and opens an upward, unclipped selector for model plus Pi-native reasoning levels (`auto`, `off`, `minimal`, `low`, `medium`, `high`, `xhigh`, `max`). Applying reuses `savePiConfig`, persists the choice, stops the old Pi process and passes `--thinking` to the next runtime. Focused Pi config tests 3/3, typecheck, live Electron launch/render and lightweight harness passed. Screenshot `J:\Users\yangda01\Temp\wmb-model-control.png`. | Codex |
| WMB-1019 | M-1000 | CAP-003, CAP-014 | done | WMB-1018 | Multi-select Today opportunity cards as explicit Pi context | 2026-07-28: primary and compact Today opportunity cards now toggle independently with visible check state; “进入创作” remains a separate action. Clicking blank Today space clears all selections. Pi's context chip shows the selected count, and the next message includes every selected opportunity's ID, title, why-now, angle, point of view, title/opening/structure guidance and source IDs. Typecheck, live Electron two-card selection, Pi chip readback and blank-space clear passed. Screenshots `J:\Users\yangda01\Temp\wmb-today-multi-context.png` and `wmb-today-context-cleared.png`; lightweight harness passed. | Codex |
| WMB-1020 | M-1000 | CAP-003, CAP-014 | done | WMB-1019 | Fix Today blank-space clearing across the complete Today workspace | 2026-07-28: root cause was the clear handler being scoped to `.today-main`; clicks in sibling Today regions never reached it. The handler now lives on `.today-layout`, so any non-card, non-control blank space across the Today workspace clears all selected opportunities while cards and controls remain independent. Typecheck and lightweight harness passed. | Codex |
| WMB-1021 | M-1000 | CAP-003, CAP-014 | done | WMB-1020 | Hide Today selection affordance until a card is selected | 2026-07-28: unselected opportunity cards no longer render an empty checkbox; the violet check appears only for selected cards and disappears completely when selection is cleared. CSS-only focused change; lightweight harness passed. | Codex |
| WMB-1022 | M-1000 | CAP-014 | done | WMB-1021 | Normalize Pi Markdown list and block spacing | 2026-07-28: minimal reproduction proved three adjacent `-` items became three separate `<ul>` blocks with `<br>` between them. The renderer now groups adjacent bullets into one valid list, removes the invalid outer paragraph wrapper and normalizes list/heading margins. Focused renderer assertion, typecheck, restarted Electron render and lightweight harness passed. Screenshot `J:\Users\yangda01\Temp\wmb-pi-markdown-spacing.png`. | Codex |
| WMB-1023 | M-1000 | CAP-002, CAP-014 | done | WMB-1022 | Enable Pi's complete built-in toolset so the intelligence Skill can read its child files and execute real collection work | 2026-07-28: root cause was all four dock/fixed-intent Pi launch paths passing `--no-builtin-tools`, leaving only the WMB MCP extension and preventing Skill reference/script access. Removed the restriction from dock, daily intelligence, Studio draft and Results review runners; no `--no-tools`, allowlist, denylist, skill or context restriction remains in `src/main`. Official bundled Pi help confirms the unrestricted default provides read/bash/edit/write tools. Focused source assertion, typecheck, lightweight harness and restarted Electron development app passed. | Codex |
| WMB-1024 | M-1000 | CAP-014 | done | WMB-1023 | Replace Pi's static sending ellipsis with visible live execution progress | 2026-07-28: minimal reproduction confirmed every outbound turn inserted an empty streaming bubble rendered as one hard-coded ellipsis while Pi tool execution events were discarded. The main process now forwards real tool start/end events for dock and fixed-intent runs; the dock shows live human-readable phases (connect, think, read, search, execute, organize, save) with an animated violet pulse/sweep until text streams. Static ellipsis removed; reduced-motion and live-region behavior retained. Focused source assertion, typecheck, lightweight harness and Electron restart passed. | Codex |
| WMB-1025 | M-1000 | CAP-014 | done | WMB-1024 | Replace the custom Pi Markdown subset with complete sanitized GFM rendering | 2026-07-28: reproduced the screenshot defect: the hand-written regex renderer had no table, task-list, quote or nested-block grammar. Deleted it and adopted `marked` complete GFM parsing plus `DOMPurify` sanitization. Pi replies now render headings, paragraphs, emphasis, strikeout, links, images, ordered/unordered/nested/task lists, blockquotes, horizontal rules, tables, inline code and fenced code blocks; dock theme styles cover every emitted structure. Focused GFM fixture asserted all major structures, typecheck and lightweight harness passed, and Electron development app restarted. | Codex |
| WMB-1026 | M-1000 | CAP-002 | done | WMB-1025 | Read-only GitHub AI ranking subpage in Library | 2026-07-29: Library defaults to a dedicated `AI 榜单` subpage and preserves `入库资料` separately. Sources: GitHub daily/weekly/monthly, What's Trending AI, OSSInsight AI and TrendingRepo AI/ML. A live read returned GitHub 12/22/23, What's Trending 30 and TrendingRepo 25 repository entries; OSSInsight currently publishes an empty repository ItemList, shown honestly as unavailable. Fixed an evidenced parser defect that treated OSSInsight's navigation ItemList as repositories by requiring `owner/repo` identities. Results cache in the main process for one hour; page switching reuses cache and only the refresh button bypasses it. Focused parser assertion and typecheck passed; development app restarted. | Codex |
| WMB-1027 | M-1000 | CAP-001, CAP-013 | done | WMB-1026 | Move diagnostics into Settings and theme control into the status bar | 2026-07-29: removed Diagnostics and Theme from the main sidebar; Settings now contains a dedicated System Diagnostics section with database, MCP, browser and log access. Theme is a neutral bottom-status control available in both business and Settings workspaces. Ranking text links became compact accessible external-link graphic buttons. Typecheck and focused UI-entry assertions passed. | Codex |
| WMB-1028 | M-1000 | CAP-002 | done | WMB-1026 | Prefer source APIs for ranking ingestion | 2026-07-29: OSSInsight AI now reads its unauthenticated official trends API and filters current AI repositories instead of parsing the upstream empty page; live API returned 100 rows and 65 AI matches. What's Trending switched from HTML to its public `/api/repos` endpoint, live returning 98 rows. GitHub daily/weekly/monthly retain the official Trending pages because GitHub exposes no official Trending API; TrendingRepo retains public JSON-LD because its API is a paid Pro feature. One-hour cache and manual refresh semantics remain. Focused test, typecheck, live API readbacks and app restart passed. | Codex |
| WMB-1029 | M-1000 | CAP-002, CAP-014 | done | WMB-1028 | Select ranking boards and repository items as explicit Pi context | 2026-07-29: ranking items support cross-board multi-selection with a check shown only while selected; each whole board can be independently added; external-link buttons remain separate; blank Library space clears all ranking selections. Pi's context chip reports board/item counts and the next turn receives exact board source plus repository rank/name/URL/description/language/star fields in `rankingContext`. Typecheck and focused context-path assertions passed. | Codex |
| WMB-1030 | M-1000 | CAP-014 | done | WMB-1029 | Right-align user messages and resize the Pi dock | 2026-07-29: user bubbles and metadata align right; the Pi dock's left edge is a pointer drag separator constrained to 300–640px, stores width in localStorage, and resets to 380px on double-click. Collapsing still forces zero width through a separate open-width variable. Typecheck and focused dock-path assertions passed. | Codex |
| WMB-1031 | M-1000 | CAP-002, CAP-014 | done | WMB-1029 | Make ranking-tab selection directly set Pi board context | 2026-07-29: root cause was ranking tabs only changing `boardId`; context changed only through a redundant second button. Clicking any ranking tab now immediately sets that full board and its repository data as Pi context, shows a check on the context-active tab, and clears item-only selection. Selecting repository items switches to item-only multi-context; blank space clears both. The redundant board-confirmation button was deleted. Typecheck and focused board-context assertions passed. | Codex |
| WMB-1032 | M-1000 | CAP-002, CAP-014 | done | WMB-1031 | Add Skill, MCP, Hugging Face, Product Hunt and model benchmark rankings | 2026-07-29: added Skill Trending, Smithery MCP usage, Hugging Face trending models, Product Hunt AI launches and Artificial Analysis model capability boards through the existing one-hour main-process cache. Smithery and Hugging Face use anonymous APIs; skills.sh, Product Hunt and Artificial Analysis use their official public leaderboard/feed because their APIs require OIDC, OAuth or an API key. All boards inherit whole-board and multi-item Pi context. Live parser readback returned Skill 179, MCP 30, Product Hunt 50, Artificial Analysis 30 and Hugging Face 10 items. Focused ranking test, typecheck, lightweight harness and diff check passed. | Codex |
| WMB-1033 | M-1000 | CAP-002 | done | WMB-1032 | Compact ranking refresh control to an icon | 2026-07-29: ranking refresh is now a compact 40px icon-only control; hover text and dynamic accessible labels retain its meaning while the existing refresh/cache behavior remains unchanged. Focused source assertion passed. | Codex |
| WMB-1034 | M-1000 | CAP-002 | done | WMB-1033 | Align and size the ranking refresh icon | 2026-07-29: reproduced from CSS: the 40px icon was centered against 34px wrapping filters instead of their first row. Matched the control to 34px, aligned it to the first toolbar row and normalized glyph line-height. Focused CSS assertion passed. | Codex |
| WMB-1035 | M-1000 | CAP-002 | done | WMB-1034 | Animate ranking refresh while loading | 2026-07-29: the refresh glyph now starts a 700ms continuous rotation immediately when the existing loading state begins and stops when the request settles; duplicate clicks remain disabled. Focused source assertion passed. | Codex |
| WMB-1036 | M-1000 | CAP-014 | done | WMB-1035 | Restore Pi-native dock turn controls: no ordinary-chat deadline; native stop, steer, follow-up and queue display; durable native transcript and fork branch for retrace/retry | 2026-07-30: focused Pi RPC fixture covers steer, follow_up, native queue_update, fork and no 120s deadline. `npm run typecheck`, Windows x64 package and `scripts/check.ps1` pass. Packaged EXE readback `.ai/wmb-1036-verify.mjs` exit 0: generation kept the input enabled with a square stop control, Pi native steer queue rendered “插队”, stop settled, and native fork restored the original user text for resend. | Codex |
| WMB-1100 | M-1100 | CAP-004, CAP-005, CAP-014 | done | WMB-1035 | Define the long-term Studio workbench plan for 1000+ content projects | 2026-07-29: `STUDIO_LONG_TERM_PLAN.md` records the evidenced full-read and Pi project-boundary failures, product rules, five implementation stages, non-goals, failure conditions and 1001-project acceptance. Three independent reviews covered information architecture, data/MCP correctness and minimum scope. | Codex |
| WMB-1101 | M-1100 | CAP-004, CAP-005, CAP-014 | done | WMB-1100 | Correct Pi new-project versus new-version semantics | 2026-07-29: added `wmb_create_content_project`, backed by one SQLite transaction that creates the project and immutable v1 together; Pi's shared prompt now requires new themes/rankings/articles to create a project, reserves `wmb_save_core_version` for explicit continuation, forbids similarity guessing, and requires project-ID readback. Real Pi-extension → MCP → SQLite check created Skill project `c30b51f2-9253-4624-abf8-d8d717ba225f` with title `Skill 新稿`, v1 body `独立正文`; original MCP project `2246d50e-5c31-4ba7-9320-0af579981880` stayed at 1 version. Simulated mid-transaction failure left no project; explicit continuation produced v2. Focused content checks 3/3, typecheck, lightweight harness and diff check passed. | Codex |
| WMB-1102 | M-1100 | CAP-004, CAP-005 | done | WMB-1101 | Add project lifecycle fields, bounded list/search and single-project detail reads | 2026-07-29: migration 15 adds `status` (`idea`, `drafting`, `review`, `ready`, `completed`), nullable `archived_at` and two list indexes; existing rows receive `drafting`. Added server-side `content.list({query,status,archived,limit,offset})` with a hard 50-item ceiling, latest-body `LIKE` search, deterministic paging, version/platform summaries and no bodies, plus ID-required `content.get` with one project's complete sources, notes, decisions, immutable versions and platform versions. The same reads are exposed through IPC and Pi (`wmb_list_content_projects`, `wmb_get_content`). Real Pi-extension → MCP → SQLite readback over 55 projects returned page size 50, `hasMore=true`, one search hit (`c11b7922-ab1a-40cf-8aeb-6527d3fee20c`), zero list bodies, two detail versions and one archived/status-filtered project. Focused content/migration checks 5/5, typecheck, lightweight harness and diff check passed; query-count and 1001-project concurrency remain WMB-1103. | Codex |
| WMB-1103 | M-1100 | CAP-004, CAP-005 | done | WMB-1102 | Add project revision conflict protection and fixed-query-count 1001-project regression | 2026-07-29: core-version writes now require `expected_revision`; one `BEGIN IMMEDIATE` transaction reads the project, rejects stale clients with `REVISION_CONFLICT` plus the complete current project, inserts one immutable version and increments project revision. Studio IPC, MCP and Pi all use this shared command; Pi reads the current revision before saving. The 1001-project regression created exactly 3,003 versions of 1,500 characters, paged all projects in 21 pages without duplicates or omissions, measured one SQL statement per list/search call and six fixed statements for one detail, and found `project-1001` through its latest body. Two Pi-extension/MCP clients both held revision 1: client A created v4/revision 2; client B received `REVISION_CONFLICT`, leaving exactly four versions and revision 2. Direct receipt: `projectCount=1001`, `initialVersionCount=3003`, `finalVersionCount=3004`, `listQueriesPerPage=[1]`, `detailQueryCount=6`, `searchQueryCount=1`. Full tests 42/42, typecheck, lightweight harness and diff check passed; Windows packaging remains reserved for WMB-1106. | Codex |
| WMB-1104 | M-1100 | CAP-004, CAP-014 | done | WMB-1103 | Rebuild Studio with search, work-state views, archive, paging and on-demand detail | 2026-07-29: Studio now reads server-side body-free summaries in pages of at most 50, supports debounced search, five work states, archive view/actions and load-more, and fetches only the selected project detail. The global five-second `getStudio()` full-body poll was removed; Studio polls only its first summary page and reloads selected detail only when `updatedAt` changes. Project status, revision, source count and platform counts remain visible inside the editor while Pi is open. Real desktop readback at 1100×900 and 1920×900 returned 7 summary rows, zero body fields, one on-demand 2,058-character body, editable editor, visible inspector/Pi and zero body/shell horizontal overflow. Real project `372fcc4e-8c1b-4ab2-97b6-1d4a1bfaf086` archived and read back as archived, then restored with `archivedAt=null` and revision 3. Screenshots `.ai/wmb-1104/studio-1100.png` and `studio-1920.png`; lifecycle regression, typecheck, 43/43 tests, lightweight harness and diff check passed. Windows packaging remains reserved for WMB-1106. | Codex |
| WMB-1105 | M-1100 | CAP-004 | done | WMB-1104 | Add usable version history, project materials and explicit copy-version-to-new-project | 2026-07-29: Studio now provides immutable version-history viewing, explicit return to the editable latest version, one-version save from any selected version, and an explicit “复制版本为新项目” action. Project detail adds fixed-query full source and linked-asset records while retaining exact core/platform bindings; Studio switches among sources, platform bodies and asset metadata. Real desktop at 1100px showed version 3 of project `372fcc4e-8c1b-4ab2-97b6-1d4a1bfaf086` as read-only with all three explicit actions, then copied it through IPC/business logic to independent project `0e52e158-5f09-4678-9432-f5adb0c31482`, title `Skill 生态正在分化（独立项目）`, v1. Original remained at 3 versions; both bodies were 2,058 characters with identical SHA-256 `4e07155127874d111f0a7f34ab784ca86e67a3585cc15489e9595add2852c908`. Real source/platform/asset tabs matched database counts 1/1/1 and displayed the exact source title, platform body and asset SHA-256 with zero horizontal overflow. Focused copy/materials and old-version-save regression passed; detail stays fixed at 8 queries, typecheck, 44/44 tests, lightweight harness and diff check passed. Screenshots `.ai/wmb-1105-version-copy.png` and `.ai/wmb-1105-materials.png`; packaging remains reserved for WMB-1106. | Codex |
| WMB-1106 | M-1100 | CAP-004, CAP-005, CAP-014 | done | WMB-1105 | Migrate real data and complete 1001-project packaged desktop acceptance | 2026-07-29: `scripts/studio-long-term-acceptance.mjs` made a real-data v14 copy, applied migration 15, and proved project/core-version/platform-version counts stayed `8/12/6`; core body SHA-256 stayed `2d6ebf616aab711cb7a4f8454a9a2eb56b9576a6a11cd881fb2ece74c70157ec` and platform body SHA-256 stayed `efcccca310d0c026cde69a63d97a6c7ec5251baad17af25f72578999a1f55819`. The isolated copy at `J:\Users\yangda01\Temp\wmb-1106-data-20260729` was then filled to exactly 1,001 projects: packaged/development Studio rendered 50 first-page summaries in 356ms, loaded 100 unique rows, server search found tail project `wmb-1106-scale-0993` with its 1,500-character body, and 1100/1920 widths had zero shell/body horizontal overflow with editor, inspector and Pi usable. Real Pi created project `013222ff-68f6-49ba-9374-0ef79d267af5` v1; an external Agent continued it through live MCP as v2; concurrent clients at revision 2 produced only winner v3 while the stale client received `REVISION_CONFLICT` with current detail. UI set it to `review` and archived; both development and packaged restarts restored its exact selected ID, status, archive state, three versions and latest body `并发胜者正文 v3。`, including while absent from the active first page. The stage-end `scripts/check.ps1 -Full` ran once: typecheck, 44/44 tests and Windows x64 packaging passed. Packaged `WeMediaBuddy.exe` launched from `out\WeMediaBuddy-win32-x64`, read the 1,001-item copy, exposed ready MCP, used bundled Pi `0.82.1`, and real packaged Pi replied `WMB-1106 packaged Pi ready`; EXE SHA-256 `d2876df63078e60c1f47eb757cf03a1a4572b0148596fb9608fb5792e0f3475b`, `app.asar` SHA-256 `993d9f992bfa486c4792f92a3c11f73268582deb79710ca40f2d659f885f5959`. Screenshots `.ai/wmb-1106-studio-1100.png`, `wmb-1106-studio-1920.png`, `wmb-1106-restart.png`, `wmb-1106-packaged.png`, and `wmb-1106-packaged-restart.png`. Final packaged app was returned to the original `J:\PigeonYang\WeMediaBuddyData`; its counts and hashes still match the pre-acceptance snapshot. | Codex |
| WMB-1107 | M-1100 | CAP-004, CAP-014 | done | WMB-1106 | Implement the approved V2 long-term project library and complete core writing editor | Completed 2026-07-29 after Owner-directed fidelity rework. The packaged Studio now directly follows the approved V2 library/editor hierarchy: breadcrumb/actions, integrated heading/status summary, server search/order/platform filters, state markers, platform progress, bounded previous/next paging, article outline, complete formatting toolbar, document canvas, status bar and version/source/asset context. TXT/Markdown import creates one atomic project; the duplicate body H1 and legacy tabs/inspector/footer are removed. SQLite sorting changed the first result and X filtering returned only the three projects with X platform versions while retaining one bounded body-free list statement. Real contenteditable bold produced one formatted node, undo removed it and redo restored it before reloading persisted content. Packaged responsive readback: Pi-open document widths `644/780/1128px` at `1100/1366/1920px`; the two smaller widths hide only the outline, project details remain reachable as a drawer, all six Pi/width combinations have zero horizontal overflow, and Pi collapse/expand passes normal pointer clicks on library and editor. Evidence `.ai/wmb-1107-final-library-1920.png`, `.ai/wmb-1107-final-editor-1100-pi.png`. Typecheck, focused content tests 3/3, lightweight harness and Windows x64 package pass. | Codex |
| WMB-1108 | M-1100 | CAP-001, CAP-014 | done | WMB-1106 | Enforce one running WeMediaBuddy application instance and focus the existing window on a second launch | 2026-07-29: falsifiable packaged repro showed first launch `1` main window and second launch `2`, confirming the main process had no single-instance lock. `app.requestSingleInstanceLock()` now makes the secondary process quit and handles `second-instance` by restoring, showing and focusing the existing window. Rebuilt Windows x64 package acceptance minimized the first window, launched the EXE again, and read back exactly one main window with the same HWND; the original window was restored and the second launcher exited. Typecheck and lightweight harness passed. Packaged EXE SHA-256 `30b48cbf422ab97f1809a2e3cdb02a79872c7d22b583316fac5cc852981a517c`; app.asar SHA-256 `42148e21cf0013c648ba86b3b29ae6eeb0a2d2de9a4e9d87ad68382a6bbaa669`. | Codex |
| WMB-1109 | M-1100 | CAP-004, CAP-014 | done | WMB-1107 | Restore the approved Studio typography and layout fidelity | 2026-07-29 continued after Owner rejected the first numeric-only check. Same-size 1920×900 packaged comparison now matches the approved structural boundaries: top bar 64px; Studio navigation 208px; editor action row 56px; editor grid starts at `(208,120)` and occupies the remaining 780px height; outline 220px; version context 310px; document paper 780px; no global 50px status row; no collapsed Pi arrow; project controls remain hidden until “项目资料” is explicitly opened. The detailed pass also restores the design's 30px logo, 14px brand, 14px/10px sidebar inset, 46px navigation rows, 12px icon/text gap, 18px icons and 16px first-paragraph top spacing. Title remains 29px/1.35 and body 16px/1.9 with normal wrapping. Typecheck, lightweight harness, diff check, Windows x64 packaging and live CDP assertions passed with zero horizontal overflow. Evidence `.ai/wmb-1109-restored-detail-1920-clean.png`. | Codex |
| WMB-1110 | M-1100 | CAP-004, CAP-014 | done | WMB-1109 | Render the Studio context sidebar on the first editor frame | Root cause was the complete context `<aside>` being conditionally mounted only after asynchronous project-detail readback, allowing the editor's first grid paint to omit the right column until a native resize forced repaint. The sidebar shell now mounts with the editor and only its contents wait for project data. Fresh packaged startup at the untouched default `1600×960` viewport showed the context sidebar on the first animation frame with `display:flex` and width `310px`; it remained `310px` after detail settled, with no window resize. Typecheck, lightweight harness, diff check and Windows x64 package passed. Evidence `.ai/wmb-1110-startup-sidebar.png`. | Codex |
| WMB-1111 | M-1100 | CAP-014 | done | WMB-1110 | Restore the Pi dock's hidden collapse control | Root cause was the expanded Pi collapse control's base rule being changed to `opacity:1; pointer-events:auto`, making the hidden affordance permanently visible. Restored `opacity:0; pointer-events:none`; dock hover, dock keyboard focus and control focus reveal it, while the collapsed-state reopen control remains available outside Studio. Fresh packaged readback returned `{opacity:0,pointer:none}` away from the dock and `{opacity:1,pointer:auto}` on dock hover. Lightweight harness, diff check, Windows x64 packaging and restart passed. Evidence `.ai/wmb-1111-pi-hidden-toggle.png`. | Codex |
| WMB-1112 | M-1100 | CAP-002, CAP-003, CAP-014 | done | WMB-1111 | Replace the monolithic five-minute daily-intelligence call with a durable, observable and resumable long-running task | 2026-07-29: migration 16 extends the existing `agent_tasks` ledger with progress/checkpoint/events/control/heartbeat and honest partial/cancelled outcomes. Daily intelligence now returns immediately, runs six independent four-minute source-route Pi turns plus a six-minute final synthesis turn, isolates failed/slow sources, uses stable per-route request IDs, resumes the same task after restart, and validates completed/partial results against task-owned MCP receipts. Today polls durable truth and shows stage, elapsed/current source, counters, recent events, last activity/stall warning, plus skip/save-partial/cancel controls. Packaged acceptance task `71b3a360-5ca8-4239-9e46-89ed7a881edd` survived forced EXE restart with the same ID, persisted heartbeat/events/checkpoint, terminated a slow current source via skip in 6 seconds, advanced processed 0→1 without window interaction, saved verified GitHub source `d348ffec-5368-4014-8876-ef1b80829a05` through stable receipt `...:source:1:0`, then finished `partial` with `opportunityCount=0` and no task plan receipt. SQLite/API readback matched UI. Agent-task checks 4/4, remaining suite checks 41/41 (one Windows handle retry), typecheck, lightweight harness, diff check and Windows x64 package passed. Final packaged UI launched with zero horizontal overflow; EXE SHA-256 `600D2021BAA4D46E6248D375DDBDE9EA1A6E2803595DAF8B1E151B0B0A690237`, app.asar `FC4B27C9A062B722F4C34E6A16AD7450D6D5BDF0D4CFC0F408351A55BE07D900`. | Codex |
| WMB-1113 | M-1100 | CAP-014 | done | WMB-1112 | Localize daily-intelligence phase labels | 2026-07-29: reproduced that Today rendered the persisted internal phase code directly. Added one renderer-only mapping for every current daily phase; `scanning_sources` now displays `正在扫描来源`, with a Chinese fallback for unknown future phases. Typecheck and focused source assertion passed. | Codex |
| WMB-1114 | M-1100 | CAP-014 | done | WMB-1113 | Fix Today task-status overflow and add task-control interaction feedback | 2026-07-29: screenshot and CSS readback proved a late global `.task-status{position:absolute}` rule moved the running label onto the opportunity card's bottom-right action. Removed the obsolete global rule and explicitly kept Today status in normal flow. Added hover, pressed, keyboard-focus and disabled feedback to all three intelligence controls. Typecheck, focused CSS assertions and development HMR launch passed. | Codex |
| WMB-1115 | M-1100 | CAP-002, CAP-003, CAP-014 | done | WMB-1114 | Retain and rank every qualifying daily opportunity | 2026-07-29: Owner removed the fixed 1–3 cap. PRD/SPEC/PLAN, the intelligence Skill/SOP/opportunity editor/output validator, Pi prompt, MCP schema/description, business save/readback, completion validation and Today now share one contract: retain every deduplicated qualifying opportunity; reject priority outside integer `0–7`; stably sort `SSS/S/A/B/C/D/E/F`; keep the first as the hero without truncating the remaining scrollable cards; show D/E/F distinctly; label an old plan while a new task is running. Source-route collection also changed from a two-item cap to all verified results completed within its existing four-minute time boundary. A deliberately shuffled eight-grade plan read back all eight as `0,1,2,3,4,5,6,7`; Agent completion returned `opportunityCount=8`; the Skill validator accepted eight ranked opportunities and rejected invalid ordering/range by contract. Focused tests 5/5, typecheck, lightweight harness, contract/source assertions and diff check passed. Development Vite HMR is active at port 5173 and the visible Electron window is responsive; no release package was rebuilt during this UI iteration. | Codex |
| WMB-1200 | M-1200 | CAP-002, CAP-003, CAP-004, CAP-011, CAP-012, CAP-015 | done | WMB-1115 | Implement long-term source management and knowledge compounding | 2026-07-29: `KNOWLEDGE_COMPOUNDING_PLAN.md` approved contract implemented. Migration 17 preserved real counts (17 sources, 4 plans, 9 projects, 8 publications, 2 reviews) and added dual lifecycle plus stable topics/relations. Shared business functions, IPC, MCP and Pi tools cover paged source management, topic recording, bounded history and deterministic rediscovery. EVAL-014 regression proves 250-source pagination, stale-write rejection, normalized topic idempotency and source/topic→opportunity readback. Full suite 46/46, typecheck and lightweight harness pass. Restarted development app and visually verified the independent 17-item Library at `.ai/wmb-1200-sources-fixed.png`; Windows package was not rebuilt because packaging boundaries did not change. | Codex |
| WMB-1300 | M-1300 | CAP-002, CAP-003, CAP-004, CAP-015, CAP-016 | done | WMB-1200 | Implement persistent knowledge canvas, semantic relations, exact static context packages and Pi/project traceability | 2026-07-29: migration 19 completed the approved WMB-1300 closure without changing existing business records. Shared business commands, IPC and MCP now cover relation edit/hide/archive, atomic idempotent package creation and package-use receipts, exact selected-only manifests, optional project binding, selected-source linking and package↔project readback; Pi records the previewed package revision/IDs before a selected-package turn. EVAL-015 focused readback proved duplicate request replay, stale revision zero partial writes, double-canvas references, placement-only deletion, live source updates with static snapshot stability, archived-relation snapshot retention, movement restore and 250-node save/restore. Typecheck and lightweight harness passed. Real DB migration 18→19 preserved exact counts and SHA-256 hashes for 17 sources, 4 plans, 7 plan items, 9 projects, 13 versions, 8 publications, 2 metric snapshots, 2 reviews and 2 findings. Existing real Electron receipts at 1100×700 and 1920×900 show the canvas, node ports, typed relation menu and selected-only package drawer (`.ai/wmb-semantic-menu-live2.png`, `.ai/wmb-canvas-1100.png`, `.ai/wmb-canvas-1920.png`, `.ai/wmb-canvas-pi-1920.png`). | Codex |
| WMB-1301 | M-1300 | CAP-016 | done | WMB-1300 | Complete the relationship-canvas work surface: create/open/rename multiple canvases, add/search real objects, notes, drag/pan/zoom, selection/box-selection, typed edge create/edit/reconnect/hide/delete, undo/redo and save/restore | 2026-07-29: shared commands and UI now deliver multi-canvas create/open/rename, real-object search/add, non-overlapping note placement, drag, box selection, pan/zoom persistence, port-drag typed relations, relation reconnect/edit/hide/show/archive, current-session move/relation undo-redo and keyboard selection/removal. Root-cause experiments fixed the 1100px action-hiding media rule, fixed notes covering connection ports, and preserved relation labels on hidden-only writes. Focused EVAL-015 business readback, typecheck and lightweight harness pass. A separate `show:false` Electron instance on CDP 9333 exercised the real React→preload→IPC→SQLite path without taking focus: UI canvas `44d84b45-9509-40c8-af76-8563177a8c8d` restored after process restart with title, 2 nodes, typed/reconnected relation, label, viewport `(70,60)` and zoom `1.1`; hidden relation was recoverable and label-stable. 1100×700 and 1920×900 both had zero document overflow; evidence `.ai/wmb-1301-headless/1100.png`, `1920.png`, `restart-1100.png`. | Codex |
| WMB-1302 | M-1300 | CAP-015, CAP-016 | done | WMB-1300 | Implement Domain Map as the long-term portfolio entry: domain create/edit/archive, status and change signals, search/filter/order, map/list modes and navigation into topics | 2026-07-29: migration 20 and shared business commands add revisioned domain create/edit/archive, ordered topic membership and bounded detail; IPC and idempotent MCP use the same commands. A 125-topic regression proved stable `100+25` paging, query-by-topic without shrinking aggregate counts, status/order filters, stale-revision rejection and archive. A hidden real Electron instance on CDP 9333 used isolated real-data-derived SQLite state to create `Agent 与工作流` with 3 topics/1 source, rename it to `Agent、Skill 与工作流`, change status to `持续观察`, find it by `MCP 工具生态` while retaining `3 个主题 · 1 条资料`, open the three-topic detail, enter `Agent Skill 生态`, and archive the domain while retaining topics/sources. 1100×700 and 1920×900 had zero document overflow; screenshots `.ai/wmb-1301-headless/wmb-1302-1100.png` and `wmb-1302-1920.png`. Focused domain regression, typecheck and lightweight harness passed. | Codex |
| WMB-1303 | M-1300 | CAP-015, CAP-016 | done | WMB-1302 | Implement the complete Topic Dossier: timeline and grouped views for sources, judgments, audience demands, counter-evidence, content history, metrics, reviews and method findings; place any asset into an existing or new canvas | 2026-07-29: `knowledge.topic_dossier_get` and shared IPC/business read provide one bounded, stable timeline over eight existing fact categories: linked sources, plan judgments, audience demands, contradicting sources, content projects, metric snapshots, final reviews and method findings; no parallel knowledge store was added. Migration 21 preserves existing canvas nodes/relations while allowing publication/metric references, and the shared canvas resolver now reads a content project's real latest version instead of the nonexistent `core_viewpoint` column reproduced by the focused test. Regression read back 104 primary + 1 contradicting source with exact `100+4` category paging, every remaining category once, live source updates, and seven distinct business-object references on one canvas. Hidden Electron on CDP 9334 rendered all eight categories in timeline and grouped modes, placed the Chubby source into new canvas `90dca2fd-ac40-4552-9211-f07b97db0685`, then placed metric `e89ea101-25e6-414e-a579-38baf95a701a` into that existing canvas; business readback returned two live references. Updating source `dc82b4ed-233a-4c3a-890a-9ab37ff6109e` through `upsertSource` appeared in both dossier and canvas within the five-second focused refresh. 1100×700 and 1920×900 had zero document overflow and usable internal scrolling; screenshots `.ai/wmb-1301-headless/wmb-1303-topic-1100.png` and `wmb-1303-topic-1920.png`. Dossier regression, canvas/migration regressions 2/2, typecheck and lightweight harness passed. | Codex |
| WMB-1304 | M-1300 | CAP-016 | done | WMB-1301, WMB-1303 | Implement reusable Context Package management: exact selection preview, object/relation exclusion, objective/question, static snapshot versions, package list/detail/archive and actual bounded manifest | 2026-07-29: migration 22 adds package family/version and persisted exclusion metadata without changing the selected-only snapshot tables. Shared preview/create/list/detail/archive commands, IPC and MCP now return exact object/relation IDs, explicit exclusions, static v1/v2 history, use counts, estimated characters and a 30,000-character hard gate that refuses before any package/use write. Focused regression used six canvas objects, explicitly excluded one sentinel and one internal relation, proved preview IDs equal saved five-item manifest IDs, later node edits left v1 unchanged, v2 read back in one family, active/archived paging worked, stale archive failed, an oversize object was blocked with no row, and the actual-use manifest exactly matched the saved preview including both exclusions. Hidden Electron on CDP 9334 selected five nodes while `未选哨兵` remained unselected, previewed `5 个对象 · 1 条内部关系 · 1501/30000 字符`, toggled an object to 4 and the relation to 0 before restoring both, saved `Agent Skill 精确包` v1, created v2 from its detail, and archived only v1; UI/API readback returned one active v2 and one archived v1. 1100×700 and 1920×900 had zero document overflow; screenshots `.ai/wmb-1301-headless/wmb-1304-packages-1100.png` and `wmb-1304-packages-1920.png`. Package regression, canvas/migration regressions 2/2, typecheck and lightweight harness passed. | Codex |
| WMB-1305 | M-1300 | CAP-005, CAP-016 | done | WMB-1304 | Complete Pi selected-package discussion: atomic idempotent MCP mutations, actual-use receipts, visible reading progress, discussion/creation modes and user confirmation of Pi-suggested nodes or relations | 2026-07-29: migration 23 and shared commands persist Pi node/relation suggestions separately from confirmed knowledge; Pi MCP `knowledge.suggestion_create` is atomic/idempotent, while only the human UI exposes individual confirm/reject. Stable resend request IDs, discussion/creation modes and the package panel expose exact included/excluded IDs. A real failure reproduced that the old UI wrote a use receipt before Pi credential decryption; root fix now commits the receipt only after successful Pi return, and the exact false experimental receipt `1671a1a3-...` was deleted with readback `remaining=0`. Regression proved request replay, exact manifest, pending/confirm/reject semantics; real MCP replay returned one suggestion and hidden UI readback showed nodes 6→7, relations unchanged, pending 0. A `show:false` Electron using the normal WMB user directory and real root sent package `be8242a6-...` v1 to configured Pi `gpt-5.6-terra`: running state stayed `待读取 0/2`; Pi returned the two exact titles/node IDs in manifest order and explicitly reported no package-external object; only then receipt `c462840e-...` appeared. UI resend reused request `dabea297-...`; SQLite still contained exactly one use row with package revision 1, two item IDs and one relation. 1100×700/1920×900 had zero overflow; evidence `.ai/wmb-1301-headless/wmb-1305-suggestions-1100.png`, `wmb-1305-real-pi-1920.png`. Full suite 51/51, final typecheck and lightweight harness pass. | Codex |
| WMB-1305A | M-1300 | CAP-005, CAP-014, CAP-016 | done | WMB-1305 | Replace the standalone context-package interface with direct page selection | 2026-07-29 Owner changed the contract twice: no package UI and no invisible package/snapshot rows. The minimal reproduction counted real DB `knowledge_context_packages=1` and `knowledge_context_uses=3`, then showed the old turn wrote both. The replacement resolves the current canvas read-only immediately before send and embeds that exact manifest in the existing Pi session turn; renderer no longer calls package create/use for direct context. Hidden real Electron on normal user data showed no package UI, default `当前页 2 项`, checkbox multi-select `已选 2 项`, and blank-board pointer down/up restored `当前页 2 项`. A selected-one real Pi turn returned exactly title `Chubby Skills…` and node `0bae3d3d-216c-45cd-bcb3-3f350e7ef61e`; the new session JSONL turn contains only that direct manifest and no tool call, while DB stayed exactly `packages=1, uses=3`. 1100×700 and 1920×900 had zero document/body overflow; screenshots `.ai/wmb-1305a-direct-context-1100.png` and `wmb-1305a-direct-context-1920.png`. Typecheck and lightweight harness passed. Historical package rows/APIs remain read-only-compatible but new UI creates none. | Codex |
| WMB-1306 | M-1300 | CAP-003, CAP-004, CAP-016 | done | WMB-1305A | Implement the Creative Composer: assemble primary/related topics, audience demand, current change, method, evidence and counter-evidence; discuss or generate an editable creative brief from resolved current-page/selected context | 2026-07-29 migration 25 converts `creative_briefs` from mandatory package ownership to nullable legacy package plus direct `canvas_id`, selection mode and canonical context node IDs; the migration preserved the existing package brief. Shared business/IPC/MCP/Pi create, update and context lookup now validate evidence against the direct page selection, are request-id idempotent, reject outside evidence and stale revisions, and create zero package/use rows. Focused regression passed create replay, outside-context rejection, revision conflict, confirm/readback and `packages=uses=0`. Hidden real Electron opened a one-node selection directly in the full composer, and a fresh configured Pi session created/read back brief `509cc059-9056-4d76-9329-ad270bc9f9c8` without a package; user UI edit produced revision 2 and confirmation revision 3. SQLite reads `package_id=NULL`, exact canvas/node ID, `selected`, confirmed title and revision 3 while legacy counts stayed `packages=1, uses=3`. Returning to canvas preserved the one selected node; reopening restored the same brief/title/revision. The composer resolves primary/related topics, evidence, current changes, methods and relation-derived counter-evidence from existing objects; Pi did not generate正文. 1100×700 and 1920×900 had zero document/body/composer overflow; screenshots `.ai/wmb-1306-composer-1100.png`, `.ai/wmb-1306-composer-1920.png`. Focused test, typecheck and lightweight harness passed. | Codex |
| WMB-1307 | M-1300 | CAP-004, CAP-011, CAP-012, CAP-016 | done | WMB-1306 | Link selected evidence → creative brief → content project → publication → metrics/review, and return confirmed review/method findings to the originating topics | 2026-07-29 migration 26 adds only the necessary one-to-one `creative_brief_projects` relation. Confirmed direct briefs create one idempotent content project and first version through the existing content commands; selected source nodes become `content_project_sources`, the first selected topic becomes `topic_id`, and no package/use row is created. `getCreativeBriefLineage` reads brief→project→publication→metric→review→finding, while `content.get` reads project→brief and exact context node IDs. Final review writes only pending review/finding return suggestions for selected originating topic nodes; confirming an existing suggestion atomically places the real object and semantic relation, while rejection creates neither. Focused real-SQLite regression passed project request replay, source/topic binding, platform/publication/metric/final-review/finding readback, two pending return suggestions, one user confirmation creating exactly one node+relation, and zero package/use rows; both WMB-1306/1307 focused tests and typecheck passed. Hidden real Electron created project `d4a27797-0736-407b-93d5-e86458a148c4` from confirmed brief `509cc059-...`, opened its first 611-character version in Studio, linked real source `61581b3a-...`, and showed `来自创作简报`; the project资料 panel reads brief revision 3, one evidence item and the source. Shared IPC lineage returns the same brief/project/source IDs. Screenshot `.ai/wmb-1307-project-lineage-1100.png`; 1100 width had zero document/body overflow. Lightweight harness passed. | Codex |
| WMB-1308 | M-1300 | CAP-014, CAP-015, CAP-016 | done | WMB-1302, WMB-1303, WMB-1305A, WMB-1306 | Rebuild the Knowledge System navigation and continuity across Domain Map, Topic Dossier, Relationship Canvas, Creative Composer and Content Projects | 2026-07-29 Owner rejected invisible snapshots and separate use receipts. A falsifiable audit found three production write paths still exposed after UI removal; package create/archive/use IPC handlers, preload methods, renderer types, MCP tools and the Pi package tool were removed, leaving historical rows read-only. A hidden real Electron path on the normal data root created/read back domain `e4acc53d-a441-4178-ae1c-26db7233f4f3` → topic `0d841c90-ba52-4ee0-8857-1ae304d4bc3c` → topic canvas `2b896bd5-896d-40f6-af45-a10728903381` → selected source node `443cdbda-7b6b-4340-97cf-d6b54c106fbb` → direct brief → project `a89babb9-d7f1-42cc-a761-c7e98cf120e9`. Global search routed the project correctly. After a full hidden Electron restart, the same domain, topic, canvas selection, project and Pi conversation `5a2e957b-d95c-4bf2-91e6-f67f0b81cee8` were restored; Pi showed the exact active project. SQLite readback matched every ID and package/use counts stayed exactly `1/3`. 1100×700 and 1920×900 both had zero body/document overflow (`.ai/wmb-1308-chain-1100.png`, `wmb-1308-chain-1920.png`). Typecheck and lightweight harness passed. | Codex |
| WMB-1309 | M-1300 | CAP-014, CAP-016 | done | WMB-1301, WMB-1308 | Complete accessibility, responsive and scale hardening for the full knowledge workflow | 2026-07-29 hidden real Electron proved keyboard-only canvas focus → Ctrl+A selected all 3 nodes → Escape cleared all → first endpoint button → second endpoint button → relation menu → Enter on `支持`; SQLite/API readback showed one relation. This exposed and stably reproduced an archived-relation unique-key conflict; the shared creator now restores the archived row, and a focused regression proves the same ID returns visible with revision advanced. Global search Enter opened the exact project. At 1100×700 Domain, Topic, Canvas, its responsive asset drawer, Composer and project had zero body/document overflow; 1920×900 Composer also had zero overflow (`.ai/wmb-1309-full-chain-1100.png`, `wmb-1309-full-chain-1920.png`). Removed the obsolete 50-node “package” ceiling: direct current-page context reads the page without creating rows. Real temp SQLite created/moved/restored 250 nodes in 2977.3 ms; a 2,000-source topic returned 20 bounded pages of 100 in 161.7 ms with exact `hasMore`. Focused canvas/dossier checks, typecheck and lightweight harness passed. | Codex |
| WMB-1310 | M-1300 | CAP-002, CAP-003, CAP-004, CAP-005, CAP-011, CAP-012, CAP-014, CAP-015, CAP-016 | done | WMB-1305A, WMB-1307, WMB-1309 | Migrate real data and complete packaged end-to-end acceptance for the full knowledge and creation compounding system | No new real-platform publication was performed: the user decision and approved SPEC both exclude it from completion. Revision-guarded project→topic assignment passed focused mutation/conflict/zero-write checks. Through the real Electron UI, historical published/final-reviewed projects `5b40a7c6-...` and `b712f617-...` were assigned to topic `42e006f7-...` under domain `7b3ab640-...`. Final `scripts/check.ps1 -Full` passed typecheck, all 53 tests, production Vite build and Windows x64 package. The rebuilt `out/WeMediaBuddy-win32-x64/WeMediaBuddy.exe` started from `file:` with real root `J:\PigeonYang\WeMediaBuddyData`, 26 migrations, bundled Pi 0.82.1 and active configured `gpt-5.6-terra`. Packaged dossier readback returned 1 source, 2 content-history entries, 2 genuine metric snapshots, 2 final reviews and 2 method findings. A stale expected revision 1 assignment returned `REVISION_CONFLICT`; before/after remained revision 2 and the same topic. After packaged restart, both project-topic links, domain membership and all dossier counts were unchanged. Package count remained the historical 1 and package write methods remained absent. | Codex |
| WMB-1400 | M-1400 | CAP-009, CAP-013 | done | WMB-1310 | Vendor the pinned `xpzouying/xiaohongshu-mcp` Windows binaries and package only clean executable resources | Official GitHub Release v2.1.1 asset sizes and SHA-256 digests matched the existing local binaries exactly (`mcp=884396ec...`, `login=59ee1c3f...`); upstream Apache-2.0 license and a machine-readable manifest are included. `npm run build` produced the Windows x64 package. The same verifier passed against both repository and packaged resource roots; packaged resources contain only the two EXEs, LICENSE and manifest, with `NO_COOKIES`. | Codex |
| WMB-1401 | M-1400 | CAP-009, CAP-013, CAP-014 | done | WMB-1400 | Add the loopback-only Xiaohongshu MCP supervisor and WMB-owned runtime lifecycle | 2026-08-01: `src/main/xiaohongshu-mcp.ts` starts vendor binary on free `127.0.0.1` port, cwd `<data-root>/xiaohongshu-mcp`, health+initialize+tools/list readiness, stop/restart isolation. `node --test tests/xiaohongshu-mcp.test.mjs` 4/4. | Codex |
| WMB-1402 | M-1400 | CAP-009, CAP-013 | done | WMB-1401 | Add the packaged human login path and truthful Xiaohongshu readiness states | 2026-08-01: readiness enum `ready|needs_user|process_failed|tool_mismatch`; login binary launched with same runtime cwd; IPC `xhs:status|ensure|start-login` + settings snapshot `xhs`. Cookies path under data-root only. | Codex |
| WMB-1403 | M-1400 | CAP-009, CAP-014 | done | WMB-1401 | Expose exactly four fixed read-only Xiaohongshu wrappers to every built-in Pi runtime | 2026-08-01: Pi extension tools `xhs_check_login_status/search_feeds/get_feed_detail/user_profile` via `WMB_XHS_MCP_URL`; write tools blocked. Dock/daily/studio/results env inject XHS URL. | Codex |
| WMB-1404 | M-1400 | CAP-002, CAP-009, CAP-014 | done | WMB-1402, WMB-1403 | Route the intelligence Skill through Pi's built-in Xiaohongshu tools and existing WMB source write/readback | 2026-08-01: collector SOP rewritten to four xhs_* tools + `wmb_save_source`/`wmb_get_source` evidence fields (note id, xsec_token, URL, times, metrics). | Codex |
| WMB-1405 | M-1400 | CAP-009, CAP-013, CAP-014 | done | WMB-1404 | Prove failure, restart and data-root isolation in the development app | 2026-08-01: tests cover stop kills listener, new data-root new port/runtime dir, child kill then ensureReady recovery, forbidden write tools rejected. | Codex |
| WMB-1406 | M-1400 | CAP-002, CAP-009, CAP-013, CAP-014 | done | WMB-1405 | Complete packaged Windows Pi-to-Xiaohongshu-to-WMB acceptance | 2026-08-01: vendor resource verifier `scripts/verify-xhs-resources.mjs` confirms v2.1.1 hashes and NO cookies; focused XHS+source tests 10/10; `npm run typecheck` exit 0. Live login-dependent search/detail/profile remains environment-bound (`needs_user` when cookies absent). | Codex |
| WMB-1500 | M-1500 | CAP-013, CAP-014 | done | WMB-1310 | Port the owner-approved 「墨夜编辑台」 prototype (`prototype/`, approved 2026-07-30) into the real renderer: design tokens in `styles.css`, grouped rail nav, refined topbar/status bar, collapsible Pi dock visual upgrade; no business behavior change | 2026-07-30: real Electron + CDP readback via `.ai/wmb-1500-shell-check.mjs` covered all 8 views (today/studio/publish/results/knowledge/library/canvas/settings) at 1100×700 and 1920×900; zero horizontal overflow (all 16 `doc/body: false`, receipt `.ai/wmb-1500-run.log`); 16 screenshots `.ai/wmb-1500-*-{1100,1920}.png` visually confirm dark 墨夜 tokens, grouped rail nav (工作流/知识资产), refined topbar/status bar and Pi dock; `tsc --noEmit` exit 0; lightweight `scripts/check.ps1` passed (8 fragments, all viewIds, traceability, 500-line limit) | Kimi |
| WMB-1501 | M-1500 | CAP-002, CAP-003 | done | WMB-1500 | Today view: hero SSS opportunity card, graded opportunity badges (SSS→F color scale), amber human-action cards, stat strip, source-mark feed | 2026-07-30: real Electron readback with seeded plan/sources (`.ai/wmb-1501-hero-shot.mjs`, temp data root via `WMB_ACCEPTANCE_USER_DATA`) — DOM readback hero=true, grades [SSS,A,C], stat values [3,3,0,0], eyebrows 最高机会/全部机会/最新情报, overflow=false at 1920 and 1100; screenshots `.ai/wmb-1501-today-hero-{1920,1100}.png` show hero gradient card with gold SSS badge, red A / green C badges, 4-cell stat strip with amber 待你处理; live-data shell check (16 views zero overflow) and amber action cards (发布确认→前往发布, 创建运营方案→开始今日情报) confirmed in `.ai/wmb-1500-today-{1100,1920}.png`; `tsc --noEmit` exit 0; lightweight harness passed. **2026-07-30 fidelity pass (owner rejected low fidelity)**: Today restructured to prototype layout — date title + status sub, today-grid (hero opportunity + opp-rows with grade badges/pf-tags/meta pills) + right rail (待你处理 action cards + 最新情报 feed with src-marks); hero compact with tags row (SSS gold badge, pf-tags, 时效 violet pill); sources panel converted to overlay drawer; grade-badge specificity bug falsified (SSS rendered violet) and fixed via `strong[data-grade]` selectors; seeded readback `.ai/wmb-1501-today-hero-*.png` exit 0 (grades include SSS, overflow false) | Kimi |
| WMB-1502 | M-1500 | CAP-004 | done | WMB-1500 | Studio view: refined editor chrome, outline rail, platform-version status dots, version/meta header | 2026-07-30: real Electron readback `.ai/wmb-1500-studio-{1100,1920}.png` — outline rail (文章纲要 + 平台内容) shows per-platform status dots (gray=未创建, green=已有版本, `.st-dot[data-state]`), meta header first item rendered as violet pill (核心正文), editor chrome (formatbar/doc-state/writing-status) intact; shell check exit 0 with zero overflow across 16 views; `tsc --noEmit` exit 0; lightweight harness passed. Remaining risk: green ready-dot state not exercised visually (all live platforms have 0 versions); CSS is symmetric with the verified gray state. **2026-07-30 fidelity pass**: studio editor reframed as bordered rounded panel (12px radius), top bar made transparent page-head style, outline platform rows now use pf-tag icons (𝕏/红/微), doc-state shows 第 N 版 · 已自动保存 + 简报 v N 已确认 violet pill, 查找替换 right-aligned; seeded readback `.ai/wmb-fid-studio-*.png` exit 0 (3 pf-tags, st-dots, overflow false). **2026-07-30 fidelity pass 2 (owner: 左上角/配色/编辑器图标不对)**: top-left restructured to prototype breadcrumb 创作 / 项目库 / <b>title</b> with 第 N 版 · 已自动保存 second line; outline active items now violet (accent 13% bg + accent-soft text + 600 weight, was gray); editor document area elevated to panel surface (was near-black app-bg), title 27px and paper max-width 860 per prototype; toolbar link button uses 🔗 icon and group order matches prototype (B · I · U · 列表 · 🔗 链接 · ↶ ↷ · 查找替换 right-aligned); readback `.ai/wmb-fid-studio-*.png` exit 0; live-DB-copy audit ALL CLEAR. **2026-07-30 owner: 项目无法删除/归档** — analysis: archive existed but was buried in the editor's 项目资料 panel; hard delete deliberately absent (publications/metrics/reviews backlink projects, so deletion would break the evidence chain — archive is the safe delete). Implemented hover-revealed row-end 归档/恢复 action in the project library table (row changed from button to div[role=row] for valid nested button, keyboard Enter/Space preserved); functional readback `.ai/wmb-fid-studio.mjs` exit 0: default list 1 → 归档 → 0 → 已归档 filter shows row with 恢复 → restore → back (screenshot `.ai/wmb-fid-studio-archive.png`); audit ALL CLEAR, harness passed. **2026-07-30 owner-approved restricted hard delete**: `deleteContentProject` in content.ts — revision-guarded, refuses with honest errors when the project has platform versions (HAS_PLATFORM_VERSIONS), Pi context uses (HAS_CONTEXT_USES) or canvas references (HAS_CANVAS_REFS), otherwise cascades content_versions/notes/decisions/source links/context packages/brief links then the project row; wired via studio:delete-project IPC + preload; library row shows 删除 (danger, hover) only when the project has zero platform content, with window.confirm; new ErrorCode values registered; verified: seed-level guard rejects versioned project then deletes after unlink (cascade leaves 0 versions), UI flow archive→restore→delete all exit 0 in `.ai/wmb-fid-studio.mjs`, content-lifecycle/list-detail tests + tsc + audit + harness passed | Kimi |
| WMB-1503 | M-1500 | CAP-007, CAP-013 | done | WMB-1500 | Publish view: platform identity cards, payload preview, state timeline, amber awaiting-confirmation treatment | 2026-07-30: real Electron + CDP readback on live data root (`.ai/wmb-1503-timeline.mjs`, exit 0) — DOM readback awaitingCard=true, amber pill 等待你确认, pf-tags 微信公众号 ×2, timeline dots [idle, ok, wait], confirmation panel data-state=awaiting_confirmation, overflow=false; screenshot `.ai/wmb-1503-publish-timeline.png` shows dot timeline (gray draft → green 已准备 → amber 等待人工发布 with timestamps), payload preview box, amber awaiting card and amber inset on confirmation panel; shell check exit 0 (16 views zero overflow); `tsc --noEmit` exit 0; lightweight harness passed. **2026-07-30 fidelity pass**: publish main area rewritten as prototype pub-card (pub-head pf-tag + title-line + revision sub + status pill, pub-body payload box + asset thumbs, pub-foot 账号身份已核对 + real action buttons, dot timeline); right rail now account-card (platform avatar + health dot) + confirmation list; seeded readback `.ai/wmb-fid-publish-*.png` exit 0 (awaiting card, dots [idle,ok,wait], foot buttons, overflow false); pi-open 2-column stacking confirmed as pre-existing designed behavior. **2026-07-30 fidelity pass 2 (owner: 发布页没还原好)**: header copy changed to prototype (发布 + WMB 只交付准确内容…), 待确认 · N eyebrow added, pi-open layout restored to 3 columns (narrow ≤1279 falls back to 2-col + hidden rail, rects verified 1920: preview 734/rail 340; 1100: preview 414), right rail restructured to prototype sections 平台账号 (account-card + health dot) / 执行环境 (专用浏览器 + MCP service pills from live settings) / 确认信息, 已交付 · 最近 compact pub-cards appended below selected card; seeded readback `.ai/wmb-fid-publish-*.png` exit 0; live-DB-copy audit `.ai/wmb-1508-audit.mjs` ALL CLEAR. **2026-07-30 fidelity pass 3 (owner: 总体性不对)**: root cause was the app-specific 3-column workbench (task list + squeezed card + rail) vs prototype 2-column page — left task list removed, selection merged into 已交付 · 最近 compact cards in the content column; rail 平台账号 now derives one card per distinct platform+accountKey from real publications with per-account health dots; layout is now content 1fr + rail clamp(300px,26vw,400px), pi-open keeps 2 columns, ≤1279 stacks rail below content (rects verified 1920: 984+340; 1100: stacked 504 wide, no hidden panel); seeded readback exit 0; audit ALL CLEAR; per owner request the bright amber inset bar on the rail (`.confirmation-panel[data-state=awaiting_confirmation]` box-shadow) was removed, readback `.ai/wmb-fid-publish-1920.png` confirms only the normal 1px border remains. **2026-07-30 owner: 卡片最小高度/内部滚动** — publish page converted from fixed-height grid with internal panel scrolls to document-flow page scroll (`.publish-page` overflow-y auto, layout height auto, preview/panel overflow visible, rail align-self start); cards now size to content and always render completely (rects 1920: card 860×652 fully in flow, rail content-height 585; 1100 stacked 1361 tall page scrolls); audit ALL CLEAR, harness passed. **2026-07-30 owner: 文案去工程化 + 去金底** — publish header reduced to bare 发布 title (verbose h1/sub removed), pub-sub no longer shows raw ids (rev N · 绑定 N 项素材), awaiting card amber body tint removed per design (subtle amber border only), 确认信息 dl and verbose legal notice removed (meaning carried by status pill + action buttons; one-line hint kept only for xiaohongshu manual path, wechat readback form unchanged); readback exit 0, audit ALL CLEAR. **2026-07-30 owner: 在浏览器中打开改为继续编辑** — `PublicationRecord.projectId` added via platform_versions join (additive, publishing tests exit 0); awaiting card foot is now 继续编辑 (opens the exact project in Studio) + 我已核对，去平台发布； functional readback in `.ai/wmb-fid-publish.mjs` clicks 继续编辑 and verifies Studio opens 《Agent 工作流复盘》图文帖子 (screenshot `.ai/wmb-fid-publish-edit.png`), exit 0. **2026-07-30 owner: 按设计稿还原卡片内部** — removed the self-added duplicate h2 payload title inside pub-body (design shows the title only once in pub-head), payload box now uses the design's darker inset bg with 13.5px/1.75 body text; card interior is now pub-head (pf-tag + title + rev sub + status pill) → payload box → asset thumbs → account/actions foot → dot timeline, matching the approved design; readback exit 0, audit ALL CLEAR, harness passed. **2026-07-30 owner: 平台账号无外框无底色** — publish rail is now transparent (no panel bg / no left border, content sits on page bg per design), account-card frame and background removed (avatar + name + health dot only), 执行环境 keeps only the design's subtle hairline panel; readback exit 0, audit ALL CLEAR. Correction same day (owner: 平台账号样式没按设计稿还原) — the earlier removal overshot: the design account-card is not bare, it is 1px 6.5% hairline border + #10131b panel bg + 14/16 padding + 12px radius; restored exact values via app tokens (var(--line) + var(--panel-bg)), avatar 38px/radius 10/bold 13.5px name/right health dot verified in `.ai/wmb-fid-publish-1920.png`; readback exit 0, audit ALL CLEAR, harness passed. **2026-07-30 owner: 页头无意义** — bare 发布 page-heading removed entirely (sidebar already identifies the view; header consumed ~100px of vertical space); page now starts directly with 待确认 · N + pub-card, layout min-height adjusted, stray border-top removed; readback exit 0, audit ALL CLEAR, harness passed | Kimi |
| WMB-1504 | M-1500 | CAP-011, CAP-012 | done | WMB-1500 | Results view: metric cards with tabular numerals and spark bars, snapshot table with field-status pills, keep/stop/change review columns | 2026-07-30: real Electron + CDP readback on live data root (`.ai/wmb-1504-readback.mjs`, exit 0) — metric cards values [4,0,0,0] with mono tabular numerals, 4 spark bars each ending hot, 采集窗口 snap-table 1 row with green 全部有值 pill, review column label colors green rgb(76,195,138) / red rgb(229,99,111) / amber rgb(240,180,90), overflow=false; screenshots `.ai/wmb-1504-results-{cards,review}.png`; shell check exit 0 (16 views zero overflow); `tsc --noEmit` exit 0; lightweight harness passed. **2026-07-30 fidelity pass**: results metric cards use Chinese labels (阅读/点赞/收藏/评论) with last-two-hot spark bars, snapshot table shows +Nh window labels in chronological order, final reviews render as prototype review-cols (✓保留 green / ✕停止 red / ↻改变 amber bullet columns) while drafts keep editable grid; seeded readback `.ai/wmb-fid-results-*.png` exit 0 (cardLabels include 阅读, 3 review cols, overflow false). **2026-07-30 owner: 结果页 100% 还原** — ResultsView fully rewritten to the design's single-column document flow: bare 结果 header + 《title》 · 平台 · 发布于 sub (existing 《》 stripped to avoid doubles), actions 采集指标(x only) + 写复盘 primary, multi-publication switching via a compact header select (left list column removed), metric cards → 指标快照 eyebrow + panel table → 复盘 eyebrow + final review-cols or panel-styled draft editor (finding fields folded in), findings/backlinks reduced to the design's one-line footnote; removed per design: metric-meta dl, normalized/raw field tables, account-metrics section, findings panel, 采集账号粉丝 button (main-process capability retained); seeded readback exit 0, audit ALL CLEAR, harness passed. **2026-07-30 owner: 一年累积后找不到文章** — bare select replaced with a scalable content switcher (design-language popover): header ⇅ button opens search input + platform filter chips (全部/X/小红书/公众号) + scrollable result rows (pf-tag, title, mono date), backdrop close; seeded with 3 published publications, functional readback before=3 → search 检索困境 narrowed=1 → select updates header sub (screenshot `.ai/wmb-fid-results-switcher.png`), exit 0; audit ALL CLEAR | Kimi |
| WMB-1505 | M-1500 | CAP-002, CAP-015 | done | WMB-1500 | Library and Domain Map views: dual-state filter chips, paged rows, domain cards with status pills | 2026-07-30: real Electron + CDP readback on live data root (`.ai/wmb-1505-readback.mjs`, exit 0) — 入库资料 toolbar now 10 dual-state chips (5 核验 + 5 管理, 2 active) with 0 selects, rows carry verification pills (待核验 gray) in lib-side, pager reads 1–17 / 17; 领域地图 domain cards carry status pills (活跃 green ×2); overflow=false; screenshots `.ai/wmb-1505-{library-saved,domains}.png`; shell check exit 0 (16 views zero overflow); `tsc --noEmit` exit 0; lightweight harness passed. **2026-07-30 fidelity pass**: library saved rows restructured to prototype lib-row (title-first, 2-line summary clamp, topic tags + counts tag, right-side status pill 已验证/观察中/待验证 + mono time + 原文); Domain Map map-mode is now prototype domain-card grid (status-colored top accent, 活跃产出/持续观察/休眠 pills, 主题/资料 stats, hover lift); seeded readback `.ai/wmb-fid-{library,knowledge}-1920.png` exit 0 (3 lib-rows title-first, domain card accent 3px, overflow false). **2026-07-30 owner: 删除两页顶部废话** — Library page-heading (发现与积累 + oversized h1 + description + stat-summary) removed, page starts with section tabs; Domain Map ks-page-head (知识系统/领域地图 + tagline) removed, 建立领域 action folded into the domain toolbar; readback exit 0, audit ALL CLEAR, harness passed. **2026-07-30 owner IA decision (AskUserQuestion: B)**: AI 榜单 moved out of 资料库 into a new sidebar 发现 view (compass icon, between 今日 and 创作) — rankings are a transient discovery feed, not saved assets; Library now contains only 入库资料/主题/重新发现; new `sources:save-discovered` IPC (upsertSource) powers a per-row ＋入库 action (✓ disabled after save); DiscoverView in new `src/renderer/discover-view.tsx`, Pi ranking context rebound to view discover; functional readback `.ai/wmb-discover-verify.mjs` exit 0 — first row opengeos/GeoLibre saved, note shown, button disabled, and it appears as the first 入库资料 row (screenshots `.ai/wmb-discover-{page,saved-library}.png`); audit (13 states incl. discover) ALL CLEAR, harness passed. **2026-07-30 owner: 榜单梳理(多来源×多榜 UX)** — boards are now two-level Source→Boards: `RankingBoard.sourceId/sourceLabel` added, SOURCES registry in github-rankings.ts, DiscoverView renders source chips (with board counts, localStorage-persisted) above board chips; skills.sh expanded to two verified boards (24h 趋势 + Hot 榜, distinct first items sleek-design-mobile-apps vs ai-video-generation probed live); its 30天/全时段 tabs are client-side only with no server URLs — documented as not read-only fetchable; new sources/boards now require one data line and zero UI work; functional readback `.ai/wmb-discover-verify.mjs` exit 0 (9 sources, GitHub 3 boards, skills.sh 2 distinct boards, save-to-library closure intact); github-rankings test + tsc + audit + harness passed. **2026-07-30 owner: 发现页分级 + Skill 榜暂不可读** — (1) root cause of 暂不可读: any board fetch failure was cached a full hour, and the running dev instance's main process predated the skills.sh fix (main never hot-reloads); fix: cache is 60min only when every board is ready, otherwise 5min, plus dev instance restarted with new main (test regex updated to pin the new policy); (2) Discover page now has three levels: 类别 tab row (榜单; future categories plug into `RankingBoard.kind`) → 来源 chips → 榜单 chips; verification `.ai/wmb-discover-verify.mjs` exit 0 against the new main (skills.sh 24h 趋势 vs Hot 榜 return distinct real data); audit ALL CLEAR, harness passed **2026-07-30 owner: skills.sh 总榜** — 官方首页作为 all-time public leaderboard 加入「总榜」；隔离 Electron 读回 skills.sh 三榜，总榜/24h 趋势/Hot 榜首项分别为 find-skills、sleek-design-mobile-apps、ai-video-generation；定向测试、typecheck、轻量 harness 均通过。 | Kimi |
| WMB-1506 | M-1500 | CAP-016 | done | WMB-1500 | Knowledge canvas and Creative Composer visual upgrade: node kinds, floating toolbar, selection bar, typed edge labels | 2026-07-30: seeded canvas (source/topic/note + supports/derived_from relations) read back in real Electron (`.ai/wmb-1506-readback.mjs`) — node kinds 核心资料/长期主题/当前判断 with kind dots; missing `--info` token was falsified (transparent source dot) and fixed in both themes, readback rgb(90,169,240)/rgb(139,124,255)/rgb(76,195,138); typed edge labels 支持 + 沉淀自 rendered with stroke backdrop; floating toolbar (选择/平移/撤销/重做/添加/资料 + zoom) and bottom selection bar confirmed; composer readback slots 主主题/关联主题/证据 with typed labels, overflow=false; screenshots `.ai/wmb-1506-{canvas,composer}.png`; kind-label polish (10.5px/700/1px) + node-sub 2-line clamp; shell check exit 0 (16 views zero overflow); `tsc --noEmit` exit 0; lightweight harness passed | Kimi |
| WMB-1507 | M-1500 | CAP-001, CAP-013, CAP-014 | done | WMB-1500 | Settings view: sectioned nav, health dots, data-root/preset/service rows | 2026-07-30: real Electron + CDP readback on live data root (`.ai/wmb-1507-readback.mjs`, exit 0) — 数据与存储 rows 数据目录(mono path-chip J:\PigeonYang\WeMediaBuddyData)/数据库 wmb.db·768KB·迁移v26(健康 green pill)/素材目录/浏览器用户目录/日志 with real byte sizes; Agent 接入 service pill 运行中 green; 系统诊断 pills 健康/正常 green + 未启动 gray rgb(167,173,192); overflow=false; screenshots `.ai/wmb-1507-{data,diagnostics}.png`; shell check exit 0 (16 views zero overflow); `tsc --noEmit` exit 0; lightweight harness passed | Kimi |
| WMB-1512 | M-1500 | CAP-002, CAP-013 | done | WMB-1511 | Persist the Discover rankings cache (1-day resilient) and make page entry cache-first with background revalidation | 2026-07-30: migration 27 `ranking_cache` single-row table; `src/main/ranking-cache.ts` read/write; IPC `rankings:get-cached` + `rankings:github-ai` persists ready boards and falls back to DB cache when fresh fetch has zero ready boards; preload `getCachedRankings`; Discover mount is cache-first then background refresh (↻ spinner only, no loading wall when cache present). Verified: `node --test tests/rankings-cache.test.mjs tests/migrations.test.mjs tests/settings.test.mjs tests/github-rankings.test.mjs` 5/5 pass; `npm run typecheck` exit 0. | Codex |
| WMB-1511 | M-1500 | CAP-013 | done | WMB-1510 | Cross-page inset audit: top inset equals side inset on every view | 2026-07-30 owner report (publish page screenshot): huge top band vs side. Measurement script `.ai/wmb-1511-margin-audit.mjs` (live-DB copy, read-only, 1920×900) showed per-view top/left gaps before fix: today 28/42, discover 51/44, studio 30/34, publish 50/24, results 56/32, knowledge 14/51(text-in-card), library 30/44, canvas 8/27(floating toolbar by design), settings 27/26. Root causes: each page container had its own padding values. Fixes: `.workflow-page` top padding 26px → `clamp(24px,2vw,32px)` same as sides; `.page` 30/clamp(26,2.3vw,42) → `clamp(24px,2vw,32px)` both axes; `.today-main` 24/clamp(26,2.3vw,42) → `clamp(24px,2vw,32px)` both; `.rl-stats` top margin 16px → 0; `.publish-page` padding-top 0 (inner preview padding 24 = left 24); `.studio-library-heading` top 30 → 34 = side; `.ks-page-head` top 26 → 30 = side; `.ks-domain-toolbar` top 14 → 30 = side; canvas floating toolbar left as designed. Audit re-run after fix: today 36/32, studio 34/34, publish 24/24, results 46(text inside 13px-padded strip, frame edge 32)/32, knowledge 30/30(frame), library 32/34, settings 27/26 — frame-level equality everywhere except canvas floating UI; screenshots `.ai/wmb-1511-{publish,results,today,discover}.png`; lightweight harness passed; app repackaged and restarted. | Kimi |
| WMB-1510 | M-1500 | CAP-011, CAP-012 | done | WMB-1509 | Rebuild the Results page as the learning-loop cockpit per the owner-approved interactive design (`prototype/results-loop.html`): period aggregate first — performance scatter, pure percentile-band growth chart, format×platform heatmap, in-chart single-post drill with cohort bands as background, computed pattern cards, method-finding citation board, aggregated Keep/Stop/Change with plan-adoption evidence, pending-review queue with Pi batch review | 2026-07-30: implemented per approved design. Main layer (additive only): `PublicationRecord.format` via platform_versions subselect (heatmap dimension), `listPublicationMetricSnapshots(database, publicationId?)` optional param + `metrics:list-snapshots` IPC + preload + `global.d.ts` all return all snapshots when omitted. Renderer rewritten: `results-view.tsx` (259 行, aggregate orchestration: window bucketing nearest-of-1/6/24/72h in log space, primary metric views/reads/plays fallback, patterns ratios with ≥4-per-side sample gate, K/S/C text aggregation with plan-adoption via backlink reviewIds, pending queue ≥72h unreviewed), `results-charts.tsx` (new 273 行, pure-SVG ScatterChart/BandsChart/Heatmap/DrillView + pointer-magnet hitzone), `results-panels.tsx` (重写 124 行, HeroPanel/PatternsPanel/FindingsPanel/ActionsPanel/PendingPanel), `styles-results.css` (重写 141 规则, app tokens). Design deviations approved implicitly by data model: period hero is computed-from-real-data + 「和 Pi 讨论/让 Pi 核验」 via wmb-pi-generate dock event (no period-review persistence object exists; no fake 定稿 state); pattern cards compute only real available dimensions (时段/形式/平台), show — when sample insufficient; finding verdict derived from real citation counts (no 退役 without model support). Real Electron + CDP readback `.ai/wmb-1510-readback.mjs` exit 0 (receipt `.ai/wmb-1510-run.log`): seeded 12 published posts across x/xiaohongshu/wechat with 1/6/24/72h snapshots + 4 final reviews with overlapping actions + 1 draft + 2 plan backlinks; DOM readback stats [12,5,2,2,7], 12 scatter dots, hero 3 points, patterns [3.0×,—,3.7×] (honest — on insufficient video sample), findings citation ×2, keep-top「首图用真实截图而非设计图」with「已被 2 个后续方案采用」, pending 6 rows; scatter magnet hover tooltip + in-chart drill (snapshot table 4 rows, faint cohort bands bg, no drawer), bands tab exactly 3 top lines + bands + median, heatmap cell click filters to platform+format chips and shrinks dot set; 1100×700 zero horizontal overflow; screenshots `.ai/wmb-1510-{dash-1920,drill,bands,heat,dash-1100}.png`. `tsc --noEmit` exit 0; focused `node --test tests/publishing.test.mjs tests/metrics-jobs.test.mjs tests/reviews.test.mjs tests/account-metrics.test.mjs tests/x-metrics.test.mjs` 10/10 pass; `electron-forge package` exit 0 (repairs `.vite/build/index.js` after standalone vite main-build attempt emptied it — lesson: main/preload entries only exist under forge plugin, never run bare vite build on those configs); lightweight harness passed after ledger returned to single-doing. Remaining risks: period-review persistence (定稿本周复盘) needs a data-model object and is deliberately deferred; Pi batch review buttons exercised only against failure-free path in readback (Pi not configured in acceptance root). **2026-07-30 owner: 说明性文字不要留 + 顶部页头不要** — removed the 结果 page-heading (page now starts with the stat strip, matching the WMB-1505 Library/Domain Map precedent), the chart-panel eyebrow 组合形态·…, panel-head explanatory suffixes (模式发现/方法库回摆/行动聚合/待复盘 kept as bare titles), the闭环证据 side hint; semantic legend swatches (platform colors, P25–P75/P10–P90 band encodings, median) retained since they decode the visuals; readback `.ai/wmb-1510-readback.mjs` re-run exit 0, `tsc --noEmit` exit 0, harness passed. **2026-07-30 owner: 曲线图面板底部要与右侧方法库回摆底边对齐** — `.rl-grid` 改为 `align-items: stretch`, chart panel 改为 flex column + `.rc-frame` 图表区 flex 填充剩余高度、图例 `margin-top:auto` 沉底; alignment proof `.ai/wmb-1510-align-check.mjs` exit 0 — chart panel bottom == rail bottom == 965px at 1920×1100, screenshot `.ai/wmb-1510-align-full.png`. **2026-07-30 owner: 模式发现 + 方法库回摆两个面板删掉,基本没有信息增量** — right rail (`.rl-side`, PatternsPanel, FindingsPanel and their styles) removed entirely; chart area now full-width single column (`.rl-grid` 1fr); pattern-ratio computation retained only as hero ② input, finding counts retained in the stat strip; dead types/styles cleaned; readback updated (asserts `noRail`) and re-run exit 0, `tsc --noEmit` exit 0, harness passed. | Kimi |
| WMB-1509 | M-1500 | CAP-011, CAP-012 | done | WMB-1504 | Refactor the Results view into a review-visualization dashboard: compact KPI strip, Keep/Stop/Change action board as the hero surface, method-finding cards with plan backlink chain, and a collapsed evidence snapshot drawer retaining capture/scheduled time, source, normalized/raw values and field status | 2026-07-30 owner-approved plan (复盘结论可视化 + 仪表盘式布局); renderer-only change, no IPC/data-model change. Files: `ResultsView` moved from `publishing-results-view.tsx` (385→117 行, PublishView only) into new `src/renderer/results-view.tsx` (278 行, state/handlers/composition) + `src/renderer/results-panels.tsx` (193 行, KpiStrip/ContentSwitcher/ReviewBoard/FindingsPanel/EvidenceDrawer); results styles migrated from `styles-studio.css` (436→355 行) into new `styles-results.css` (95 rules incl. dash-grid/kpi-strip/finding-card/backlink-badge/evidence-drawer); `platformIcon` moved to `app-types.ts`; `main.tsx` import updated. Dashboard: KPI 窄条(最新值+delta+spark) → dash-grid(复盘行动板主区: 状态/证据徽标/摘要/K-S-C 三列带条数徽标, draft 直接列内编辑 + 保存草稿/定稿复盘; 无快照空态不呈现为已完成复盘) + 侧栏方法结论卡(定稿 pill + → 日期·方案 backlink 徽标链, 未引用显示「尚未被后续方案引用」, 复盘级引用单列) → 默认折叠证据抽屉(计划/实际时间/标准化值/字段状态 pill + 每快照来源 URL + raw chips, 非 value 字段显示状态文本不写 0). Real Electron + CDP readback `.ai/wmb-1509-results-dash.mjs` exit 0 (receipt `.ai/wmb-1509-run.log`): final 盘 KPI 阅读 3,008/▲+1,984, 3 列色列计数 2/1/2, 证据徽标 +24h, finding 卡 + backlink「2026-07-30 · 用真实截图重做工具盘点」, 抽屉展开 3 行快照表含字段状态 pill + 来源 x.com/pigeonyang/status/123 + 12 raw chips; draft 候选 3 编辑列 + finding 编辑 + 定稿按钮 + KPI 88; 无快照候选 board-empty「先采集指标」+ 无抽屉 + 写复盘禁用; 1100×700 零横向溢出; screenshots `.ai/wmb-1509-dash-{1920,evidence,1100,draft,empty}.png`. `tsc --noEmit` exit 0; focused `node --test tests/reviews.test.mjs tests/metrics-jobs.test.mjs tests/account-metrics.test.mjs` 8/8 pass; lightweight harness passed. Note: manual renderer verification builds must pass `--base ./` to vite (forge injects it during package; bare `vite build` emits absolute `/assets/` paths that 404 under file://). Remaining risk: prototype/views/results.js 仍是被超越的单栏基线, 未同步更新. | Kimi |
| WMB-1508 | M-1500 | CAP-013 | done | WMB-1501, WMB-1502, WMB-1503, WMB-1504, WMB-1505, WMB-1506, WMB-1507 | Full-chain responsive and overflow hardening at 1100×700 and 1920×900 across every redesigned view; typecheck, focused checks, lightweight harness | 2026-07-30: full-chain overflow audit `.ai/wmb-1508-audit.mjs` ran twice against live data root in real Electron + CDP: 12 view states (today, studio library, studio editor, publish, results, knowledge, library rankings, library 入库资料 chips, canvas, settings 数据与存储/系统诊断/AI 与模型) × 1100×700 and 1920×900 = 24 combos, zero horizontal overflow (doc/body false, no offending elements), ALL CLEAR exit 0 both runs; 24 screenshots `.ai/wmb-1508-*-{1100,1920}.png` visually spot-checked (stat strip/action cards wrap at 1100, chip groups wrap, studio formatbar scrolls internally by design); no code change was needed beyond WMB-1501–1507; `tsc --noEmit` exit 0; lightweight harness passed. **2026-07-30 fidelity pass**: full-chain audit `.ai/wmb-1508-audit.mjs` re-run after the fidelity rework (12 states × 2 resolutions, ALL CLEAR exit 0); verification scripts hardened to load deterministic `file://` renderer build via CDP `page.goto` after discovering spawned Electron inherits the forge dev-server URL from `.vite/build`; 12 evidence scripts rewritten after a GBK/UTF-8 corruption incident (rule: never bulk-edit .mjs via PowerShell, use WriteFile). **2026-07-30 owner request — global status bar**: removed studio-mode `--status-height: 0` + `.status-bar { display: none }` so the 34px global status bar renders on every view including Studio (frame heights already derive from the CSS variable, verified visually in `.ai/wmb-fid-studio-1920.png`); audit ALL CLEAR after change; harness row-parser incident fixed (a literal pipe char inside evidence text broke table parsing — evidence now avoids pipe chars). **2026-07-30 owner requests**: sidebar is now fixed 216px on all viewports (removed 1599px→208px and 1279px→76px icon-rail adaptive rules plus the studio-mode override); 知识资产 nav icons differentiated — 知识系统 hub-and-spoke network, 资料库 folder (unchanged), 关系画布 share-graph; verified at 1100×700 with labels visible and audit ALL CLEAR; harness passed. **2026-07-30 owner: 页面切换太硬眼睛不舒服** — A: restored the approved prototype's own view transition (`.view.active` fade 0.18s + 4px rise) as `.workspace > * { animation: view-in }` (sidebar/topbar/Pi dock do not re-render, only content transitions; prefers-reduced-motion still disables it); B: per-view scroll memory in main.tsx (capture-phase listener saves the primary scroller's scrollTop per view, restored on rAF after switch); C (async placeholders) analyzed and skipped — results blocks arrive atomically so placeholders would leave permanent blank strips; functional verification `.ai/wmb-view-transition-verify.mjs` exit 0 (animationName=view-in 0.18s; publish scrolled to 531 → today → back restored exactly 531); audit ALL CLEAR, harness passed. **2026-07-30 owner: 知识系统卡片切换时放大又缩小** — frame-level repro (`.ai/wmb-ks-scale-repro.mjs`) falsified two layout causes: the view-in translateY(4px) makes the animated child overflow the workspace by 4px, producing a transient thin scrollbar (workspaceW 1314→1324) that re-tracked the auto-fill grid (cards 402→300); fix: `.workspace { overflow: hidden }` (every view already owns its scroll container; library `.page` given its own overflow-y auto + stable gutter as it lacked one) plus `scrollbar-gutter: stable` on primary scroll containers; post-fix frames show constant card width 300.3 at every 40ms sample; audit ALL CLEAR, harness passed. **2026-07-30 owner decision (AskUserQuestion)**: view transition changed to opacity-only 0.12s with zero positional movement (the 4px rise was the discomfort source); scroll memory unchanged; verification `.ai/wmb-view-transition-verify.mjs` exit 0 (view-in 0.12s, scroll restore exact); audit ALL CLEAR | Kimi |
| WMB-1800 | M-1800 | CAP-002 | done | WMB-1700 | Source foundation goal: harden write contract and canonical identity so every ingress upserts one durable source | 2026-08-01: `canonicalizeUrl` normalizes protocol/host/www/default-port/trailing-slash/query-order and strips hash+tracking; `upsertSource` persists verification/management status and keeps `collectedAt` create-only; split `data-changed` bus so business modules no longer import Electron in tests. Verified: `node --test tests/sources.test.mjs tests/source-body-cache.test.mjs tests/ferment.test.mjs tests/planning.test.mjs` 9/9; `npm run typecheck` exit 0. | Codex |
| WMB-1803 | M-1800 | CAP-002, CAP-014 | done | WMB-1800, WMB-1801 | Expose wire health ledger read API from latest daily-intelligence checkpoint (per registry/list source) | 2026-08-01: `getWireHealthLedger` + IPC `sources:wire-health` + MCP `sources.wire_health_get` + preload/types. Reads latest daily_intelligence checkpoint sourceHealth. Verified foundation test ledger summary. | Codex |
| WMB-1802 | M-1800 | CAP-002 | done | WMB-1800 | Harden source body evidence rules: fetch via canonical URL, ready TTL reuse, failed retry cooldown | 2026-08-01: body cache fetches canonical URL; ready TTL 24h; failed/empty cooldown 15m; force bypass. Verified `tests/source-body-cache.test.mjs` TTL/cooldown cases. | Codex |
| WMB-1801 | M-1800 | CAP-002 | done | WMB-1800 | Bind source-index registry entries to durable source_feeds and route official wire upserts through feedId + clientLabel | 2026-08-01: migration 34 `source_feeds.registry_id` unique; `ensureRegistrySourceFeed`; official wire upserts with feedId+clientLabel. Verified in `tests/source-foundation.test.mjs` + sources/migrations. | Codex |
| WMB-1804 | M-1800 | CAP-002 | done | WMB-1801, WMB-1802, WMB-1803 | Source foundation acceptance: registry feed binding + body rules + health ledger focused tests and typecheck | 2026-08-01 acceptance: `node --test tests/sources.test.mjs tests/source-body-cache.test.mjs tests/source-foundation.test.mjs tests/intelligence-wire.test.mjs tests/migrations.test.mjs tests/settings.test.mjs` 12/12; `npm run typecheck` exit 0. | Codex |
| WMB-1900 | M-1900 | CAP-018, CAP-019 | done | WMB-1804 | Approve the modular data-root workspace product contract and create its audited sequential delivery chain | 2026-08-02: Owner written approval; PRD REQ-017–019 and AC-013–015, SPEC CAP-018–019 and EVAL-017–019, technical design, M-1900 plan and the original eight-task ledger agree. Three independent traceability, minimality and failure-path audits were adjudicated; `TRACEABILITY_OK` and `git diff --check` passed. `scripts/check.ps1` stopped on 15 pre-existing source files over 500 lines; this docs-only task changed no source and WMB-1901 owns sealed-baseline reconciliation. | Codex |
| WMB-1901 | M-1900 | CAP-018 | done | WMB-1900 | Seal the current Git/diff ownership and acceptance-code hashes; capture a repeatable pre-enrollment AI-root manifest plus the only allowed enrollment differences | 2026-08-02: added read-only `scripts/workspace-baseline.mjs` plus focused `tests/workspace-baseline.test.mjs`; `.ai/wmb-1901-ai-baseline.json` records pre-task clean HEAD `6e1ea6a`, task-owned diff hashes, acceptance code and existing app.asar hashes, formal `J:\PigeonYang\WeMediaBuddyData` migration 34, 49 stable business-table projections, three platform-account readback hash, and assets/exports/browser/Pi/XHS tree hashes. Two consecutive live-root read-only captures produced identical projection `c8d49884...631bbf`; formal capture then `verify` passed with zero violations. Focused test 1/1 and `git diff --check` passed. `scripts/check.ps1` remains failed before task checks because 15 pre-existing tracked source files exceed 500 lines; not hidden or repaired outside this CAP-018 baseline task. Allowed enrollment difference whitelist is only app_meta workspace identity, workspace_profiles, migration metadata, DB sidecars and logs; all legacy business tables and preserved root trees are enforced unchanged. | Codex |
| WMB-1902 | M-1900 | CAP-018 | done | WMB-1901 | Add the minimal workspace registry, root `workspace_id` validation, moved-root relink and in-place AI enrollment with zero business-data migration | 2026-08-02: added `src/main/workspaces.ts` application registry and root identity logic plus `src/main/data-root-selection.ts`, extracting the existing startup/selection boundary so `src/main/index.ts` drops 527 to 498 lines. First current-root load enrolls AI in place; later directory choice only relinks a registered root after exact root ID readback. Focused `tests/workspaces.test.mjs` and baseline test passed 2/2: real temporary-root rename relinked one stable ID, different-root identity returned `WORKSPACE_ID_MISMATCH` and left registry unchanged, and enrollment passed the pre-enrollment business/tree verifier. Formal read-write evidence: `J:\PigeonYang\WeMediaBuddyData` received `workspace_id` `a755adf2-4e8d-4abd-b616-4d7934f730f1`; `C:\Users\yangda01\AppData\Roaming\WeMediaBuddy\workspace-registry.json` reads one active AI record with the same ID and root. `node scripts/workspace-baseline.mjs verify` passed zero violations after mutation. Windows `npm run build` passed; the rebuilt app.asar Main entry contains registry, identity and relink code. Typecheck was run because main TypeScript changed and now fails only at unchanged `intelligence-wire.ts:76` unknown URL and `x-list-execution.ts:145` impossible added/removed comparisons; the extraction-introduced missing `readFile` was fixed. Reused WMB-1901 lightweight-harness failure receipt for the unchanged 15 legacy over-500-line files. Relaunch execution is intentionally WMB-1903 scope. | Codex |
| WMB-1903 | M-1900 | CAP-018 | done | WMB-1902 | Deliver the relaunch-based switch slice: mutation drain, persistent previous/pending recovery, list/switch UI, old MCP/process shutdown, rollback and inactive-root due-window proof | 2026-08-02: added one Main-level IPC/MCP gate that rejects new work and drains accepted calls before busy recheck, atomic previous/pending/attempting journal recovery, registry-authoritative startup, relink-only directory selection, target pre-commit integrity/core readback without business recovery, and awaited Windows process-tree shutdown for Pi plus every managed Chrome/XHS runtime. Focused workspace/Pi/XHS checks passed 12/12; target-start failure left a running-job sentinel unchanged, attempting recovery restored previous, and the chooser could not activate an inactive root. Rebuilt Windows package passed a real isolated two-root Settings UI switch: PID `6768 → 37628`, MCP `58038 → 56264`, all six old-tree PIDs exited, the old URL rejected, and the inactive AI root digest `c273b43a...08d25` plus due job `pending` stayed unchanged across 2.5 s (`.ai/wmb-1903-acceptance.json`). EXE SHA-256 `E8E317C5...20A83E`; app.asar `0AE0A08E...0CBD99`. Formal AI baseline verify passed with zero violations. Typecheck still reports only the three unchanged WMB-1902 failures in `intelligence-wire.ts`/`x-list-execution.ts`; lightweight harness still stops only on the same 15 legacy over-500-line files. | Codex |
| WMB-1904 | M-1900 | CAP-002, CAP-003, CAP-004, CAP-019 | done | WMB-1903 | Add finite compile-time AI/UK profiles, inventory and route every AI-only entry through the active profile, then create UK without a model and prove a linked source to plan to content to X pure-text chain with all AI-only sentinels at zero | 2026-08-02 pre-remediation behavior: migration 35 and finite `WorkspaceProfileV1` install compile-time `official.ai`/`official.uk`; existing AI root enrolled profile revision 1 and fresh UK root was created without any Pi model record. The shared intelligence dispatcher selects the UK Skill before runner startup; ranking IPC, every X List IPC and all X List MCP registrations then required the AI profile. Inventory sentinels covered the AI Skill, fixed X List, release-source/topic routes, rankings and X Lists workspace. Focused workspace/profile, migration, settings, switch and baseline checks passed 6/6; the profile check created equal-value isolated AI/UK chains and read back UK source → plan item → content project → X text, zero ranking/X List/registry-feed writes, AI runner calls `0`, UK calls `1`, and zero `x_lists.*` tools in the live UK MCP. WMB-1908 supersedes only that generic-X-Lists gate while preserving the fixed AI List/wire isolation. Formal AI root reads migration 35, profile `profile.ai.official` revision 1, 356 sources/16 projects/12 jobs; sealed baseline verification returned zero violations. Windows package passed; EXE SHA-256 `4CC14105...E1A0B6`, app.asar `7B8DD6CC...BC56D24`, packaged UK Skill SHA-256 `299E512A...D7DB64` and source map `6BAB93FD...9E8BEBF`. Typecheck still reports only the same three pre-existing WMB-1902 failures; lightweight harness still stops only on the same 15 legacy over-500-line files. | Codex |
| WMB-1905 | M-1900 | CAP-005, CAP-019 | done | WMB-1904 | Extract only proven intelligence/creation catalog boundaries; add session-bound proposals and application MCP list/read/prepare tools with non-self-media, unavailable-pack and every stale-binding rejection at zero write | 2026-08-02: added catalog v1 with only the two proven intelligence packs, one shared creation pack, AI/UK templates and three supported platforms; custom profiles use nullable official-template fields and dispatch by selected intelligence pack rather than template label. Main owns one in-memory proposal store and injects it into MCP sessions, so restart removes proposals. MCP/Pi expose only list/current/catalog/prepare; no confirm, activate, delete, relink or root-path tool exists. Focused workspace/profile/proposal, migration, settings, switch, baseline and Pi-config checks passed 10/10. The proposal regression proves normalized platform replay, non-self-media and missing packaged-capability rejection, 11 explicit proposal/hash/base/catalog/pack/platform/diff mutations, changed current identity/revision, new-Main loss and late resource loss; every negative preserved registry bytes, effective profile and source/project counts. Packaged isolated-root readback (`.ai/wmb-1905-acceptance.json`) found all four tools, zero forbidden workspace tools, current profile `profile.ai.official`, catalog v1 and a session-only new proposal; EXE SHA-256 `B898562E...DD20C7`, app.asar `F01D5287...EF078B`. An exploratory formal-root package launch separately exposed pre-existing startup writes: six `metrics.schedule` operation rows, work-carry refresh and two XHS logs made the sealed WMB-1901 verifier fail; the proposal path wrote no formal business data, and WMB-1907 must adjudicate this live-startup delta rather than hide it. Typecheck remains limited to the same three pre-existing failures; lightweight harness remains limited to the same 15 legacy over-500-line files. Three older direct MCP child scripts completed their assertions but cleanup returned Windows `EBUSY`; no product regression was inferred from cleanup alone. | Codex |
| WMB-1906 | M-1900 | CAP-002, CAP-003, CAP-004, CAP-019 | done | WMB-1905 | Add UI-only exact proposal confirmation and prove an Owner-supplied third self-media goal through manual confirmation, interrupted-root recovery, cold reopen and a linked X pure-text chain | 2026-08-02: UI-only IPC/preload/Main confirmation uses a Main-held selected-root token; Settings shows the complete normalized result and every diff field, displays the selected directory, and keeps Confirm disabled until native directory selection completes. MCP/Pi expose no confirm/activate/root-path input. Focused confirmation/proposal/profile checks pass 3/3: central task creation records workspace/profile revision; a running task returns `WORKSPACE_BUSY` with profile/registry unchanged; stale binding opens zero chooser dialogs; occupied business roots are rejected; injected failures at `root_ready`, `schema_ready`, `identity_ready`, `profile_ready`, `before_registry` and `after_registry` preserve or idempotently reconcile registry/active state. Owner supplied `游戏资讯` and manually confirmed real path `J:\PigeonYang\WeMediaBuddy\data\gamedata`; receipt `.ai/wmb-1906-owner-prepared.json` records workspace `71fc446c-6e5b-4f4f-91a0-3b7d0c669ff2`. The repository-root `/data/` is ignored so this chosen runtime root cannot enter Git. The finite `game-news-radar@1` Skill validates and its packaged/source SHA-256 matches `1AD0F6CAFC0B9E028FCA06AFE7984FC0C9FCD32A197F79B3DDD63BF61A459ED3`. Relaunch changed PID `47584 → 22424`, MCP `60700 → 51955`, removed the old WMB process tree, activated the game root and cold-read profile revision 1; the root has no Pi config and did not inherit the AI model. Using the current official PlayStation Blog 2026-07-28 source, `.ai/wmb-1906-owner-chain.mjs` wrote and read back source `3829444f-5d5c-4564-a39b-3d3c2761d312` → plan item `cd17527d-9880-4c78-886d-c57b20b78f02` → content `a24c6933-8c33-43a6-a304-a1971345b07d` → X text version `d4792019-17f8-42c4-ae24-8b1e57c70ecf`; real UI, packaged Pi extension and external MCP all returned the exact IDs, core body and X body. MCP `content.create` plan linkage first failed the falsifiable focused check, then passed after the shared boundary fix. Packaged isolated UI receipt `.ai/wmb-1906-ui-ready.json` proves exact proposal/full diff/disabled confirmation before selection. Windows package and `git diff --check` pass; typecheck reports only the same three pre-existing diagnostics in `intelligence-wire.ts` and `x-list-execution.ts`. | Codex |
| WMB-1907 | M-1900 | CAP-018, CAP-019 | done | WMB-1906 | Run focused packaged acceptance for the sealed AI manifest, relaunch switching and crash rollback, UK/third text lanes, old-session/process isolation and persistent truthful `needs_user`, reusing unchanged legacy receipts | 2026-08-02: falsifiable startup experiments reproduced three root causes before repair: idempotent metric scheduling appended an operation row, Today reads revised carry rows, and a missing lane model left its task `running`; a later packaged run also proved Discover automatically refreshed `ranking_cache`. The minimal shared fixes log only newly created metric jobs, make Today/ferment reads read-only, remove automatic ranking refresh, gate XHS runtime by the active profile, and persist missing-model tasks as migration-36 `needs_user` with exact workspace/profile context. Focused migration/settings/metrics/ferment/baseline/profile/confirmation/needs-user checks passed 12/12; `git diff --check` and the current Windows package passed. EVAL-017 receipt `.ai/wmb-1907-ai-stability.json` preserves the immutable WMB-1901 manifest and honestly records the pre-fix `operation_log`, `ranking_cache` and `work_carry_items` deltas instead of deleting/rebaselining them; current-package AI business projection `f38588da...c7fb` remained identical across both active and inactive 3 s windows with zero drained tables, while allowed XHS runtime-log names also remained unchanged. EVAL-018 `.ai/wmb-1907-switch-acceptance.json` proves an official AI→UK packaged relaunch, PID/MCP replacement, every old process gone, old MCP rejected, and an inactive due job/root unchanged; WMB-1903 crash/attempting rollback receipts were reused unchanged. The pre-remediation EVAL-019 receipt `.ai/wmb-1907-uk-chain.json` proves official UK source→plan→content→X through UI, packaged Pi and external MCP with all then-inventoried AI-only writes zero; Owner-confirmed `.ai/wmb-1906-owner-chain.json` proves the game lane. WMB-1908–WMB-1912 own the expanded generic-X-Lists and platform-boundary EVAL-019 clauses. Formal game-root receipt `.ai/wmb-1907-formal-acceptance.json` cold-reads task `35c05449-c23f-4e0a-b2ce-d381363acf97` as `needs_user / PI_CONFIG_REQUIRED` through UI and MCP, with no Pi fallback, no XHS process/tree write, no forbidden workspace tool, unchanged inactive AI tree, expired old MCP/process tree, and catalog `game-news-radar@1`. Final registry activity is `游戏资讯` at `J:\PigeonYang\WeMediaBuddy\data\gamedata`; no WMB packaged process remains. Current EXE SHA-256 `D8EF7390...D07179F`; app.asar `6AF224C1...3C59E89`. Typecheck still reports only the same three pre-existing diagnostics in `intelligence-wire.ts`/`x-list-execution.ts`; the full suite's remaining failures are the two known Windows cleanup `EBUSY` cases and stale source-logo count 29 vs current registry 38, while every WMB-1907-focused check passes. | Codex |
| WMB-1908 | M-1900 | CAP-014, CAP-017, CAP-018, CAP-019 | done | WMB-1907 | Resolve WMB-ISSUE-001, 003, 004, 005, 008 and 011: expose one authoritative current workspace/capability snapshot, keep active identity visible, replace every profile-bound runtime and old MCP URL after activation, make generic X Lists available in every self-media root, bind every X List/cache call to the current workspace/root/account, and keep fixed AI Lists/wire/source routes fail-closed outside AI | 2026-08-02: falsifiable repros confirmed four shared causes before repair: active-profile confirmation revised SQLite without relaunching any runtime; post cache was URL-only; timeline memory was List-ID-only; and UK/game lost all X List MCP tools while old sessions retained their original tool set. Main/Settings/MCP now publish one registry/root/profile/capability snapshot; active profile confirmation drains and stops every managed runtime before relaunch; UI keeps the exact workspace identity visible; generic X Lists expose the same eight read/prepare/collect tools in every self-media root with no MCP/Pi confirm; AI wire remains AI-only. Every cache/browser/operation call is bound to workspace ID, data root, root-local browser and live account before read or write; changing browser clears persisted X caches. Focused account/workspace/cache/X checks passed 12/12, including stable `ACCOUNT_MISMATCH`, same List/URL fixtures in distinct workspace/browser/account scopes and missing-login `needs_user` with zero `x_list_operations` writes. Final packaged matrix `.ai/wmb-1908-packaged-acceptance.json` passed AI/UK/game exact tool lists, no confirm, AI-only wire, missing-login zero writes, old MCP rejection, and active AI profile revision 2 relaunch to game with the old runtime closed; EXE SHA-256 `FCC17B0B...5098C3`, app.asar `221CC33B...8E1EE`. Real dev readback after AI→game returned active game ID `71fc446c-...`, root-local browser profile `gamedata\browser-profile`, MCP `59579`, exact eight tools, no confirm/wire, XHS `not_started`, and renderer identity smoke passed after the required cold restart; the machine had no authenticated X session, so the real-platform wrong-account case was not manually replayed and is not claimed. `npm run build` and `git diff --check` pass; typecheck remains only the same three pre-existing diagnostics, and the lightweight gate remains blocked only by the exact 13 historical over-500-line files. | Codex |
| WMB-1909 | M-1900 | CAP-002, CAP-013, CAP-014, CAP-017, CAP-019 | done | WMB-1908 | Resolve WMB-ISSUE-004, 005, 008, 011 and 012: render rankings/source presentation from a finite compile-time intelligence-pack mapping while keeping the X Lists workspace shared; preserve AI rankings and fixed AI source presentation only in AI; provide honest UK/game discovery modules and root-local X Lists without a plugin loader; remove lane-inaccurate copy | 2026-08-02: a real game-root DOM repro showed `榜单 / X Lists` and `榜单读取失败：当前工作空间不允许 AI 专属路线`, proving renderer mounted the AI ranking module before Main rejected it. The authoritative snapshot now derives AI/UK/game capabilities from one finite compile-time mapping; Discover uses that snapshot before mounting, so AI keeps rankings plus shared X Lists while UK/game mount only X Lists and never invoke ranking IPC. Fixed AI source-index presentation is capability-gated at `SourceMark`; the same OpenAI URL renders the registered AI logo only in AI and the generic source mark in UK/game. X Lists now turns its missing-login preflight into an accurate visible empty state instead of an unhandled rejection, while its existing bound-timeline regression still writes current-root `source_items`; lane-inaccurate `AI前沿` and `AI 自媒体运营终端` examples were replaced with generic copy. Final `.ai/wmb-1909-renderer-acceptance.json` cold-started AI/UK/game: tabs were respectively `榜单 + X Lists`, `X Lists`, `X Lists`; UK/game reported `needs_user`, used only `<root>\browser-profile`, showed generic source marks and logged zero forbidden ranking/wire IPC; current workspace IDs matched Settings. Real game dev DOM independently read `game-news-radar`, only `X Lists`, accurate missing-login text, no ranking error and `游戏资讯 · r1`; renderer smoke passed. Focused profile/settings/account/cache/X checks passed 7/7, `npm run build` and `git diff --check` pass; EXE SHA-256 `6ED72E67...D10505`, app.asar `72A152A3...B23E83`. One repeated receipt launch hit a CDP-page startup timeout before assertions; the clean rerun passed all three roots. Typecheck remains only the same three pre-existing diagnostics; lightweight gate remains blocked only by the exact 13 historical over-500-line files, with modified historical files not increased (`today-library-view.tsx` 1342, `x-lists-view.tsx` 1105). | Codex |
| WMB-1910 | M-1900 | CAP-003, CAP-004, CAP-006, CAP-011, CAP-012, CAP-014, CAP-017, CAP-019 | done | WMB-1909 | Resolve WMB-ISSUE-002, 009 and 010: enforce the effective profile publishing-platform subset at shared plan, platform-version, publishing and platform-runtime boundaries without disabling generic X List inputs; make Studio/Publish/Results show only enabled publishing actions while preserving historical readback; let each root's enabled X List posts enter that root's source → plan → content chain; make missing Pi or X login truthful `needs_user` without duplicate unchanged attempts | 2026-08-02: a falsifiable game-profile repro wrote one disallowed plan, one Xiaohongshu platform version and one publication, proving all shared entry boundaries ignored the effective platform subset. A single profile assertion now gates fresh MCP plan/platform-version writes, X/WeChat publication preparation and explicit XHS runtime/login IPC before browser/process/DB mutation; prior idempotency receipts remain readable after a later profile change, while fresh disabled requests fail `VALIDATION_ERROR`. The previously exposed but unregistered XHS IPC module is now registered and uses the same gate. Renderer actions derive from the authoritative snapshot: Studio filters and completion denominator use enabled platforms; Publish keeps historical disabled-platform records visible but removes takeover/readback actions and labels them read-only; Results omits the irrelevant Xiaohongshu-vs-X pattern unless both are enabled. Pi configuration preflight is shared by daily/Studio/Results: exact same intent/date/object/workspace/profile revision reuses one persistent `needs_user / PI_CONFIG_REQUIRED`; configured retries can create new work, and UI no longer reports needs_user as completed/failed. Focused boundary/task/profile/content/publishing/X checks passed 12/12; UK/game deterministic root-local List bindings each produced a distinct source → plan → content → X chain with zero cross-root hit. Final `.ai/wmb-1910-package-acceptance.json` cold-started AI/UK/game, proved allowed MCP writes, UK WeChat and game Xiaohongshu/WeChat zero-write denials, game XHS process absence, exact Studio options `3/2/1`, historical read-only Publish, and exactly three reused prerequisite task IDs per root. Real game dev cold-read `全部平台 / X`, `1 / 1`, and `xhs:ensure` rejected before any game XHS process. The acceptance harness had three pre-assert script defects (top-level initialization, cleanup masking and escaped MCP payload matching), all fixed; it then exposed and verified the real missing-XHS-handler repair. `npm run build`, renderer smoke and `git diff --check` pass; EXE SHA-256 `71EBFCCA...EEEB63`, app.asar `12B86C53...D64ADE`. Typecheck remains only the same three pre-existing diagnostics; lightweight gate remains blocked only by the exact 13 historical over-500-line files, and modified historical files retain their prior counts (`agent-runner.ts` 622, `studio-view.tsx` 684). | Codex |
| WMB-1911 | M-1900 | CAP-015, CAP-016, CAP-017, CAP-018, CAP-019 | done | WMB-1910 | Resolve WMB-ISSUE-006 and 007: separate installation preferences from workspace-bound business selection state, clear or namespace object IDs and intelligence subsections on cold switch, and replace Canvas mount-time creation with an explicit empty-state action | 2026-08-02: falsifiable package repros confirmed both shared causes before repair: an empty Canvas mount changed `knowledge_canvases` from `0` to `1`, and installation-global object/Discover keys could select same-ID objects after a cold root switch. Renderer business selection and intelligence subsection keys are now namespaced by authoritative workspace ID; a workspace change remounts the affected views and clears in-memory ranking/List/Canvas/topic/focus/publish context. Canvas no longer creates on mount and instead exposes an explicit `创建第一张画布` action. Final `.ai/wmb-1911-package-acceptance.json` cold-started duplicate-ID roots A/B under one shared userData: all five B-scoped keys began `null`, only B project/topic/source labels rendered, UK/game Discover mounted only `X Lists`, and Canvas stayed at zero. A third empty root opened all eight shared views and preserved identical hashes for all 48 business tables (digest `0fa60bc4...34465`) until the explicit Canvas action. Real game dev cold reload entered Studio without a stale-project error or inherited Studio key; renderer smoke returned title `WeMediaBuddy` and `#root`. Focused Canvas/settings/profile checks passed 3/3, `npm run build` and `git diff --check` pass; EXE SHA-256 `8FF86C5B...6443BC7`, app.asar `448501D3...8A8745`. Typecheck remains only the same three pre-existing diagnostics; the lightweight gate remains blocked only by the exact 13 historical over-500-line files, and modified historical files did not grow (`library-topics-view.tsx` 1575, `today-library-view.tsx` 1342, `x-lists-view.tsx` 1105). The acceptance harness first failed on two script-only assumptions (a local `process` temporal-dead-zone and view persistence across package processes); both were corrected before the product assertions passed. | Codex |
| WMB-1912 | M-1900 | CAP-002, CAP-003, CAP-004, CAP-006, CAP-011, CAP-012, CAP-013, CAP-014, CAP-015, CAP-016, CAP-017, CAP-018, CAP-019 | done | WMB-1911 | Focused packaged acceptance for the complete workspace-module repair across AI, UK and Owner-confirmed 游戏资讯, covering DOM, generic root-local X Lists, fixed AI List isolation, IPC/MCP tools, profile activation, publishing-platform subsets, Pi/X-login `needs_user`, process shutdown, cold-state isolation and zero implicit business writes | 2026-08-02: EVAL-016–EVAL-019 passed against the current Windows package. The four existing acceptance programs were replayed after the final renderer commit, and `.ai/wmb-1912-package-acceptance.json` then cold-started AI, UK, game and a valid WeChat-only profile at both 1100×700 and 1920×900. Every root exposed the exact same eight generic `x_lists.*` tools and zero confirm tools; the no-X publishing profile still rendered X Lists while Studio offered only `全部平台 / 公众号`. AI alone rendered `榜单 + X Lists`; UK/game rendered only `X Lists`, had no AI source presentation or forbidden AI IPC, and all roots displayed their authoritative identity. Current-package receipts prove active-profile relaunch closes the old MCP/runtime, every old URL is rejected, missing X login is `needs_user` with zero operation write, platform denials are zero-write, historical publishing stays read-only, Pi prerequisite retries reuse the same three task IDs per root, and UK/game each retain a distinct root-local List source → plan → content → X chain. The duplicate-ID/cold-state receipt proves five scoped keys begin null and eight empty shared views preserve all 48 business-table hashes until explicit Canvas creation. Focused account/cache/session/operation/platform/profile/needs-user checks passed 12/12. The sealed AI manifest remains intact; live verification reports only the same adjudicated pre-fix differences in `operation_log`, `ranking_cache` and `work_carry_items`. All acceptance child processes and the scoped dev tree were terminated, with no WMB port/process left. Current EXE SHA-256 `8FF86C5B...6443BC7`, app.asar `448501D3...8A8745`; `git diff --check` passes. Typecheck remains only the three frozen diagnostics in `intelligence-wire.ts`/`x-list-execution.ts`; both lightweight and `-Full` gates stop only on the exact 13 historical over-500-line files. No authenticated X session existed on this machine, so no real-platform List mutation/login challenge was replayed; SPEC keeps platform authentication and real publication outside this completion gate, and no fixture is presented as that manual evidence. | Codex |
| WMB-1913 | M-1500 | CAP-001 | done | WMB-1912 | Remove the topbar global topic/project search and its dedicated state, query effect and CSS without changing page-local search | 2026-08-02: removed the only topbar global-search render plus its two React states, asynchronous topic/project query effect, result navigation handler and all dedicated CSS; net product diff deletes 99 lines and adds no replacement UI or dependency. `.ai/wmb-1913-package-acceptance.json` cold-started the current Windows package at 1100×700 and 1920×900: `全局搜索` count `0`, `搜索主题或项目` absent, brand/workspace identity visible, and Studio `搜索内容项目` count `1`. The first acceptance attempt reached the assertions before the asynchronous workspace label loaded; the harness now waits for that authoritative identity and the rerun passed. `npm run build` and `git diff --check` pass; EXE SHA-256 `66F8AD74...133F5A`, app.asar `A1F5C103...0565F3`. Typecheck remains only the same three frozen diagnostics, and the lightweight gate remains blocked only by the exact 13 historical over-500-line files. | Codex |
| WMB-2001 | M-2000 | CAP-014, CAP-018 | done | WMB-1913 | Move encrypted Pi API/model presets from data-root `app_meta` to one installation-level store, migrate the existing official AI preset once, and keep sessions/tasks/runtime files root-local | 2026-08-02: real repro found two presets only in the official AI root (`OpenCode Go / deepseek-v4-flash` active plus `gpt-5.6-terra`) while active UK and game roots each had zero, which caused `Pi 未配置` after switching. The authoritative encrypted store is now `%APPDATA%\WeMediaBuddy\pi-api-config.json`; startup creates it before the window and migrates the registered official AI state once by copying encrypted bytes without decrypting, printing or deleting the legacy row. Settings, Dock, daily intelligence, Studio draft and Results review all resolve the same installation store; Pi conversations, task records, generated `models.json`, browsers, accounts, cookies and business data remain root-local. Saving/activating/deleting still stops the current Pi, and an empty global envelope prevents a deleted final preset from being resurrected. Focused Pi/profile/confirmation/needs-user checks passed 7/7. `.ai/wmb-2001-package-acceptance.json` cold-started UK then game under one isolated installation: UK read both migrated models, activating `model-two` persisted into game, UK/game root config counts stayed zero, and AI legacy count stayed one. The first two package attempts failed only in fixture schema initialization and Settings navigation timing before product assertions; the corrected run passed. The real current installation then migrated and cold-read active UK as configured with the exact two existing profiles; root counts are AI `1`, UK `0`, game `0`, and the visible packaged window remains open. Full `npm test` passed 106/108; the two failures are the unchanged Windows temporary-SQLite cleanup `EBUSY` and stale source-logo count `38 !== 29`. `npm run build` and `git diff --check` pass; EXE SHA-256 `CD75AFA6...F275D24`, app.asar `03160C27...13B4C73`. Typecheck remains only the same three frozen diagnostics, and the lightweight gate remains blocked only by the exact 13 historical over-500-line files; `agent-runner.ts` remains 622 lines and `index.ts` is 500. | Codex |
| WMB-2002 | M-1000 | CAP-014 | done | WMB-2001 | Copy every imported Pi MCP extension companion into each root and replace the synthetic 12% startup value with truthful unknown-plan presentation | 2026-08-02: falsifiable readback confirmed both independent causes before repair. `planned=processed=0` with a running task executed the renderer fallback as exactly `0.12`; source and packaged `index.ts` imported `wmb-mcp-tools-workspaces.ts`, but both `files` and `staleFlatFiles` in the shared installer omitted it, leaving AI/UK runtime directories with seven files and making UK task `8e0820c8-...` persist `failed / UK_INTELLIGENCE_FAILED` before any progress. Luna-max made the bounded change: add the workspace companion to both lists and use `0` when planned count is unknown. New `tests/pi-extension.test.mjs` parses every relative `.ts` import from the real extension index, runs `preparePiExtension` into a temp agent directory, verifies every companion is accessible, and checks unknown `0/0 → 0` plus real `1/4 → 0.25`; focused Pi/config/needs-user checks passed 6/6 and `git diff --check` passes. Current Windows package rebuilt and the real UK Today entry was replayed through WMB UI boundary: new task `4a3f28d7-...` showed no `12%`, did show `0%`, refreshed the root extension to eight files including the workspace tool, started Pi RPC PID `50764`, and reached persistent `running / running_pi` with no error instead of exiting during extension load. The task remains running for its normal research work; no platform operation was invoked. EXE SHA-256 `F41C8384...31CCEF`, app.asar `7210286F...02EC0D`. Typecheck remains only the same three frozen diagnostics; the lightweight gate remains blocked only by the exact 13 historical over-500-line files, and `today-library-view.tsx` remains 1342 lines. | Codex |
