# WeMediaBuddy 产品需求文档

- 产品名：WeMediaBuddy
- 简称：WMB
- 中文定位：人类与 AI 共同操作的自媒体运营伙伴
- 文档状态：产品愿景已确认
- 日期：2026-07-27
- 本次范围修订：2026-08-05 工作空间隔离的人机协同合同
- 本次文档修订：2026-08-05 结构质量修订：新增需求优先级分层、产品质量指标与产品风险章节，复合验收标准拆为原子子项，补齐画布场景与六种格式枚举；不改变任何既有 REQ/AC 语义

## 1. 产品愿景

WeMediaBuddy 是一个面向个人 IP、赛道无关的自媒体创作与运营终端。AI 是首个官方赛道工作空间，不是产品边界。

它让人类与不同 AI Agent 共享同一个运营现场，共同完成情报发现、资料沉淀、运营决策、内容生产、网页发布、数据采集和创作复盘。

产品最终形成以下长期闭环：

> 每日侦察 → 资料入库 → 机会判断 → 运营方案 → 人机讨论 → 内容创作 → 多平台适配 → 网页发布 → 数据采集 → 创作复盘 → 更新方法库 → 改进下一轮运营

WeMediaBuddy 的目标不是让 AI 批量制造内容，而是让人类与 AI 长期共同经营一个个人品牌，并把每天产生的情报、判断、内容和数据持续沉淀为资产。

### 1.1 叙事与优先级

产品完整闭环包含发布与采集，但**第一价值**不是自动发帖。

第一价值按此顺序理解与宣传：

1. 情报/素材收集
2. 资料整理与沉淀
3. 机会判断：今天什么值得做、为什么、怎么讲

所有达到最低机会标准的结果都应保留，不以固定数量截断。内容机会按 `SSS、S、A、B、C、D、E、F` 从高到低排列；首页可以突出最高等级，但不得丢弃或隐藏其余合格机会。
4. 人机共同创作
5. 复盘并把结果喂回下一轮
6. 网页发布与数据采集（执行环）

实现上可以先打通发布以获得真回执；  
叙事上不得把 WMB 说成“跨平台自动发布工具”或“AI 批量文案机”。

验收口令：

> 用户听完后，应记住“帮我判断并做出有价值的内容”，而不是“帮我自动发帖”。


## 2. 产品定位

### 2.0 产品形态（Owner lock 2026-08-07）

WeMediaBuddy 的产品形态是 **AI 驱动的自媒体人机协同应用终端**，不是传统软件工作台，也不是「人类主写、AI 侧栏辅助」的写作 IDE。

类比只用于对齐形态（无技术依赖）：

- **要对齐**：Codex Desktop 一类 Agent 主路径终端——默认由 Agent 生产，人类定目标、批呈报、派工、终审与担责。
- **要拒绝**：VS Code 一类 IDE——默认人类直接完成主业，AI 对话仅辅助。

因此：

1. **人类角色是主编/操作员**，不是默认资料员或一线码字写手。
2. **Agent（内置 Pi 与受权外部 Agent）承担采、判、归主题、出选题、起草、复盘准备** 等主路径劳动；人手深度编辑是旁路。
3. **今日页是主编办公桌**：只放需要决策或知情的呈报（可批选题、主题之今日须知进展、在写任务、运行状态）；不得把「未做成选题的原料」当作默认上桌物，从而把做选题方案的活甩回人类。
4. **业务对象分层**：资料是证据；选题是一炮可批方案；**主题是长期关注的正式容器**（可含多资料、多选题、多篇稿与复盘）；创作项目是一炮文稿与版本。Agent 判断「值得长期关注」时，应归入或升级为主题（LLM 编辑归纳，不得以正则/标题串匹配作为主归并引擎）；一炮结束的选题不必强行建主题。
5. **前台 Pi** 贴业务页共写与纠偏；**后台流水线** 负责定时/跑批的采集、归主题、选题呈报与进展标记。平台最终发布与精确外部副作用仍需人类确认。
6. 完整形态宪法与房间隐喻见 `PRODUCT.md` 与 `docs/spark/2026-08-07-product-form-agent-desk-constitution.md`。PRD/SPEC/实现冲突时，以本小节与 `PRODUCT.md` 形态条款为优先解释。

### 2.1 产品是什么

WeMediaBuddy 是：

- **AI 驱动的自媒体人机协同终端**（Agent 主路径，人类终审）；
- 人类与 AI Agent 共用的自媒体运营工作空间；
- 一个安装可登记多个相互隔离的赛道工作空间，但同一时间只运行一个；
- 保存资料、选题、主题、方案、草稿、发布记录和复盘数据的统一事实源；
- 通过内部 API 和 MCP 向不同 Agent 开放运营能力的控制面；
- 通过固定浏览器和 CDP 操作内容平台的执行终端；
- 让 Agent 用官方能力包提出赛道配方、由 WMB 校验并由用户确认启用的组装终端；
- 能根据创作结果持续改进后续运营决策的学习系统。

### 2.2 产品不是什么

当前阶段，WeMediaBuddy 不是：

- 传统自媒体管理后台或内容 CMS（人类填表/手搓主流程）；
- **以人类直接编辑为默认主路径的写作 IDE**（VS Code 型「人写为主、AI 聊天为辅」）；
- 单一 AI 写作工具或文案生成器；
- 热点链接聚合器；
- 套着聊天界面的大模型客户端；
- 覆盖编程、设计等任意工作的通用人机协作平台；
- 第三方插件市场、自由节点工作流或通用工作流引擎；
- 共享一个业务数据库的多租户系统或并行多工作空间运行时；
- 自研 Agent 运行时或多 Agent 调度框架；WMB 只内置经过验证的 Pi 执行器；
- 依赖各内容平台官方 API 的发布系统；
- 自动点击平台最终发布按钮的工具。

WMB 内置 Pi 作为默认执行者，同时保留外部 Agent 通过 MCP 接续同一业务状态。Pi 不是另一套业务系统，也不演变为多 Agent 编排框架。

### 2.3 固定编制与授权扩展（Owner lock 2026-08-07）

1. 产品内置固定身份智能体编制（非编排器）：**桌助、记者、策划（兼复盘）、写手、资料员**。编制跨赛道稳定；赛道差异只体现在赛道包（Skill、信源、受众、渠道配置），不得复制第二套角色权限。
2. **写手**必须能只读引用资料库以创作；不得获得库整理/移出/硬删写权。**资料员**获得整理类写权；不得获得 `plans.save` 选题决策与正文主写权。
3. Agent 写业务的权限以 **Capability 注册表 + Task/Page Grant + Precise Gate** 为硬约束；system prompt 与 Skill 不得单独构成写权。最终平台发布点击与硬删仅人类 UI。
4. 须提供一级 **智能体（班组）** 可视化面（状态/进度/详情入口/设置跳转）；今日桌可保留一行值班摘要，但不得以运维大屏取代机会判断主席台。
5. 新内部写命令上线前必须登记 Capability 并声明默认角色绑定；未登记不得合并。规范详案见 `docs/spark/2026-08-07-role-permission-design.md`。


## 3. 目标用户

第一阶段只服务项目所有者本人。

用户以个人 IP 为内容主线。首个 AI 官方工作空间覆盖：

- AI 观点与行业观察；
- AI 工具、Skill 和工作流实测；
- AI 产品开发和创业记录；
- 使用 AI 找甲方、接单、报价和交付；
- AI 副业与商业机会；
- 重要新闻、争议、案例与职业变化。

第二个官方工作空间面向在英华人的英国生活内容。两者验证同一套自媒体内核可以承载不同信源、创作方法和复盘规则；未来赛道不改变 WMB 的自媒体边界。

产品先在真实账号和真实运营流程中验证，暂不以多人团队或通用 SaaS 为前提。

## 4. 核心原则

### 4.1 人机共享同一现场

终端界面和 AI Agent 操作同一份数据。任何有业务价值的资料、决策和产物都不能只留在某个 Agent 的聊天记录中。

### 4.2 人类掌舵，AI 主路径协作

人类负责目标、价值判断、个人观点和最终责任。AI（内置 Pi 与受权外部 Agent）默认承担侦察、整理、研究、归主题、出选题、起草、执行准备和复盘准备；人手深度编辑是旁路，不是默认主路径。凡把「做选题方案」默认甩回人类从裸资料倒推的设计，视为形态违规（见 §2.0）。

### 4.3 Agent 中立

获得用户授权的外部 Agent 均可通过同一套 MCP 能力读取并操作项目状态。更换 Agent 后，工作可以从原状态继续。

### 4.4 浏览器是平台执行器

不依赖内容平台官方 API。终端通过 CDP 控制固定的专用浏览器，复用用户的真实登录状态完成编辑器准备、发布结果读取和数据采集；平台最终发布按钮始终由用户在浏览器中手动点击。

### 4.5 结果必须反馈到下一轮创作

数据采集不是终点。复盘必须给出下一篇内容应当保留、停止或改变的具体行动，并影响后续运营方案。

### 4.6 固定主线，赛道可组装

资料、判断、创作、平台版本、发布交付和复盘是固定业务主线。官网与 X Lists 是所有赛道共用的固定情报渠道模块；每个 data-root 只配置自己的具体网站、List、受众和编辑目标。InstallationContext 登记浏览器程序、BrowserProfile registry 与 `defaultProfileId`；新工作空间创建时显式绑定该 defaultProfileId，并在根内保存自己的 `browserProfileId`、期望账号身份快照、binding revision、List 绑定、缓存、操作、采集结果和其他业务数据。Owner 可在 UI 明确改绑或创建独立 profile；即使多个根绑定同一 profile，各根业务事实仍不共享。物理 Cookie 与 profile 文件不进入 SQLite 或 data-root。官方能力包保留受众/编辑上下文及确有差异的展示或创作能力，不再复制渠道采集流程。复盘仍是固定内核，直到真实赛道差异证明需要拆分。Agent 或用户不得任意新增业务阶段、执行代码或跨阶段连线。

### 4.7 工作空间隔离的人机协同与授权

人类拥有每个工作空间的目标、个人观点、grant 和最终责任。内置 Pi 与外部 Agent 是对等的、受任务授权的 workers：在同一任务内可自主研究、分析、整理、入库、创作和复盘，但不拥有独立的业务真相、最终发布权或跨根权利。任何有价值的资料、证据、判断、草稿、内容版本、发布证据、指标、复盘和方法结论都必须进入当前根的可追溯业务事实；chat transcript、Pi session、模型思考和工具展示不是业务真相。

授权分三层，不能互相替代：

1. task grant 绑定 Owner 目标、当前 workspace、允许的任务范围、输入/依赖 revision、worker 和有效期；它允许任务内的自主研究与业务编排，但不直接授权外部副作用。
2. precise execution grant 绑定一项精确业务或浏览器副作用的目标、账号/profile、对象 revision、输入内容指纹、范围和一次性结果读回和一次性结果读回；只有在仍匹配的 task grant 下才可执行，陈旧或扩大范围即零写入。
3. 工作空间创建/激活、BrowserProfile 绑定或复用、profile 变更、grant 的最终签发/撤销属于 UI-only；平台最终发布始终是用户在可见平台页面手动点击。自然语言对话、session 文本或 MCP 调用本身从不等于 grant。

## 5. 核心使用场景

### 5.1 每日情报侦察

用户打开任意已接入的外部 Agent 并发起每日运营后，AI 寻找“今天有什么值得受众知道、讨论、尝试或拿去赚钱”，覆盖：

- 重大 AI 新闻和行业变化；
- 新模型、新产品和新功能；
- 工具、Skill、提示词和工作流；
- AI 接单、交付、定价、副业和创业机会；
- 成功案例、失败案例和可复用经验；
- 行业争议、职业变化和观点冲突；
- 各内容平台正在升温的话题与表达形式；
- 用户自己的产品开发、实验和阶段性进展。

情报不能以无序链接结束。每条资料应记录：

- 原始来源；
- 内容摘要；
- 分类与关键词；
- 为什么值得关注；
- 与用户个人 IP 的关系；
- 可采用的创作角度；
- 适合的平台和内容形式；
- 时效性与建议优先级。

每个自媒体工作空间都可在「设置」中管理本根 X List，把自己创建或关注的列表作为可复用的情报信源；「发现」只浏览用户选中显示的 List 动态和其他外部发现内容。自己创建的 List 可新建、编辑、删除和管理成员；外部 List 仅在 X 授予的范围内读取或接入/移出 WMB 情报渠道，不把“退出别人 List”自动化为拉黑创建者。Pi 或外部 Agent 只能在当前根有效 task grant 内完成解析、候选比对和准备；创建、成员变更等副作用还必须使用冻结准确账号、`browserProfileId`、名称/隐私状态或 List ID/handles 的 precise execution grant，并在执行后真实读回。编辑和删除 List 仍按风险保留 UI-only 确认。对话文本不能跳过该边界。设置页只负责配置、历史和故障处理，Pi dock 只负责提议、工具进度、解释和结果。List 拉取的帖子仍以可追溯资料进入当前根的原有资料库，不另建资讯孤岛。X Lists 是通用情报来源能力，不依赖该工作空间是否把 X 选为发布平台；绑定时的账号快照、List 配置、缓存、操作和已入库资料均按 data-root 隔离。

每个工作空间还可在「设置 → 情报渠道」管理任意公开网站：用户输入网站名称或 URL 后，WMB 必须识别规范内容入口并真实试读，展示准确匹配和可读状态。Pi 或外部 Agent 可在 task grant 内完成解析、试读、去重和提案；新增、启停或移除来源必须取得绑定当前根、profile、规范来源身份、完整差异清单和输入内容指纹的 precise execution grant，或者由 UI-only 确认签发后执行。官方模板只提供可见的初始来源建议，不形成永久白名单。今日情报自动运行当前根全部已启用官网和 X Lists；渠道增删、启停、就绪状态、检查回执和配置引导只在「设置」呈现。Today 只展示内容机会和创作信息；Discover 只展示榜单、所选 List 动态和其他可用于找题材的外部发现内容。所有渠道使用同一套扫描、去重、按需核验和内容机会判断流程。

### 5.2 生成每日运营方案

AI 结合当日情报、用户定位、历史内容和账号表现生成可执行的运营方案。

方案应包含：

- 候选选题及优先级；
- 推荐理由与时效；
- 用户适合表达的观点和切入角度；
- 目标受众；
- 推荐平台；
- 推荐形式，如短图文、短视频、口播、帖子串或长文；
- 标题、开头和内容结构建议；
- 需要使用的资料；
- 预计工作量。

同一个核心选题可以针对不同平台形成不同表达，但不得为了多平台分发而制造重复、低价值内容。

### 5.3 人机共同创作

用户选择选题并补充观点后，AI 可以继续研究、组织证据、生成提纲、撰写初稿、修改内容、准备配图和适配平台。

终端应集中展示和关联：

- 原始资料与引用来源；
- 用户观点和补充信息；
- 提纲、草稿和修改结果；
- 标题、封面、正文和媒体素材；
- 各平台的最终发布版本；
- 从资料到成稿的关键决策。

### 5.4 网页发布

终端通过当前根绑定的固定浏览器 profile 准备内容，用户在平台页面核对并手动执行最终发布。WMB 不点击任何平台的最终发布按钮。

当前产品必须支持：

- X：文字、图片和视频帖子；
- 小红书：图文和视频笔记；
- 微信公众号：文章。

以上合计六种约定平台格式：X 文字、X 图片、X 视频、小红书图文、小红书视频、公众号文章。

基本过程：

1. 连接专用浏览器及其长期用户目录；
2. 复用用户已登录的平台状态；
3. 打开目标平台发布页面；
4. 填写内容并上传媒体；
5. 用户在平台页面手动点击发布；
6. WMB 只读核对并记录平台、账号、发布时间、帖子地址和页面身份；
7. 将核对结果回写内容项目。

遇到登录失效、验证码、平台风控或页面异常时，自动化应暂停并允许用户在同一浏览器中接管。

准备前冻结不可变 publication snapshot，至少绑定当前根、平台版本/revision、账号身份、绑定 profile，以及素材与发布内容的确定指纹。浏览器填写、上传和读回是有状态的副作用提交；中断后只能进入准确的待人工、未知或对账状态，绝不自动再次触发平台最终发布。

### 5.5 数据采集与复盘

终端通过网页读取平台可见的内容和账号数据，不依赖平台官方 API。

可采集指标包括但不限于：

- 展示量、阅读量或播放量；
- 点赞；
- 评论或回复；
- 转发、引用或分享；
- 收藏；
- 平台页面提供的停留、完播等指标；
- 账号粉丝变化及其他页面可见数据。

终端应保存多个时间节点的数据快照，而不只保存当前总数，以便观察内容增长速度。

复盘需要关联：

- 选题与观点；
- 标题、封面和开头；
- 内容形式；
- 发布平台、账号和时间；
- 各阶段表现；
- 本次得到验证或被否定的判断；
- 下一轮具体应当保留、停止或改变的行动。

咨询、私域转化、订单和收入等暂时无法自动取得的数据，未来可支持手动补录，但不阻塞平台可见数据的首轮闭环。

### 5.6 创建与切换赛道工作空间

用户可把现有数据根原地登记为 AI 工作空间，也可从 AI、UK 官方模板创建新的完整数据根。每个工作空间独立保存业务、知识、来源、内容、任务、Pi session、grant、账号绑定和回执；安装级只保存应用、固定 runtime/model preset、Skill library、浏览器程序、BrowserProfile registry 与 defaultProfileId。新根显式绑定 defaultProfileId，并保存自己的 `browserProfileId`、期望账号身份和 binding revision；Owner 可在 UI 明确改绑或创建独立 profile。切换采用有界冷切换：旧浏览器进程与 CDP 必须关闭，目标根按自己的绑定取得 browser lease，非活动工作空间不运行后台任务。

对于其他自媒体赛道，用户在现有 Pi 或外部 Agent 中描述目标。Agent 只能从官方能力包中生成完整配方提案；WMB 做确定性校验，界面展示准确结果与差异，最终创建或改装必须由用户在 UI 确认。Agent 不能确认、激活或传入任意数据目录。

### 5.7 管理 Pi Skills 与可信内容创作

设置页提供安装级 `Pi Skills` 管理。用户可查看所有当前 Pi 可用 Skill；WMB 业务操作 Skill 和当前工作空间赛道 Skill 作为系统必需项只读展示，普通共享 Skill 可新增、编辑和删除，并自动用于所有工作空间的新 Pi 进程。用户删除或修改普通内置 Skill 后，重启和应用升级不得静默恢复旧内容。

WMB 默认提供一个可编辑、可删除的可信内容写作 Skill。只要用户要求基于真实题材写文章、正文、口播稿、小红书文案或深度内容，Pi 就应先拆解可验证主张、补齐证据、按证据写作，再从成稿反向提取事实进行第二次核查。无法验证的主张必须删除、收窄或明确标注；内容增厚依靠机制、数字、案例、影响、限制和行动信息，不以同义重复或空泛扩写冒充丰富。

Skill 只影响 Pi 的工作方法，不能新增 WMB 业务阶段、MCP 工具、任意代码执行权限、数据库/文件直写或最终发布能力。

### 5.8 在 Pi 对话中发现并调用命令

用户在 Pi 输入框首字符输入 `/` 时，可直接看到当前 Pi RPC 进程真实加载的扩展命令、提示模板和 Skills，并继续输入名称或说明文字筛选。列表必须来自当前工作空间实际 Pi 命令目录；设置页新增、修改或删除 Skill 后，下一个 Pi 进程和下一次命令目录读取必须反映最新状态。

键盘上下键移动选择，`Enter` 或 `Tab` 将完整原生命令写入输入框，`Escape` 关闭；鼠标也可选择。选择动作不自动发送，用户可继续补充参数，再经现有发送、插入当前回复或下一轮队列语义提交。WMB 不展示 Pi RPC 无法执行的交互终端专用命令，也不把斜杆面板扩成新的业务权限或任意命令执行入口。

Pi 顶部会话菜单支持归档和恢复会话。归档只从默认会话列表隐藏，不删除会话消息、Pi session 文件或业务引用；已归档会话在同一菜单的独立视图中恢复。归档当前会话后切换到最近的未归档会话，没有则新建；Pi 正在输出时不得归档。

### 5.9 用关系画布组织长期知识

用户可把资料、选题、内容项目、复盘和方法结论等已有业务对象放入持久的关系画布，并手动建立语义关系。画布只是这些对象的工作视图：删除画布节点不删除被引用的对象，同一对象可出现在多个画布中，每个画布保存自己的布局与视野。

Pi 默认使用当前画布页面作为上下文；用户勾选对象时，Pi 只使用选中对象。上下文随原有 Pi 会话轮次保存，产品不新增独立的「上下文包」或静态快照对象。

## 6. Agent 与 MCP

### 6.1 当前策略

WeMediaBuddy 内置并监管固定版本的 Pi RPC 执行器，但不承载模型权重或推理服务。Pi 只使用用户单独配置的 OpenAI Responses 或 OpenAI Chat Completions 兼容接口执行显式智能任务；现有外部 Agent 继续通过 MCP 接入终端。

常用模型可以包括 DeepSeek V4 Flash、GPT-5.6 Luna 和 MiMo-V2.5-Pro，但保存和调用的模型 ID 必须来自当前服务的真实 `/models` 目录或用户明确输入。WMB 不在模型或服务不可用时静默切换到其他模型、协议或供应商。

终端的核心业务操作先形成统一的内部 API，再暴露为 MCP 工具。界面、Pi 与外部 Agent 调用同一业务能力，避免形成两套逻辑；Pi 与外部 Agent 在任务边界内地位对等，均不因聊天记录、会话或 transport 获得额外写入权。

WMB 不定时唤醒 Agent。每日侦察、运营方案和创作由用户在 WMB 页面显式触发 Pi，或从任意外部 Agent 发起；指标采集等不需要 AI 推理的任务可由 WMB 自动调度。

当当前主模型不支持图片输入时，内置 Pi 使用随固定 runtime 打包的可维护视觉扩展，把明确的识图请求委派给同一用户配置服务中的视觉模型，并只把文字观察结果返回主模型继续推理。视觉失败必须原样报告，不切换主模型、不静默改用其他模型，也不授予视觉模型 WMB 业务写入或发布能力。

用户显式启动今日情报或 X List 趋势观察后，WMB 可为本次冻结的当前根 List 创建有限的非 AI 指标复查任务，保存帖子多个真实时间点的可见指标并计算传播速度。它不在用户未启动时自行扫描，不在应用关闭或工作空间非活动时运行，也不把该能力扩成全网 X 监控或自动社交工具。

项目提供一套版本化的共享资讯流程，供内置 Pi 或外部 Agent 处理已由固定官网/X Lists 模块取得的候选资料，执行按需事实核验、同一事件聚合和内容机会判断。工作空间能力包提供受众与编辑上下文；具体网站、List 和启停状态是根内业务配置，不再藏在按赛道复制的采集 Skill 中。除 REQ-023 中一次显式动作产生的有界指标复查外，渠道不自行定时运行。

### 6.2 预期能力域

MCP 最终可覆盖：

- 查询、保存、整理和归类资料；
- 创建和评估选题；
- 读取、生成和更新运营方案；
- 创建、读取和修改内容草稿；
- 管理平台版本和媒体素材；
- 准备网页发布内容并等待人工发布；
- 查询发布状态与结果；
- 读取账号和帖子数据；
- 创建和读取复盘结论；
- 查询历史方法和已验证经验。

具体工具名称与参数由后续接口设计确定，本 PRD 不提前固定。

## 7. 浏览器连接层

### 7.1 运行方式

- 使用固定的专用 Chromium 浏览器；
- 使用安装级 BrowserProfile registry 中登记、且由当前根显式绑定的独立持久浏览器用户目录；
- 用户首次或失效后手动登录各平台；
- 终端通过 CDP 连接并执行网页操作；
- 所有平台自动化共享统一的上层发布和采集流程；
- 平台页面差异只在对应的薄适配层处理。

### 7.2 明确限制

网页结构、验证码、登录状态和平台风控具有不确定性，因此产品不承诺完全无人值守。

任何发布和采集任务都必须能明确显示：

- 当前操作的平台和账号；
- 执行状态；
- 成功后的可验证结果；
- 失败原因；
- 是否需要用户接管。

## 8. 核心数据资产

产品长期积累四类资产：

### 8.1 情报资产

来源、资料、新闻、案例、工具和行业变化。

### 8.2 观点资产

用户对问题的判断、立场、偏好、表达和已经形成的方法。观点资产记录「用户怎么想」，由用户主动表达；它与运营资产中的方法结论分工明确——方法结论记录「什么被验证有效」，由复盘从真实结果中提炼。

### 8.3 内容资产

选题、研究材料、草稿、平台版本、媒体素材和发布记录。

### 8.4 运营资产

账号数据、帖子数据快照、复盘结论以及经过实际结果验证的内容方法。

## 9. 核心对象

当前产品至少需要表达以下业务对象：

- 工作空间登记；
- 工作空间配方提案与当前有效 revision；
- 官方模板与官方能力包版本；
- 资料；
- 情报来源；
- 选题；
- **主题**（长期关注的正式容器：可关联多资料、多选题、多内容项目与复盘；Agent 判断值得长期跟时应归入或升级为主题，见 §2.0）；
- 每日运营方案；
- 内容项目；
- 草稿与版本；
- 媒体素材；
- 平台；
- 账号；
- 发布记录；
- 指标快照；
- 复盘；
- 方法结论；
- Agent 操作记录；
- task grant、precise execution grant、命令回执和审计记录；
- 当前根 browserProfileId、期望账号身份和实际账号读回快照；
- X List、其发现信源绑定与操作记录；
- 工作空间官网来源配置、渠道就绪状态和逐来源扫描回执；
- X List 帖子指标快照、有界观察任务及确定性趋势证据。

本节只定义产品概念，不规定数据库表或技术实现。

## 10. 产品阶段策略

### 当前产品范围

完整实现以下真实闭环：

> 情报进入终端 → AI 生成可执行选题 → 人机完成内容 → 固定浏览器发布到 X、小红书或微信公众号 → 网页采集结果 → AI 给出下一轮改进

一个 WMB 安装可登记多个完整、隔离的赛道数据根，首轮包括原地登记的 AI 工作空间和新建的 UK 工作空间；同一时间只有一个活动工作空间。AI 根不迁移既有业务数据；既有 AI 官网目录和固定 List 转成该根可见、可管理的普通渠道配置，AI 排行榜等真正的赛道展示能力继续由 AI profile 限定。UK 和其他根使用相同官网/X Lists 模块，但只读取本根独立配置并明确启用的来源。用户还可让 Agent 基于官方能力包提出其他自媒体赛道配方，经 WMB 校验和 UI 精确确认后创建新工作空间。

每个工作空间当前仍只按既有单账号语义运行。新根初始绑定 InstallationContext.defaultProfileId；此后每个根只认自己的 browserProfileId、binding revision 与期望账号身份，Owner 可以 UI 明确改绑或创建独立 profile。即使多个根继续复用 defaultProfileId，账号 snapshot、来源/List binding、cache、operation、grant、receipt 与业务数据仍隔离。一个工作空间内的多账号管理仍属于后续阶段。

当前产品支持 X 文字、图片和视频帖子，小红书图文和视频笔记，以及微信公众号文章。

所有自媒体工作空间还支持在「设置」中管理本根可访问的 X List，并将用户选定的 List 接入为情报信源；「发现」只浏览选中的 List 内容。这项输入能力与发布平台选择分离，也不是通用 X 客户端或自动化社交操作工具。

### 后续阶段

在真实闭环稳定后，再按实际需求增加：

- 除 X、小红书和微信公众号之外的内容平台；
- 更完整的内容与素材协作；
- 手动补录商业转化；
- 多账号管理；
- 多 Agent 并行协作；
- 面向团队或其他创作者的产品化能力。

以上均不是当前阶段的默认承诺。

## 11. 产品级验收标准

当以下条件同时成立时，产品愿景才算形成真实闭环：

1. AI 找到的资料能够在终端中查看、追溯和再次使用；
2. AI 能基于资料和历史结果生成具体可执行的运营方案；
3. 用户与不同外部 Agent 能接续操作同一个内容项目；
4. 内容从研究、创作到平台版本均保存在终端中；
5. 终端能交付适合 X、小红书和微信公众号人工发布的最终平台版本与素材；
6. 用户可在人工发布后选择回填帖子或文章链接；
7. 已回填链接的内容可从网页取得可见指标并保存快照；
8. AI 能基于创作过程和结果提出下一轮具体改进；
9. 下一轮运营方案能够使用此前沉淀的资料、观点和复盘结论；
10. 现有 AI 数据根原地登记后，既有业务对象、文件和已验收能力零回退；
11. AI 与 UK 工作空间可安全冷切换，业务数据、登录态、运行时和迟到响应不串线；
12. UK 官方工作空间及一个由 Agent 提案、用户确认的第三自媒体工作空间都能完成最小内容闭环。
13. AI 与 UK 都可通过相同官网/X Lists 模块管理本根来源，任意公开网站和同名 List 经过真实解析后不串根；
14. 今日情报对每个来源留下真实检查回执，零更新和空方案可以成功，部分失败不丢失成功结果；
15. Agent 只有在用户确认绑定准确来源身份和完整 diff 后才能增删来源，陈旧确认零写入。
16. 用户显式启动的 X List 趋势观察能在当前根保存同一帖子的多个真实指标快照，以可解释速度支持内容机会；应用关闭、根切换和缺失指标不产生假数据或跨根写入。
17. 设置页能管理所有工作空间共享的普通 Pi Skills，系统必需 Skill 不可破坏，新增/修改/删除在新 Pi 进程中真实生效且不会被重启静默回滚；默认可信内容写作 Skill 能对一篇事实型内容留下主张核查、证据不足处置、信息增厚和成稿复核的可读结果。
18. 同一根的人、Pi 与外部 Agent 能由 task grant 接续任务，所有有价值结果写成根内业务事实；精确副作用仅凭匹配的 precise execution grant 执行，浏览器/profile/账号变更或陈旧 revision 均零写入，最终平台发布仍由人手动完成。

任何单次生成成功、网页点击成功或局部数据读取成功，都不能单独视为产品闭环完成。

## 12. 产品质量指标

第 11 节的验收标准是二值能力门槛；本节指标衡量闭环的运转质量，尤其是第一价值「机会判断」的质量。首轮目标值为经验起点，可在复盘时校准，但指标定义不得为美化结果而修改。

| 指标 | 定义 | 首轮目标 |
| --- | --- | --- |
| 情报合格入库率 | 入库资料中带完整来源、摘要、关注理由和创作角度的比例 | 100%（结构性字段，机械可查） |
| 机会采纳率 | 每周 B 级及以上内容机会中被用户选入创作的比例 | ≥ 25% |
| 方案可执行率 | 每日运营方案无需返工即直接进入创作的比例 | ≥ 50% |
| 发布准备一次通过率 | 平台版本与素材经用户核对无需修改即可发布的比例 | ≥ 70% |
| 复盘行动闭环率 | 复盘 keep/stop/change 行动在后续两周内被运营方案显式引用的比例 | ≥ 50% |

这些指标衡量判断与协作质量，不衡量内容产量；不得用产量指标替代。

## 13. 产品风险与缓解

| 风险 | 等级 | 缓解 | 残余风险 |
| --- | --- | --- | --- |
| 平台风控识别网页自动化，导致账号受限或封禁 | 高，可能损失账号 | 最终发布始终人工点击；不模拟点赞、关注等社交行为；采集有界低频；异常即暂停并交还用户 | 接受。账号归用户所有，首次连接平台时明示该风险 |
| 平台页面结构漂移导致发布准备或采集失效 | 高，必然发生 | 平台差异收敛在薄适配层；失败原因可见；用户可在同一浏览器接管 | 接受。产品不承诺任何平台永久可用 |
| 登录态失效、验证码或二次验证 | 中 | 自动化暂停，用户在同一浏览器人工完成后继续 | 低 |
| 本地 data-root 损坏导致业务事实丢失 | 中，单用户无副本 | 全部业务事实可追溯、有审计记录；备份与恢复策略由技术设计规定 | 中，待技术设计覆盖 |
| 模型服务不可用或擅自变更 | 中 | 不静默切换模型、协议或供应商，明确报错并暂停智能任务 | 低 |
| 三层 grant 与跨根隔离的复杂度超出单用户产品的直觉需要 | 中，复杂度风险 | 该模型是「AI 可写业务事实」前提下的最小完整授权，不为未来多租户预留能力，也不为便利性开绕过通道 | 接受，并在复盘时重估 |

## 14. 需求索引

以下稳定 ID 用于 SPEC、PLAN 和 TASKS 追踪，不改变前文含义。

| ID | 当前产品要求 |
| --- | --- |
| REQ-001 | 人类、界面和外部 Agent 操作同一份本地运营状态。 |
| REQ-002 | 外部 Agent 能将可追溯资料整理入库。 |
| REQ-003 | 外部 Agent 能基于资料、历史复盘和方法结论保存每日运营方案。 |
| REQ-004 | 内容项目保存来源、观点、版本、平台版本和素材。 |
| REQ-005 | 外部 Agent 通过 MCP 接入并可跨 Agent 接续工作。 |
| REQ-006 | WMB 管理安装级浏览器程序、BrowserProfile registry 与 defaultProfileId；每个新工作空间显式绑定 defaultProfileId，并保存自己的 browserProfileId、期望账号身份和 binding revision。Owner 可在 UI 明确改绑或创建独立 profile；物理 Cookie/profile 不进入 data-root，即使复用同一 profile，各根业务事实仍隔离。 |
| REQ-007 | WMB 交付准确的平台版本和素材，最终发布由用户在平台中手动完成。 |
| REQ-008 | 支持 X 纯文字、图文和视频平台版本。 |
| REQ-009 | 支持小红书图文和视频笔记平台版本；AI 操作只走指定 MCP。 |
| REQ-010 | 支持微信公众号文章平台版本。 |
| REQ-011 | 从网页采集帖子与账号可见指标并保存历史快照。 |
| REQ-012 | 复盘产生可执行结论并被后续运营方案引用。 |
| REQ-013 | 数据、浏览器目录、日志、状态和失败原因在终端中可见。 |
| REQ-014 | 资料可长期管理、跨天聚合为主题，并从历史内容表现和复盘中重新获得上下文。 |
| REQ-015 | 用户可把长期知识对象放入持久关系画布并手动建立语义关系；Pi 默认使用当前页面上下文，有勾选时只使用选中对象。上下文随原有 Pi 会话轮次保存，不新增“上下文包”或独立静态快照对象。 |
| REQ-016 | 每个自媒体工作空间都可通过本根绑定 BrowserProfile 的已验证账号管理本根明确绑定的 X List，并把它作为可追溯情报信源；X Lists 不依赖发布平台子集，账号快照、绑定、缓存、操作和资料不跨 data-root；Agent 仅凭匹配的 task grant 与 precise execution grant 以精确 handles/冻结输入执行允许的副作用，其他外部写入按 UI-only 风险确认，所有操作均可见、可停止和可读回。 |
| REQ-017 | 一个 WMB 安装可登记多个完整 data-root 工作空间；每个根保存自己的业务数据和运行状态，同一时间只允许一个活动运行时。 |
| REQ-018 | AI 与 UK 是官方自媒体模板；Agent 只能使用 WMB 官方能力包生成配方提案，WMB 确定性校验，最终创建或激活只由 UI 精确确认。 |
| REQ-019 | 现有 AI data-root 必须零业务搬迁登记；UK 使用新 data-root 并证明不调用 AI 排行榜、AI 创作路线等真正的 AI 专属能力；官网/X Lists 走共享模块且只读本根配置，非活动根不得后台运行或接收迟到写入。 |
| REQ-020 | WMB 提供固定官网与 X Lists 渠道模块；模块定义和 BrowserProfile registry 安装内复用，每个 data-root 独立保存具体来源、绑定账号快照、启停、状态、grant、回执和资料。 |
| REQ-021 | 用户可用名称或 URL 添加任意公开网站，并用名称、URL 或 ID 添加当前根绑定账号可访问的 X List；WMB 必须真实解析、试读/匹配并按规范身份去重。Agent 可在 task grant 内准备候选；任何来源变更均须匹配 precise execution grant 或 UI-only 确认，不能由对话文本直接授权。 |
| REQ-022 | 今日情报自动运行当前根全部已启用渠道，渠道管理只在「设置」进行；「发现」只呈现外部发现内容。逐来源真实回执决定 succeeded/partial/needs_user/failed，零更新和空方案可成功；Agent 来源变更只可准备绑定精确 diff，执行时必须以 grant 和当前 identity/revision 复验。 |
| REQ-023 | 用户显式启动后，WMB 对本次冻结的 X Lists 做有界、活动根内的非 AI 后续观察，追加帖子可见指标快照并确定性计算传播速度；不常驻、不跨根、不使用官方 API、不自动社交。 |
| REQ-024 | Settings 提供安装级 Pi Skills 清单；系统必需 Skill 只读，普通共享 Skill 可新增、编辑和删除并同步到所有工作空间的新 Pi 进程。WMB 默认提供可管理的可信内容写作 Skill，强制事实型内容执行写前核查、证据驱动增厚和成稿后二次核查。 |
| REQ-025 | Pi 对话输入框用斜杆面板展示当前 Pi RPC 真实可执行的扩展命令、提示模板和 Skills；支持键鼠筛选与插入，选择不自动发送，Skill CRUD 在下一个 Pi 进程和下一次目录读取中生效。 |
| REQ-026 | 当前主模型不支持图片时，Pi 可通过固定版本的现成视觉扩展把明确识图请求委派给用户配置服务中的视觉模型，再由主模型使用文字观察继续工作；不得切换主模型、静默 fallback 或扩大 WMB 权限。 |
| REQ-027 | Owner 拥有目标、观点、grant 和最终责任；Pi 与外部 Agent 是对等 task-authorized workers。工作空间业务、知识、来源、内容、任务、Pi session、grant、账号绑定和回执彼此隔离，所有有价值结果进入根内业务事实；所有命令经同一 dispatcher 以完整 identity、replay、receipt 与 audit 执行，聊天/session 不是业务真相或授权。 |

需求优先级：P0 为真实闭环主干（REQ-001 至 REQ-013、REQ-017、REQ-019、REQ-027），任一缺失即闭环不成立；P1 为当前范围内的重要能力（REQ-014、REQ-016、REQ-018、REQ-020 至 REQ-023、REQ-026），缺失阻塞对应场景的验收但不阻塞最小闭环；P2 为便利与增强（REQ-015、REQ-024、REQ-025），缺失不阻塞闭环验收。

| ID | 产品级验收 |
| --- | --- |
| AC-001 | 资料可查看、追溯和再次使用。 |
| AC-002 | 运营方案具体、可执行并引用真实上下文。 |
| AC-003 | 两个不同外部 Agent 能接续同一内容且旧 revision 不覆盖新内容。 |
| AC-004 | 研究、创作、平台版本和素材均保存在终端。 |
| AC-005 | §5.4 的六种平台格式（X 文字、X 图片、X 视频、小红书图文、小红书视频、公众号文章）均可形成完整平台版本与素材交付。 |
| AC-006 | 人工发布后的平台链接可选回填；它不是产品完成前置条件。 |
| AC-007 | 网页指标形成可追溯历史快照。 |
| AC-008 | 复盘给出具体 keep、stop、change 行动。 |
| AC-009 | 后续方案显式引用历史资料、复盘或方法结论。 |
| AC-010 | 资料生命周期、主题关系和从资料到复盘的贡献链可由 UI、业务 API 和 Agent 双向读回。 |
| AC-011 | 画布布局、语义关系、当前页/多选上下文及 Pi 实际接收的对象引用均可从原有画布、会话和内容项目记录中读回；产品不创建或暴露独立“上下文包”与静态快照管理。 |
| AC-012 | 用户可在每个活动工作空间通过其显式绑定且账号匹配的 BrowserProfile 读回、创建及管理有权限的 List；新根有 defaultProfileId 的显式绑定，Owner 改绑或创建独立 profile 留下 UI 证据。创建和成员批量变更绑定准确 workspace、账号、profile、List/创建输入、变更集与 precise execution grant；编辑和删除还必须经过 UI-only 门。所有执行均产生真实平台读回；即使共享 defaultProfileId，绑定 List 的限量拉取、账号 snapshot、cache、operation 与资料也只进入当前根。 |
| AC-012a | 每个活动工作空间可通过其显式绑定且账号匹配的 BrowserProfile 读回、创建及管理有权限的 List。 |
| AC-012b | 新根有 defaultProfileId 的显式绑定；Owner 改绑或创建独立 profile 留下 UI 证据。 |
| AC-012c | List 创建和成员批量变更绑定准确 workspace、账号、profile、List/创建输入、变更集与 precise execution grant。 |
| AC-012d | List 编辑和删除经过 UI-only 门。 |
| AC-012e | 所有执行产生真实平台读回；即使共享 defaultProfileId，绑定 List 的限量拉取、账号 snapshot、cache、operation 与资料只进入当前根。 |
| AC-013 | AI data-root 原地登记前后的对象 ID、revision、文件、安装级登录态、Pi 会话及当前业务验收读回一致。 |
| AC-014 | AI 与 UK 切换通过安全排空和应用重启完成；旧 MCP 连接失效，非活动根无进程或写入，目标启动或切换进程失败会恢复原根。 |
| AC-015 | UK 官方模板和一个第三自媒体赛道都经同一业务 API 完成资料、方案、内容和至少一个平台版本；第三赛道的有效配方只能由 UI 确认。 |
| AC-016 | Settings 能在当前根列出官网/X Lists 模块的来源、就绪状态和最近回执；任意网站和同名 X List 都经过真实候选确认，稳定身份不靠名称猜测。Discover 不包含来源配置，只呈现榜单、所选 List 动态和其他发现内容。 |
| AC-017 | 一次今日情报冻结模块选择和来源 revision；至少一个来源真实检查即允许零更新/空方案成功，部分失败保留成功结果，全部阻塞在启动前进入 needs_user。 |
| AC-018 | Pi/外部 Agent 可在 task grant 内对准确官网完成真实解析和试读；批量新增或其他来源变更仅在 matching precise execution grant 或 UI 对准确 workspace/profile/source 身份和完整 diff 的确认后执行，并逐项读回。陈旧 grant/确认零写入，AI/UK 相同来源 fixture 全部根内隔离。 |
| AC-019 | 同一 X 帖子的多次真实读取形成 append-only 快照；两点速度、三点速度变化、字段状态和原始证据可读回，有界观察在根切换/退出时停止且趋势机会沿既有 topic/plan/content 链可追溯。 |
| AC-020 | 安装级 Skills 页面准确区分系统只读与普通可管理 Skill；普通 Skill 的新增、修改、删除在 fresh/existing data-root 的真实 Pi 资源清单中一致读回，删除默认 Skill 后重启不复活；可信内容写作 Skill 在真实 Pi 创作中引用可追溯证据、处置无证据主张并输出高信息密度成稿与简洁核查摘要。 |
| AC-021 | 当前 Windows 包的 Pi 输入框输入 `/` 能读回并筛选真实命令目录；键盘和鼠标选择均只插入原生命令，补充参数后由既有 Pi 发送链执行；安装级 Skill 增删改后列表刷新且被选 Skill 在真实 Pi 会话中加载。 |
| AC-022 | 纯文本请求只调用当前主模型；明确图片路径的识图请求调用配置的视觉模型并把真实文字结果返回主模型；视觉模型或图片失败可见且不产生猜测、主模型切换或业务写入。 |
| AC-023 | 一个真实根中，人、Pi 和外部 Agent 以同一 task grant 接续研究、分析、整理、入库、创作和复盘，所有价值结果由根内业务读回；同 request+同输入内容指纹只返回原回执、异指纹冲突；未匹配的 grant、workspace/runtime epoch、browserProfile 或账号零写入；不可变 publication snapshot 与人工最终发布边界可读回。 |
| AC-023a | 同一真实根中，人、Pi 和外部 Agent 以同一 task grant 接续研究、分析、整理、入库、创作和复盘，所有价值结果由根内业务读回。 |
| AC-023b | 幂等：同 request+同输入内容指纹只返回原回执；同 request+异指纹报冲突。 |
| AC-023c | 未匹配的 grant、workspace/runtime epoch、browserProfile 或账号身份，对应副作用零写入。 |
| AC-023d | 不可变 publication snapshot 可读回。 |
| AC-023e | 平台最终发布始终由用户手动完成，任何自动化路径不触发。 |

带字母后缀的 AC 是对父项的原子分解：父项通过当且仅当全部子项通过；下游文档引用父 ID 仍然有效。
