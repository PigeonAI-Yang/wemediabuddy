# WeMediaBuddy 产品需求文档

- 产品名：WeMediaBuddy
- 简称：WMB
- 中文定位：人类与 AI 共同操作的自媒体运营伙伴
- 文档状态：产品愿景已确认
- 日期：2026-07-27

## 1. 产品愿景

WeMediaBuddy 是一个面向个人 IP 的自媒体运营终端。

它让人类与不同 AI Agent 共享同一个运营现场，共同完成情报发现、资料沉淀、运营决策、内容生产、网页发布、数据采集和创作复盘。

产品最终形成以下长期闭环：

> 每日侦察 → 资料入库 → 机会判断 → 运营方案 → 人机讨论 → 内容创作 → 多平台适配 → 网页发布 → 数据采集 → 创作复盘 → 更新方法库 → 改进下一轮运营

WeMediaBuddy 的目标不是让 AI 批量制造内容，而是让人类与 AI 长期共同经营一个个人品牌，并把每天产生的情报、判断、内容和数据持续沉淀为资产。

## 2. 产品定位

### 2.1 产品是什么

WeMediaBuddy 是：

- 人类与 AI Agent 共用的自媒体运营工作空间；
- 保存资料、选题、方案、草稿、发布记录和复盘数据的统一事实源；
- 通过内部 API 和 MCP 向不同 Agent 开放运营能力的控制面；
- 通过固定浏览器和 CDP 操作内容平台的执行终端；
- 能根据创作结果持续改进后续运营决策的学习系统。

### 2.2 产品不是什么

当前阶段，WeMediaBuddy 不是：

- 传统自媒体管理后台；
- 单一 AI 写作工具；
- 热点链接聚合器；
- 套着聊天界面的大模型客户端；
- 自研 Agent 运行时或多 Agent 调度框架；
- 依赖各内容平台官方 API 的发布系统。

未来可以评估内置优秀的开源 Agent，但当前只通过 MCP 外接现有 Agent，不为尚未发生的需求建设复杂框架。

## 3. 目标用户

第一阶段只服务项目所有者本人。

用户以个人 IP 为内容主线，内容范围包括但不限于：

- AI 观点与行业观察；
- AI 工具、Skill 和工作流实测；
- AI 产品开发和创业记录；
- 使用 AI 找甲方、接单、报价和交付；
- AI 副业与商业机会；
- 重要新闻、争议、案例与职业变化。

产品先在真实账号和真实运营流程中验证，暂不以多人团队或通用 SaaS 为前提。

## 4. 核心原则

### 4.1 人机共享同一现场

终端界面和 AI Agent 操作同一份数据。任何有业务价值的资料、决策和产物都不能只留在某个 Agent 的聊天记录中。

### 4.2 人类掌舵，AI 持续协作

人类负责目标、价值判断、个人观点和最终责任。AI 负责侦察、整理、研究、策划、创作、执行和复盘。

### 4.3 Agent 中立

Codex、Claude Code、OpenCode、Oh My Pi、WorkBuddy 等外部 Agent 均可通过同一套 MCP 能力读取并操作项目状态。更换 Agent 后，工作可以从原状态继续。

### 4.4 浏览器是平台执行器

不依赖内容平台官方 API。终端通过 CDP 控制固定的专用浏览器，复用用户的真实登录状态完成网页发布、数据读取和其他平台操作。

### 4.5 结果必须反馈到下一轮创作

数据采集不是终点。复盘必须给出下一篇内容应当保留、停止或改变的具体行动，并影响后续运营方案。

## 5. 核心使用场景

### 5.1 每日情报侦察

AI 每天主动寻找“今天有什么值得受众知道、讨论、尝试或拿去赚钱”，覆盖：

- 重大 AI 新闻和行业变化；
- 新模型、新产品和新功能；
- 工具、Skill、提示词和工作流；
- AI 接单、交付、定价、副业和创业机会；
- 成功案例、失败案例和可复用经验；
- 行业争议、职业变化和观点冲突；
- 各内容平台正在升温的话题与表达形式；
- 用户自己的产品开发、实验和阶段性进展。

情报不能以无序链接结束。每条资料应记录：

- 原始来源；
- 内容摘要；
- 分类与关键词；
- 为什么值得关注；
- 与用户个人 IP 的关系；
- 可采用的创作角度；
- 适合的平台和内容形式；
- 时效性与建议优先级。

### 5.2 生成每日运营方案

AI 结合当日情报、用户定位、历史内容和账号表现生成可执行的运营方案。

方案应包含：

- 候选选题及优先级；
- 推荐理由与时效；
- 用户适合表达的观点和切入角度；
- 目标受众；
- 推荐平台；
- 推荐形式，如短图文、短视频、口播、帖子串或长文；
- 标题、开头和内容结构建议；
- 需要使用的资料；
- 预计工作量。

同一个核心选题可以针对不同平台形成不同表达，但不得为了多平台分发而制造重复、低价值内容。

### 5.3 人机共同创作

用户选择选题并补充观点后，AI 可以继续研究、组织证据、生成提纲、撰写初稿、修改内容、准备配图和适配平台。

终端应集中展示和关联：

- 原始资料与引用来源；
- 用户观点和补充信息；
- 提纲、草稿和修改结果；
- 标题、封面、正文和媒体素材；
- 各平台的最终发布版本；
- 从资料到成稿的关键决策。

### 5.4 网页发布

用户确认内容、平台、账号和素材后，终端通过固定浏览器执行发布。

基本过程：

1. 连接专用浏览器及其长期用户目录；
2. 复用用户已登录的平台状态；
3. 打开目标平台发布页面；
4. 填写内容并上传媒体；
5. 在明确确认后发布；
6. 记录平台、账号、发布时间、帖子地址和页面身份；
7. 将发布结果回写内容项目。

遇到登录失效、验证码、平台风控或页面异常时，自动化应暂停并允许用户在同一浏览器中接管。

### 5.5 数据采集与复盘

终端通过网页读取平台可见的内容和账号数据，不依赖平台官方 API。

可采集指标包括但不限于：

- 展示量、阅读量或播放量；
- 点赞；
- 评论或回复；
- 转发、引用或分享；
- 收藏；
- 平台页面提供的停留、完播等指标；
- 账号粉丝变化及其他页面可见数据。

终端应保存多个时间节点的数据快照，而不只保存当前总数，以便观察内容增长速度。

复盘需要关联：

- 选题与观点；
- 标题、封面和开头；
- 内容形式；
- 发布平台、账号和时间；
- 各阶段表现；
- 本次得到验证或被否定的判断；
- 下一轮具体应当保留、停止或改变的行动。

咨询、私域转化、订单和收入等暂时无法自动取得的数据，未来可支持手动补录，但不阻塞平台可见数据的首轮闭环。

## 6. Agent 与 MCP

### 6.1 当前策略

WeMediaBuddy 不自行承载大模型。现有 Agent 继续在各自环境中运行，通过 MCP 接入终端。

终端的核心业务操作先形成统一的内部 API，再暴露为 MCP 工具。界面与 Agent 调用同一业务能力，避免形成两套逻辑。

### 6.2 预期能力域

MCP 最终可覆盖：

- 查询、保存、整理和归类资料；
- 创建和评估选题；
-读取、生成和更新运营方案；
- 创建、读取和修改内容草稿；
- 管理平台版本和媒体素材；
- 准备和提交网页发布任务；
- 查询发布状态与结果；
- 读取账号和帖子数据；
- 创建和读取复盘结论；
- 查询历史方法和已验证经验。

具体工具名称与参数由后续接口设计确定，本 PRD 不提前固定。

## 7. 浏览器连接层

### 7.1 运行方式

- 使用固定的专用 Chromium 浏览器；
- 使用独立且持久保存的浏览器用户目录；
- 用户首次或失效后手动登录各平台；
- 终端通过 CDP 连接并执行网页操作；
- 所有平台自动化共享统一的上层发布和采集流程；
- 平台页面差异只在对应的薄适配层处理。

### 7.2 明确限制

网页结构、验证码、登录状态和平台风控具有不确定性，因此产品不承诺完全无人值守。

任何发布和采集任务都必须能明确显示：

- 当前操作的平台和账号；
- 执行状态；
- 成功后的可验证结果；
- 失败原因；
- 是否需要用户接管。

## 8. 核心数据资产

产品长期积累四类资产：

### 8.1 情报资产

来源、资料、新闻、案例、工具和行业变化。

### 8.2 观点资产

用户对问题的判断、立场、偏好、表达和已经形成的方法。

### 8.3 内容资产

选题、研究材料、草稿、平台版本、媒体素材和发布记录。

### 8.4 运营资产

账号数据、帖子数据快照、复盘结论以及经过实际结果验证的内容方法。

## 9. 核心对象

当前产品至少需要表达以下业务对象：

- 资料；
- 情报来源；
- 选题；
- 每日运营方案；
- 内容项目；
- 草稿与版本；
- 媒体素材；
- 平台；
- 账号；
- 发布记录；
- 指标快照；
- 复盘；
- 方法结论；
- Agent 操作记录。

本节只定义产品概念，不规定数据库表或技术实现。

## 10. 产品阶段策略

### 当前阶段

先证明一个真实账号上的最小闭环可以成立：

> 情报进入终端 → AI 生成可执行选题 → 人机完成内容 → 固定浏览器发布 → 网页采集结果 → AI 给出下一轮改进

首个具体平台、界面范围、采集节奏和实现顺序在下一阶段设计中确定。

### 后续阶段

在真实闭环稳定后，再按实际需求增加：

- 更多内容平台；
- 更完整的内容与素材协作；
- 手动补录商业转化；
- 多账号管理；
- 多 Agent 并行协作；
- 内置经过验证的开源 Agent；
- 面向团队或其他创作者的产品化能力。

以上均不是当前阶段的默认承诺。

## 11. 产品级验收标准

当以下条件同时成立时，产品愿景才算形成真实闭环：

1. AI 找到的资料能够在终端中查看、追溯和再次使用；
2. AI 能基于资料和历史结果生成具体可执行的运营方案；
3. 用户与不同外部 Agent 能接续操作同一个内容项目；
4. 内容从研究、创作到平台版本均保存在终端中；
5. 用户确认后，终端能通过固定浏览器完成一次真实发布；
6. 发布结果和帖子身份能够回写终端；
7. 终端能从网页取得该内容后续的可见指标并保存快照；
8. AI 能基于创作过程和结果提出下一轮具体改进；
9. 下一轮运营方案能够使用此前沉淀的资料、观点和复盘结论。

任何单次生成成功、网页点击成功或局部数据读取成功，都不能单独视为产品闭环完成。
