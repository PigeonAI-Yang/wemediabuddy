# 2026-08-18

## GitHub Actions CI/CD 工作流编写
- 分析了项目架构：Electron 43 + React 19 + TS + Vite，Electron Forge 7.11 打包，npm，默认分支 master。
- 关键约束：`media-runtime.lock.json` 锁定 win32-x64，打包脚本强制 Windows，故 CI 构建 job 必须 `windows-latest`。
- 新建 `.github/workflows/ci.yml`：typecheck-unit（ubuntu，ELECTRON_SKIP_BINARY_DOWNLOAD=1）+ build-windows（windows-latest, npm run package, 上传未打包产物）。
- 重写 `.github/workflows/release.yml`：tag v*/手动触发；校验标签版本、可选 Authenticode 证书还原、门禁(typecheck/test/package/e2e/e2e:gate)、npm run publish 到草稿 Release、SHA-256 校验和证据。
- 用 pyyaml 校验两份 YAML 语法均通过。

## Neo-Brutalism 作品集网站设计稿
- 在 Ardot 画布新建设计文件「WeMediaBuddy 作品集 · Neo-Brutalism」。
- 按用户指定严格规则：0 圆角、2px 硬边框、无阴影/渐变、极黑 #0A0A0A 打底、荧光绿 #C7F23D 点睛、撞色橙 #FF5A3D、米白 #EDEDE3。
- 全站 Space Grotesk Bold；因系统无 Black 字重，巨字用 Bold + 大字号 + 紧字距替代。
- 完成 6 屏结构：Hero（透明导航+巨字+宣言+AI 抽象视觉）、精选作品（两列 masonry 4 卡）、关于/能力（绿面板+数据网格/能力清单）、合作客户（荧光绿跑马灯条）、联系 CTA（双行巨字+邮箱按钮）、Footer（绿顶边框+社交）。

## WeMediaBuddy 应用宣传站
- 在 Ardot 画布新建独立设计文件「WeMediaBuddy 应用宣传站 · 墨夜紫罗兰」。
- 视觉决策：使用 WeMediaBuddy 产品自身的品牌系统（墨夜深色 #0b0b0b + 紫罗兰 accent #8b7cff + Inter），而非上一版作品集的荧光绿新粗野主义；保持大字、强对比，但克制可信。
- 完成 7 屏：Nav + Hero（主编办公桌产品视觉）+ 问题钩子 + 完整闭环（6 步卡）+ 班组（5 角色卡）+ 为什么是 WMB（2×2 特性卡）+ 散装工具 vs WMB 对比表 + 下载 CTA + Footer（紫罗兰顶边框）。
- 同时输出独立可部署 HTML：`designs/wemediabuddy-landing.html`。
