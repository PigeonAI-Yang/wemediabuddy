# WeMediaBuddy 长期资料管理与知识复利方案

状态：Owner 已批准实施  
日期：2026-07-29  
范围：CAP-002、CAP-003、CAP-004、CAP-011、CAP-012、CAP-015

## 1. 目标

把当前“收录资料并生成当天机会”升级为一个长期运营记忆闭环：

> 资料持续入库 → 核验与管理 → 跨天主题聚合 → 形成机会与内容 → 发布和复盘 → 历史上下文重现 → 改进下一轮判断

完成后，WMB 不只是保存链接，而是能回答：

- 这条资料是否核验过，是否仍值得观察；
- 它属于哪个长期主题，主题最近发生了什么；
- 它形成过哪些机会、内容和发布；
- 相关内容历史表现、复盘和方法结论是什么；
- 新资料到来时，过去哪些证据和经验应该重新出现。

## 2. 当前事实与根因

当前正式数据根有 17 条资料、4 版计划、7 个机会、9 个内容项目、2 条复盘和 2 条方法结论。已有 URL/指纹去重及资料→机会→内容→发布→指标→复盘链，但存在四个根因：

1. Library 复用 Today 最近 50 条读模型，第 51 条以后不可管理；
2. 资料没有核验/观察/归档状态，也没有可操作的长期管理入口；
3. `topics` 没有稳定键和资料关系，跨天同主题无法聚合；
4. 已有反馈链无法从资料或主题反向查询，新一轮 Pi 也读不到相关历史。

## 3. 设计原则

- SQLite 继续作为唯一事实源；UI、Pi 和外部 Agent 调用同一业务函数。
- 不引入向量库、embedding、图数据库、常驻 LLM 索引或新的调度系统。
- 不把相关性宣传成因果归因；界面统一称“关联表现”或“贡献链”。
- 原始资料、主题关系、机会引用和复盘引用必须保留真实 ID。
- “已形成机会”“已用于内容”“已发布”由关系实时推导，不重复保存为可漂移状态。
- 所有列表有明确上限、分页和稳定顺序；详情查询不随全库规模增加 SQL 次数。

## 4. 第一层：资料生命周期

### 4.1 两个正交状态

核验状态 `verificationStatus`：

- `pending`：待核验；
- `verified`：已核验；
- `disputed`：证据存在冲突；
- `rejected`：已判定不可用。

管理状态 `managementStatus`：

- `active`：正常使用；
- `watching`：持续观察；
- `expired`：时效已过；
- `archived`：人工归档。

一条资料可以同时是“已核验、观察中、已用于两篇内容”。使用进度由关系推导，不塞进单一状态。

### 4.2 长期资料列表

独立业务查询取代 Today 的 50 条快照，支持：

- 标题/摘要/关键词/证据搜索；
- 核验状态、管理状态、主题、是否形成机会、是否使用过滤；
- 稳定分页；
- 资料详情、原始证据、主题、机会、内容、发布、指标和复盘反向链接。

状态更新必须携带 `expectedRevision`，写入最小状态活动记录并可在重启后读回。

## 5. 第二层：跨天主题聚合

### 5.1 复用 topics

扩展现有 `topics`：

- `canonicalKey`：Agent 明确给出的稳定键，唯一；
- `kind`：`theme` 或 `event`；
- `summary`；
- `status`：`active`、`watching`、`dormant`、`archived`；
- 首次/最近出现时间。

一个资料可属于多个主题，关系角色为：

- `primary`：核心证据；
- `supporting`：支持材料；
- `background`：背景；
- `contradicting`：反方或冲突证据。

系统不根据标题相似度擅自合并。Pi 先查历史上下文，再复用稳定主题或显式创建新主题。

### 5.2 主题详情

主题详情按时间返回：

- 所有相关资料及关系角色；
- 相关机会和当时评级；
- 形成的内容项目；
- 发布与最新指标；
- 最终复盘和方法结论。

主题列表显示最近活跃时间、资料数、机会数、内容数和发布数。

## 6. 第三层：效果反哺与历史重现

### 6.1 关系真源

沿用现有对象：

`topic/source → plan_item → content_project → platform_version → publication → metric_snapshot → review → method_finding`

不新增“效果归因”表。一个有界的知识上下文查询负责聚合真实关系。

### 6.2 下一轮使用

每日情报处理每条已核验资料时：

1. 保存或更新资料；
2. 查询相同主题的历史上下文；
3. 显式创建/复用主题并关联资料；
4. 把 sourceId/topicId 写入持久检查点；
5. 最终机会必须带真实 `topicId`；
6. 仅在相关历史存在时引用真实 reviewId/methodFindingId，禁止塞入无关爆款经验。

### 6.3 确定性重新发现

不用推荐模型，提供三组可解释列表：

- 高评级但从未形成内容；
- 观察中且最近出现新资料；
- 长期待核验或已过期。

每条都显示“为什么此刻重新出现”。

## 7. 业务 API、IPC 与 MCP

共用业务函数：

- `knowledge.listSources`
- `knowledge.updateSourceStatus`
- `knowledge.listTopics`
- `knowledge.getContext`
- `knowledge.recordBatch`
- `knowledge.listRediscovery`

Pi/MCP 最小写回协议：

- `wmb_get_knowledge_context`
- `wmb_record_knowledge`

`recordBatch` 使用 `request_id` 幂等；引用不存在的资料或主题必须失败。相同 source/topic/relation 重放不得产生重复关系。

## 8. UI

资料库二级页：

- `AI 榜单`
- `资料`
- `主题`
- `重新发现`

资料页：

- 独立分页搜索和状态筛选；
- 每行显示核验、管理状态、主题与派生使用计数；
- 点击打开详情，显示结论、证据、主题和使用记录；
- 可修改核验/管理状态。

主题页：

- 主题聚合列表；
- 主题详情时间线；
- 显示相关机会、内容、发布指标、复盘和方法结论。

重新发现页：

- 三组确定性结果；
- 明确重现原因；
- 可进入资料或主题详情。

## 9. 迁移与兼容

- 迁移只追加字段、关系表和索引，不重写资料正文。
- 现有资料默认 `pending + active`。
- 现有 topic 保留 ID；没有 canonicalKey 的旧 topic 不自动合并。
- 现有 plan/source JSON 引用继续兼容，新的主题与资料关系以关系表为真源。
- 迁移前后资料、计划、内容、发布和复盘数量必须一致。

## 10. 验收

1. 生命周期：状态更新带 revision；陈旧写入失败；重启后保持；使用计数由真实关系派生。
2. 主题：两个日期、三个资料显式关联同一主题；重复写入不增行；无关资料不出现。
3. 反馈链：构造 source→plan→project→publication→metric→review/finding，主题上下文读回全部真实 ID；无指标显示未知而非 0。
4. 下一轮：新资料查询到相关历史，计划引用真实 topic/review/finding；无相关历史时不强塞复盘。
5. 长期列表：至少 250 条资料可分页找到第 200 条；不再受 Today 50 条限制。
6. UI：资料、主题、重新发现可操作；Pi 打开时无横向溢出；详情不依赖 hover。
7. 工程：聚焦回归、typecheck、轻量门禁和开发版实机读回通过。

## 11. 非目标

- 向量语义搜索；
- 自动主题合并或拆分；
- 知识图谱画布；
- 自动因果归因；
- 新的 Agent/定时任务平台；
- 自动改写历史判断；
- 多人权限与云同步。
