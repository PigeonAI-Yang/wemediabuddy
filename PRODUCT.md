# Product

## Register

product

## One-line form

WeMediaBuddy（WMB）是 **AI 驱动的自媒体人机协同应用终端**，不是传统内容 CMS，也不是「人写为主、AI 聊天为辅」的写作 IDE。

类比（帮助对齐，不是技术依赖）：

| 形态 | 像什么 | 人干什么 | Agent 干什么 |
|---|---|---|---|
| **拒绝** | VS Code 型 | 人手做主流程 | 侧栏辅助 |
| **要对齐** | Codex Desktop 型 | 定目标、批呈报、派工、终审与担责 | 主路径生产：采、判、归主题、出选题、起草、复盘准备 |

## Users

以个人 IP 为主线的自媒体从业者（第一阶段：Owner 本人）。  
角色定位是 **主编 / 操作员**，不是资料员，也不是默认一线码字写手。

## Product Purpose

WMB 让人类与内置 Pi / 外部 Agent **共享同一本地业务现场**，共同完成：

情报发现 → 资料沉淀 → **机会判断与选题呈报** → 人机讨论与批准 → 内容生产 → 多平台适配 → 人工确认发布 → 指标与复盘 → 资产回流。

首屏（今日）优先回答：

> **今天什么值得做？为什么？我（或 Agent）应该怎么讲？**

每个判断必须能追溯到已入库资料。

## Product form constitution（产品形态宪法）

Owner lock 2026-08-07（会话定稿，写入合同）。后续设计/实现/验收不得无明示改口而偏离。

### C1. Agent 主路径，人终审

1. **默认干活的是 Agent**（后台流水线 + 前台 Pi/外部 Agent），不是人从零做编辑全流程。
2. **人的主业**是：定方向与红线、批呈报、派工、监工、发布确认、承担最终责任。
3. 人手深度改稿/整理可以存在，但是 **旁路**，不是默认上班方式。
4. 若某设计逼主编从 **裸资料** 自己倒推选题方案，视为 **形态违规**（VS Code 基因回潮）。

### C2. 今日 = 主编办公桌

1. 今日只放 **需要主编决策或知情** 的呈报。
2. 上桌对象优先：可批 **选题**、主题之 **今日须知进展**、在写任务、今日运行状态。
3. **不上桌**：员工应自己完成的半成品原料堆、应用内部健康大屏、与决策无关的流水。
4. 其它页面是「下去转转」的科室，不是把办公桌搬走。

### C3. 业务对象分层（本体论）

| 对象 | 定义 | 主操作者 |
|---|---|---|
| **资料** | 证据与原件 | Agent 采集整理；人抽查/移出 |
| **选题** | 一炮可批可写的内容方案 | Agent 起草呈报；人批/否/改 |
| **主题** | 值得 **长期关注** 的正式容器（多资料/多选题/多稿/复盘） | Agent 归纳归并；人确认长期跟或停 |
| **创作项目** | 某一炮的稿与版本 | Agent 主写；人监工/改刀 |
| **发布/结果** | 签发与评报 | 人确认发布；Agent 助复盘 |

不变量：

- **长期关注的正式身份 = 主题**，不是今日底栏杂物钉，也不是裸资料。
- **Agent 认为值得长期跟** → 应归入或升级为 **主题**（LLM 编辑判断，不得以正则/标题串匹配当主归纳）。
- **一炮就结束** 的可以只有选题+稿，不必强行建主题。
- 主题内可以有 **多篇** 创作稿与多条选题、多份资料。

### C4. 「持续关注」语义（纠偏）

1. **目标语义**：今日「持续关注」= 活跃 **主题** 中「今日仍值得主编看一眼」的 **桌面投影**（新进展/将过期/待知悉）。
2. **禁止语义**：把「没做成选题的资料」堆给主编 = 把做选题的活甩回人类。
3. **现状债务（不得加深）**：当前实现仍由 `work_carry_items` ferment carry 混 `plan_item`/`source`，UI 以 `storyKey`/`sameStory` 合并；**这不是**长期关注正式身份。后续里程碑必须 **替换** 该 rail 为 topic-progress 投影（见 `docs/spark/2026-08-07-product-form-agent-desk-constitution.md` §5/§8 与 PLAN M-5000）。

### C5. 后台 vs 前台

| | 后台 Agent | 前台 Pi（贴页） |
|---|---|---|
| 何时 | 定时/跑批/扫完触发 | 人在场、点名、共写 |
| 产出 | 入库、归主题、选题呈报、进展标记 | 按批准写改、纠偏、页内整理 |
| 不上桌的 | 半成品判断过程 | 伪造权限、静默平台副作用 |

### C6. 页面 = 编辑部房间（巡视模型）

| 页 | 房间 | 主编来此 |
|---|---|---|
| 今日 | 主编桌 | 等呈报、批、派 |
| 发现 | 前线 | 看看外面；不在此批稿主业 |
| 选题 | 策划组提案夹 | 全量决策台账 |
| 主题 | 专题组档案室 | 长期线家底 |
| 资料库 | 资料室 | 原件与整理 |
| 创作 | 写字间 | 派写、监工 |
| 发布 | 签发台 | 确认发出 |
| 结果 | 评报栏 | 复盘 |
| 画布 | 关系墙 | 理关系 |
| 设置 | 总机 | 模型/渠道/账号 |

### C7. 反形态（明确不做）

- 传统自媒体 CMS / 运营后台（人填表为主）。
- **VS Code 型写作 IDE**（人手编辑画布为默认主路径，AI 仅侧栏聊天）。
- 套壳聊天客户端 / 无来源热榜 / SaaS 驾驶舱。
- 自动点击平台最终发布。
- 用正则当主题归纳主引擎。
- 把 Agent 半成品原料默认堆上主编桌。
- 多 Agent 工作流编排图；用 prompt 冒充写权限。

### C8. 固定编制 × 授权能力注册表（Owner lock 2026-08-07）

1. 内置侧固定工种：**桌助、记者、策划（兼复盘）、写手、资料员**；跨赛道稳定。换赛道只换赛道包（Skill/信源/受众），不重做编制与权限矩阵。
2. **写手**对资料库=只读借阅；**资料员**负责资料库与主题家底整理，不得定题/主笔/复盘写权。资料员的主题结构整理先形成可批提案，Owner 批准后整批生效；真实冲突时系统自动把工作退回资料员按最新现场重提，不得把完整整理退回给 Owner 手工完成。
3. 权限扩展唯一入口=**Capability 注册表**（`src/shared/agent-capabilities.ts`）；有效写权=Grant∩角色能力∩Precise。发布最终点击与硬删永不进角色自动权。
4. 一级 **智能体** 页展示班组状态；今日仅一行值班摘要。P0 完成前不做可配置权限 UI。
5. 详案：`docs/spark/2026-08-07-role-permission-design.md`。
6. 主题整理审批详案：`docs/spark/2026-08-10-topic-maintenance-approval-design.md`。
7. 主题审批冲突合同与自动重提修订：`docs/spark/2026-08-10-topic-maintenance-conflict-reproposal-design.md`。

### C9. 班组多实例不变量（Owner lock 2026-08-08；2026-08-08 落档正式施工）

1. **固定角色 ≠ 槽位**：五角色（桌助/记者/策划/写手/资料员）是跨赛道稳定的劳动分工类型，不绑定槽位数量；**同角色可同时存在多个实例且显式可见**。
2. **实例 ≠ 永久员工**：实例按任务创建、任务终态后退出活动视图；无「待命」实例、无持久员工档案、员工不自动多跳（无编排图）。**唯一窄例外（ResearchJob 单跳续派，Owner lock 2026-08-10）**：研究任务达门槛（终态 + EvidencePack）后，系统终态处理器按原任务边界、原角色自动创建一次 successor 工单（新 jobId；续派即新建工单、非重试）；research→research 禁止；续派后原角色再次缺料 → 不自动再派研究，转 `needs_user` 交人决策；不扩展为通用多跳。
3. **实例按任务创建，终态退出活动视图**：succeeded/partial/failed/cancelled 立即退出；**needs_user 停留活动视图「等你批」直至用户处理/关闭，期间不占并发、不持 lease/grant/锁**，不自动重试。
4. **不预设空槽**：五角色分组始终可见，空角色显示「当前无任务」，不画空槽、不虚构「待命」。
5. **共享并发池 = 纯系统容量**：`maxWorkers`（0..7，0=停用派工，默认 2）是全角色共用的并行工单上限，不构成任何角色的配额或编制。
6. **主管（desk）= 软件内主管 / 主编席（2026-08-10 主管授权翻转，Owner lock）**：持全站内部 standing 写权（全部 grantable 业务能力命令 ∪ INFRA_GRANT_COMMANDS，含内部准备命令，不含三类红线执行命令）；任一页 dock（含发布页、智能体页）绑定主管签发覆盖全量内部命令的 grant；内部审批归主管；主管不进员工槽（`spawn(roleId:'desk')` 仍被拒）、单跳派工；红线恰三类（最终平台发布、硬删执行、外部平台变更执行）最终动作仅人类 UI 新鲜确认（Precise Gate + Owner UI），主管可准备不可执行；人 = 三类红线最终动作的确认与担责，不是软件日常监督者。
7. **实例权限 = 任务精确授权 ∩ 角色能力 ∩ 资源边界**；**dispatcher 对象级硬隔离（施工必需）**：按工单对象键（businessDate/projectId/sourceIds/scope）校验，跨对象写请求拒绝（BLOCKED + 审计）。
8. **发布/硬删红线不变**：最终发布点击与硬删（及平台副作用）仅人类 UI，对任何实例、任何 grant 组合不可达。
9. **实例一等身份 = jobId**：角色编号（如「记者 #2」）仅活动期显示、重启后重新计数，不进入契约/存储；跨重启指认只用 jobId。
10. **持久续派合同**：spawn 合同把 jobId/roleId/brief/边界参数写入既有 `context_refs_json`，重启后完整指认并一键续派；**「不新增表/列」明确限定于续派合同与 `agent_tasks`/`task_grants`/`execution_grants` 三表结构（结构零改动）**；`research_claims` 为 REQ-031/CAP-028 授权的唯一研究业务表（证据判定/查询审计面），非续派合同表。
11. **不新增通用角色/云/多租户/平台 API**：角色 Skill / lane pack 零授权命令与角色绑定；Capability registry 除 REQ-031/CAP-028 正式登记 `cap.research`、其可选读白名单及 `research` intent 投影外不扩展，运行层只落对应读硬门与对象级强制点。
12. **不做可配置权限 UI**：P0（注册表 + 角色过滤 + 只读班组页）完成前零权限配置控件。
13. **同任务同刻只归属一个活动实例**：scan→judge 可复用同一 `agent_task`（实例与任务非 1:1），进度/等你批投影不双计。
14. 详案（正式施工 Owner lock；TASKS doing 仍是唯一施工许可）：`docs/spark/2026-08-08-agent-crew-multi-instance-design.md` §17。

## Narrative Priority

对外叙事第一价值顺序：

1. 素材收集  
2. 素材整理  
3. 机会判断（今天写什么、为什么、怎么写）——**Agent 呈报，人批**  
4. 人机共同创作——**Agent 主写，人监工**  
5. 复盘与资产沉淀  
6. 网页发布与采集（执行环，非宣传主角）

验收口令：

> 若有人把 WMB 听成「自动发帖工具」或「又一个 AI 写作框」，说明叙事说歪了。  
> 正确记忆应是：**Agent 干活的自媒体协同终端；人定事、批事、担责。**

## Brand Personality

敏锐、可信、克制。像有判断力的私人主编台 + 员工班子，不像运营后台，也不像程序员 IDE。

## Design Principles

1. 可批对象先于原料与内部状态。  
2. 一个明确首选胜过平铺。  
3. 判断必须带理由、角度与资料追溯。  
4. UI、Pi、外部 Agent 同一事实源。  
5. 系统默认正常；异常才诊断。  
6. **形态优先于功能堆砌**：新功能先过 C1–C7，再进 TASKS。

## Accessibility & Inclusion

正文和关键控件满足 WCAG AA；不只依赖颜色；键盘焦点清晰；尊重 reduced motion。
